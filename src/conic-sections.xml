<?xml version="1.0" encoding="utf-8"?>
<d:chapter version="5.0" xmlns:d="http://docbook.org/ns/docbook"
           xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink"
           xmlns:mml="http://www.w3.org/1998/Math/MathML">
  <?dbhtml filename="conic-sections/index.html" ?>
  <d:title>Conic Sections</d:title>
  <d:para>
    In this chapter we will study parabola, ellipse and hyperbola, their properties and elements like
    tangents and normals. The study of conic sections combines geometry and algebra. Using the coordinate
    plane, every conic can be represented by an equation in $x$ and $y$, allowing geometric properties to be
    studied analytically. Conics have rich mathematical structures involving focus, directrix, axis,
    eccentricity, tangents, normals, and chords.
  </d:para>
  <d:para>
    Pair of straight lines, circles, parabolas, ellipses and hyperbolas are sections of a right circular
    cone by a plane, and so they are called conic sections.
  </d:para>
  <d:para>
    As you can see in the <xref linkend="cs1" /> that when a plane cuts the right circular cone perpendicular
    to the axis of the cone then we get a circle, when the plane cuts the cone parallel to the generator we
    get a parabola, when the plane is parallel to the axis we get hyperbola, and when the cone is neither
    parallel nor perpendicular to axis or generator we get an ellipse.
  </d:para>
  <d:sect1>
    <d:title>Definition</d:title>
    <d:para>
      A conic section is defined as the locus of a point, which moves in a plane such that the ratio of its
      distance from a fixed point and a fixed line is a constant.
    </d:para>
    <d:para>
      The fixed point is called the <d:emphasis role="italic">focus</d:emphasis> and the fixed line the
      <d:emphasis role="italic">directrix</d:emphasis> of the conic section. The constant ratio is called
      <d:emphasis role="italic">eccentricity</d:emphasis> of the curve and is usually denoted by
      <m>$e$</m>. One such conic section is shown below:
    </d:para>
    <d:figure xml:id="cs1">
      <d:title>Conic Sections</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/conic sections.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:figure xml:id="cs2">
      <d:title>A Conic Section</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/6_1.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      If <m>$e = 1$</m>, then the resulting coninc is a <d:emphasis role="italic">parabola</d:emphasis>, if
      <m>$e &lt; 1$</m>, then the resulting conic is an <d:emphasis role="italic">ellipse</d:emphasis>,
      and if <m>$e > 1$</m>, then the resulting conic is a <d:emphasis
      role="italic">hyperbola</d:emphasis>. In the given diagram <m>$\frac{PS}{PN} = e$</m>.
    </d:para>
    <d:para>
      The straight line perpendicular from the focus to the directrix is called the <d:emphasis
      role="italic">axis</d:emphasis> of the conic. The point of intersection of the conic and its axis us
      called the <d:emphasis role="italic">vertex</d:emphasis> of the conic.
    </d:para>
    <d:para>
      In the given diagram <m>$S$</m> is the focus, <m>$AN$</m> the directrix, <m>$AS$</m> is the axis and
      <m>$A$</m> is the vertex of the conic.
    </d:para>
    <d:sect2>
      <d:title>Equation of a Conic</d:title>
      <d:figure xml:id="cs3">
        <d:title>A Conic Section</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_2.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let <m>$S(\alpha, \beta)$</m> be the focus and <m>$QN$</m> the directrix whose equation is <m>$Ax +
        By + C = 0$</m>.
      </d:para>
      <d:para>
        Let <m>$P(x, y)$</m> be any point on the conic section. From <m>$P$</m> draw <m>$PN \perp
        QN$</m>. Let <m>$e$</m> be the eccentricity, then by definition <m>$\frac{PS}{PN} = e \Rightarrow
        PS^2 = e^2PN^2$</m>.
      </d:para>
      <d:para>
        <m>$PS^2 = (x - \alpha)^2 + (y - \beta)^2$</m> and <m>$PN^2 = \frac{(Ax + By + C)^2}{A^2 + B^2}$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow (x - \alpha)^2 + (y - \beta)^2 = \frac{e^2(A x + B y + C)^2}{A^2 + B^2}$</m>, which
        on simplification will have the form <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m>, where <m>$a, b, c, f,
        g, h$</m> are constants.
      </d:para>
      <d:para>
        <d:emphasis role="bold">Corollary:</d:emphasis> Let the focus <m>$S$</m> lie on the directrix
        <m>$QN$</m> and <m>$S$</m> be taken as the origin and <m>$QN$</m> be taken as the
        <m>$y$</m>-axis. Let <m>$P(x, y)$</m> be any point on the conic then
      </d:para>
      <d:para>
        <m>$\frac{PS}{PN} = e \Rightarrow \frac{\sqrt{x^2 + y^2}}{|x|} = e \Rightarrow \left(e^2 -
        1\right)x^2 = y^2$</m>, which is a pair of straight lines through the focus or the origin.
      </d:para>
      <d:para>
        <d:emphasis role="bold">Corollary:</d:emphasis> The general equation of the second degree <m>$ax^2 +
        2y^2 + 2hxy  + 2gx + 2fy + c = 0$</m> represents a parabols if <m>$h^2 = ab$</m> and <m>$\Delta \neq
        0$</m>, where <m>$\Delta = abc + 2fgh - af^2 - bg^2 - ch^2$</m> or alternatively a second degree
        equation represents a parabola if second degree terms form a complete square and <m>$\Delta neq
        0$</m>.
      </d:para>
      <d:para>
        The second degree equation will represent an ellipse if <m>$h^2 &lt; ab$</m>. We will see that a
        circle is a special case of an ellipse.
      </d:para>
      <d:para>
        The second degree equation will represent a hyperbola if <m>$h^2 > ab$</m> and <m>$\Delta \neq
        0$</m> and a pair of straight lines if <m>$\Delta = 0$</m>.
      </d:para>
    </d:sect2>
  </d:sect1>
  <d:sect1>
    <d:title>Parabola</d:title>
    <d:para>
      A parabola is the locus of a point which moves so that its distance from a fixed point, called the
      <d:emphasis role="italic">focus</d:emphasis>, is equal to the distance from a fixed line, called the
      <d:emphasis role="italic">directrix</d:emphasis>.
    </d:para>
    <d:para>
      The straight line perpendicular from the focus to the directrix is called the <d:emphasis
      role="italic">axis</d:emphasis> of the parabola.
    </d:para>
    <d:para>
      The equation of the parabola can be obtained if its directrix and focus are given.
    </d:para>
    <d:figure xml:id="cs4">
      <d:title>A Conic Section</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/6_3.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      Let <m>$S$</m> be the focus and <m>$QM$</m> the directrix of the parabola. From <m>$S$</m> draw
      <m>$SQ$</m> perpendicular to the directrix. Let <m>$OP$</m> be the middle point of <m>$QS$</m>. Take
      <m>$O$</m> as the origin and <m>$OS$</m> as the <m>$x$</m>-axis and <m>$OY$</m> through <m>$O$</m>
      perpendicular to <m>$OS$</m> as the <m>$y$</m>-axis.
    </d:para>
    <d:para>
      Let <m>$QS = 2a$</m> then <m>$QO = OS = a$</m>.
    </d:para>
    <d:para>
      Now <m>$S = (a, 0)$</m> and the equation of <m>$QM$</m> is <m>$x = -a$</m>. Let <m>$P(x, y)$</m> be
      any point on the parabola. Join <m>$PS$</m> and drop perpendicular <m>$PM$</m> on <m>$QM$</m>.
    </d:para>
    <d:para>
      Be definition of the parabola <m>$PS = PM \Rightarrow (x - a)^2 + y^2 = |x + a|^2$</m>
      <m>\[y^2 = 4a x\label{eq:6.1}\tag{6.1}\]</m>
    </d:para>
    <d:para><d:emphasis role="bold">Notes:</d:emphasis></d:para>
    <d:orderedlist>
      <d:listitem>
        If the point <m>$(x, y)$</m> lies on the parabola <m>$y^2 = 4ax$</m> then point <m>$(-x, -y)$</m>
        will also lie on the parabola. Hence, the parabola is symmatrical about its axis.
      </d:listitem>
      <d:listitem>
        The point of intersection of the axis of a parabola and the parabola itself is called the
        <d:emphasis role="italic">vertex</d:emphasis> if the parabola.
      </d:listitem>
      <d:listitem>
        If the perpendicular drawn from a point <m>$P$</m> to the axis of the parabola meets the parabola at
        its symmateric point <m>$P'$</m> then <m>$PP'$</m> is called a <d:emphasis role="italic">double
        oridinate</d:emphasis>.
      </d:listitem>
      <d:listitem>
        The double ordinate which passes through the focus is called the <d:emphasis role="italic">latus
        recturm</d:emphasis> of the parabola. The length of latus rectum of <m>$y^2 = 4ax$</m> is
        <m>$4a$</m>.
        <d:para>
          The equation of the latus ractum of the parabola <m>$y^2 = 4ax$</m> is <m>$x = a$</m>.
        </d:para>
      </d:listitem>
      <d:listitem>
        Joining any two points on a parabola yields a line segment called <d:emphasis
        role="italic">chord</d:emphasis>.
      </d:listitem>
      <d:listitem>
        If a chord passes through focus of a parabola is called <d:emphasis role="italic">focal
        chord</d:emphasis>.
      </d:listitem>
      <d:listitem>
        The distance of any point on a parabola from the focus of the parabola is callled <d:emphasis
        role="italic">focal distance</d:emphasis>.
      </d:listitem>
    </d:orderedlist>
    <d:para>
      The other parabolas having the same length of latus rectum as that of <m>$y^2 = 4ax$</m> are <m>$y^2 +
      4a x = 0, x^2 = 4ay$</m> and <m>$x^2 + 4a y = 0$</m>.
    </d:para>
    <d:sect2>
      <d:title>
        Intersection of a Line and a Parabola
      </d:title>
      <d:para>
        Consider a straight line given by the equation <m>$y = mx + c$</m> and a parabola given by the
        equation <m>$y^2 = 4ax$</m>.
      </d:para>
      <d:para>
        Solving the equations gives us <m>$(m x + c)^2 = 4a x \Rightarrow m^2x^2 + 2x(m c - 2a) + c^2 =
        0$</m>.
      </d:para>
      <d:para>
        The above equation, which is a quadratic equation in <m>$x$</m> will have two roots. If the roots
        are real i.e. <m>$(mc - 2a)^2 - m^2c^2 > 0$</m>, then the will cut the parabola at two different
        points. If <m>$(m c - 2a)^2 - m^2c^2 = 0$</m>, then there will be a repeated root and the line will
        be a tangent to the parabola and if <m>$(m c - 2a)^2 - m^2c^2 &lt; 0$</m>, then roots will be
        imaginary and the line will not inttersect with the parabola.
      </d:para>
      <d:para>
        We see that <m>$(m c - 2a)^2 - m^2c^2 = 0$</m> is the condition for tangency. Thus, <m>\[ c =
        \frac{a}{m} \tag{6.2}\]</m>
      </d:para>
      <d:para>
        Thus, any line of the form <m>$y = mx + a/m$</m> will be a tangent to the parabola.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of a Tangent at a Given Point on a Parabola</d:title>
      <d:para>
        Let the equation of parabola be <m>$y^2 = 4a x$</m>. Let <m>$P = (x_1, y_1)$</m> and <m>$Q(x_2,
        y_2)$</m> be any two points on the parabola. The equation of <m>$PQ$</m> will be given by
      </d:para>
      <d:para>
        <m>$y - y_1 = \frac{y_2 - y_1}{x_2 - x_1}(x - x_1) \ldots$</m> (1)
      </d:para>
      <d:para>
        Since both the points lie on the parabola, therefore <m>$y_1^2 = 4a x_1$</m> and <m>$y_2^2 = 4a x_2$</m>
      </d:para>
      <d:para>
        Subtracting these two equations yields <m>$(y_2 + y_1)(y_2 - y_1) = 4a(x_2 - x_1) \Rightarrow
        \frac{y_2 - y_1}{x_2 - x_1}(x - x_1) = \frac{4a}{y_2 + y_1}$</m>
      </d:para>
      <d:figure xml:id="cs5">
        <d:title>A Tangent to a Parabola</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_4.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Putting this in (1) gives us <m>$y - y_1 = \frac{4a}{x_2 + x_1}(x - x_1)$</m>
      </d:para>
      <d:para>
        Now, when <m>$Q \rightarrow P, x_2 \rightarrow x_1$</m> and <m>$y_2 \rightarrow y_1$</m> so the above equation becomes
      </d:para>
      <d:para>
        <m>$y - y_1 = \frac{4a}{2y_1}(x - x_1) \Rightarrow y y_1 = 2a x + y_1^2 - 2a x_1$</m>
      </d:para>
      <d:para>
        Substituting <m>$y_1^2 = 4a x_1$</m> we have <m>\[ y y_1 = 2a(x + x_1) \tag{6.3}\]</m>
      </d:para>
      <d:para>
        which is the equation of the required tangent.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of a Normal at a Given Point on a Parabola</d:title>
      <d:para>
        We have obtained the equation of the tangent to a parabola <m>$y^2 = 4ax$</m> at <m>$(x_1, y_1)$</m>
        as <m>$yy_1 = 2a(x + x_1) \ldots$</m> (1), The slope of tangent is <m>$\frac{2a}{y1}$</m>.
      </d:para>
      <d:para>
        The normal will be perpendicular to the tangent, and hence, its slope will be <m>$-\frac{y_1}{2a}$</m>.
      </d:para>
      <d:para>
        Thus equation of normal would be <m>\[y - y_1 = -\frac{y_1}{2a}(x - x_1) \tag{6.4}\]</m>
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of the Normal in terms of its Slope</d:title>
      <d:para>
        The normal to the parabola <m>$y^2 = 4ax$</m> at <m>$(x_1, y_1)$</m> is <m>$y - y_1 =
        -\frac{y_1}{2a}(x - x_1)$</m>.
      </d:para>
      <d:para>
        Let <m>$-\frac{y_1}{2a} = m \Rightarrow y_1 = 2am$</m>. Since <m>$y_1^2 = 4a x_1 \Rightarrow 4a^2m^2
        = 4ax_1 \Rightarrow x_1 = a m^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow y - (-2a m) = m(x - a m^2) \Rightarrow y = mx - 2am - am^3$</m>, which is the
        equation of the normal in terms of its slope.
      </d:para>
      <d:para>
        <d:emphasis role="bold">Notes:</d:emphasis> The equation of the normal obtained is <m>$y = mx - 2am
        - am^3$</m>. This normal passes through <m>$(x_1, y_1)$</m>. Thus, <m>$a m^3 + (2a - x_1) + m + y_1
        = 0$</m>.
      </d:para>
      <d:para>
        This is a cubic equation in <m>$m$</m>, which will have three roots of which at least one will be
        real because complex roots occur in pair.
      </d:para>
      <d:para>
        Any three points on the parabola, normals at which pass through a common point, are called co-normal
        points.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Parametric Equation of the Parabola <m>$y^2 = 4a x$</m></d:title>
      <d:para>
        The point <m>$P$</m> whose coordinates are <m>$x = at^2, y = 2at$</m> lies on the parabola <m>$y^2 =
        4a x$</m>, which can be trivially verified by substituting in <m>$y^2 = 4a x$</m>.
      </d:para>
      <d:para>
        This representation in which <m>$x$</m> and <m>$y$</m> are represented using a single parameter
        <m>$t$</m> is called the parametric equation of the parabola.
      </d:para>
      <d:para>
        The point <m>$(at^2, 2at)$</m> is also referred to as the point '<m>$t$</m>'.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of a Chord of a Parabola</d:title>
      <d:para>
        Consider two points <m>$(at_1^2, 2at_1)$</m> and <m>$(at_2^2, 2at_2)$</m> on the parabola forming a
        chord.
      </d:para>
      <d:para>
        The equation will be given by <m>$y - 2at_1 = \frac{2at_2 - 2at_1}{at_2^2 - at_1^2}(x - at_1^2)$</m>
      </d:para>
      <d:para>
        <m>\[y(t_1 + t_2) = 2x + 2a t_1t_2\tag{6.5}\]</m>
      </d:para>
      <d:para>
        is the equation of the the chord.
      </d:para>
      <d:para>
        If this line passes through the focus i.e. <m>$(a, 0)$</m> then <m>$2a + 2at_1t_2 = 0 \Rightarrow
        t_1t_2 = -1$</m>. Thus, if one point is <m>$t$</m> then the condition for the chord to pass through
        the focus requires the other point to be <m>$-\frac{1}{t}$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Tangent and Normal at any Point <m>$t$</m></d:title>
      <d:para>
        The tangent to the parabola <m>$y^2 = 4ax$</m> at the point <m>$(at^2, 2at)$</m> is given by
      </d:para>
      <d:para>
        <m>$y(2at) = 2a(x + a t^2) \Rightarrow ty = x + at^2$</m>.
      </d:para>
      <d:para>
        The normal to the parabola <m>$y^2 = 4ax$</m> at the point <m>$(at^2, 2at)$</m> is given by
      </d:para>
      <d:para>
        <m>$y - 2at = -t(x - at^2) \Rightarrow y + t x = 2at + at^3$</m>.
      </d:para>
      <d:para>
        We see that two tangents at <m>$t_1$</m> and <m>$t_2$</m> have <m>$(a t_1t_2, a(t_1 + t_2))$</m> as
        their point of intersection and the normals at the same points will have <m>$\left(2a +
        a\left(t_1^2 + t_2^2 + t_1t_2\right), -a t_1t_2(t_1 + t_2)\right)$</m> as their point of intersection.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of the Chord of a Parabola whose middle point is <m>$(x_1, y_1)$</m></d:title>
      <d:figure xml:id="cs6">
        <d:title>Chord of a parabola</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_5.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Equation of the parabola is <m>$y^2 = 4ax$</m>. Let <m>$AB$</m> be a chord of the parabola whose
        middle point is <m>$P(x_1, y_1)$</m>.
      </d:para>
      <d:para>
        Equation of the chord is <m>$y - y_1 = m(x - x_1)$</m>, where <m>$m$</m> is the slope of
        <m>$AB$</m>.
      </d:para>
      <d:para>
        Let <m>$A = (x_2, y_2)$</m> and <m>$B = (x_3, y_3) \Rightarrow y_2^2 = 4a x_2$</m> and <m>$y_3^2 =
        4a x_3$</m>
      </d:para>
      <d:para>
        <m>$\therefore y_2^2- y_3^2 = 4a(x_2 - x_3) \Rightarrow \frac{y_2 - y_3}{x_2 - x_3} = \frac{4a}{y_2
        + y_3}$</m>
      </d:para>
      <d:para>
        But <m>$P$</m> is mid-point of <m>$AB$</m>, so we can write <m>$y_2 + y_3 = 2y_1$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \frac{y_2 - y_3}{x_2 - x_3} = \frac{2a}{y_1} = m$</m>
      </d:para>
      <d:para>
        Thus, equation of the chord is <m>$y - y_1 = \frac{2a}{y_1}(x - x_1) \Rightarrow y y_1 - 2a(x + x_1)
        = y_1^2 - 4a x_1$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Position of a Point w.r.t. a Parabola</d:title>
      <d:figure xml:id="cs7">
        <d:title>Position of a point w.r.t. a parabola</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_6.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let <m>$P(x_1, y_1)$</m> be a given point. Let <m>$PL \perp OX$</m> and let <m>$PL$</m> intersect
        the parabole at <m>$Q(x_2, y_2)$</m>. Since <m>$Q(x_2, y_2)$</m> lies on the parabola, therefore
        <m>$y_2^2 = 4ax_2$</m>.
      </d:para>
      <d:para>
        Point <m>$P$</m> will lie outside, on or inside the parabola <m>$&lt;=> P L >, =, \text{or} &lt;
        Q L$</m>
      </d:para>
      <d:para>
        <m>$|y_1| >, = \text{or} &lt; |y_2| => y_1^2 - 4a x_1 >, = , \text{or} &lt; 0$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Tangents from a Point outside a Parabola to the Parabola</d:title>
      <d:para>
        Let the parabola be <m>$y^2 = 4ax$</m> and <m>$P(\alpha, \beta)$</m> be a given point.
      </d:para>
      <d:para>
        The equation of a tangent to the parabola is <m>$y = m x + \frac{a}{m}$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow m^2\alpha - \beta m + a = 0. D > 0 \Rightarrow \beta^2 - 4a\alpha > 0$</m> i.e. if
        the point lies outside the parabola then <m>$m$</m> will be real and the equation will have two real
        roots, which gives us two tangents to the parabola from the point <m>$P$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of Pair of Tangents from a Point to a Parabola</d:title>
      <d:figure xml:id="cs8">
        <d:title>Pair of tangents from a point w.r.t. to a parabola</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_7.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let the parabola be <m>$y^2 = 4a x$</m>. Let <m>$P(x_1, y_1)$</m> be a point outside the
        parabola. Let a chord of the parabola through the point <m>$P$</m> cut the parabola at <m>$R$</m>
        and let <m>$Q(\alpha, \beta)$</m> be any arbitrary point on the line <m>$PR$</m>. Let <m>$R$</m>
        divide <m>$PQ$</m> in the ratio <m>$\lambda:1$</m>, then
      </d:para>
      <d:para>
        <m>$R = \left(\frac{\lambda\alpha + x_1}{\lambda + 1}, \frac{\lambda\beta + y_1}{\lambda +
        1}\right)$</m>
      </d:para>
      <d:para>
        Since <m>$R$</m> lies on the parabola, therefore <m>$\left(\frac{\lambda\beta + y_1}{\lambda + 1}\right)^2
        = 4a\frac{\lambda\alpha + x_1}{\lambda + 1}$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow (\beta^2 - 4a\alpha)\lambda^2 + 2[\beta y_1 - 2a(\alpha + x_1)]\lambda + \left(y_1^2
        - 4a x_1\right) = 0 \ldots$</m> (1)
      </d:para>
      <d:para>
        Line <m>$PQ$</m> will become a tangent to the parabola if the roots of the above equation (1) are
        equal
      </d:para>
      <d:para>
        <m>$\Rightarrow 4[\beta y_1 - 2a(\alpha + x_1)]^2 = 4\left(\beta^2 - 4a\alpha\right)\left(y_1^2 -
        4ax_1\right)$</m>
      </d:para>
      <d:para>
        Hence, locus of <m>$(\alpha, \beta)$</m> i.e. equation of pair of tangents from <m>$P$</m> is <m>$[y y_1
        - 2a(x + x_1)]^2 = \left(y^2 - 4a x\right)\left(y_1^2 - 4a x_1\right)$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow T^2 = SS_1$</m>, where <m>$T, S$</m> and <m>$S_1$</m> have usual meanings.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of Chord of Contact of point <m>$P(\alpha, \beta)$</m> w.r.t. a Parabola</d:title>
      <d:para>
        Let the parabola be <m>$y^2 = 4a x \ldots$</m> (1) Let <m>$P(\alpha, \beta)$</m> be a point outside
        the parabola. Let <m>$PA$</m> and <m>$PB$</m> be the two tangents from <m>$P$</m> to the parabola
        (1). Let <m>$PA$</m> and <m>$PB$</m> be the two tangents from <m>$P$</m> to the parabola (1).
      </d:para>
      <d:figure xml:id="cs9">
        <d:title>Chord of contact from a point w.r.t. to a parabola</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_8.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let <m>$A = (x_1, y_1)$</m> and <m>$B = (x_2, y_2)$</m>. Equation of the tangent <m>$PA$</m> is
        <m>$yy_1 = 2a(x + x_1)$</m> and that of <m>$PB$</m> is <m>$y y_2 = 2a(x + x_2)$</m>.
      </d:para>
      <d:para>
        Since both the lines pass through <m>$P$</m>, therefore <m>$\beta y_1 = 2a(\alpha + x_1)$</m> and
        <m>$\beta y_2 = 2a(\alpha + x_2)$</m>.
      </d:para>
      <d:para>
        Now we consider the equation <m>$y\beta = 2a(x + \alpha)$</m>. We see that this line passes through
        both <m>$A$</m> and <m>$B$</m>. Therefore, it is the equation of chord of contact <m>$AB$</m> of
        point <m>$P$</m> w.r.t. the parabola.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Pole and Polar of a Parabola</d:title>
      <d:figure xml:id="cs10">
        <d:title>Pole and polar of a parabola</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_9.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let <m>$P$</m> be a given point. Let a line through <m>$P$</m> intersect parabola at two points
        <m>$A$</m> and <m>$B$</m>. Let the tangents at <m>$A$</m> and <m>$B$</m> meet at <m>$Q$</m>. The
        locus of <m>$Q$</m> is a straight line called the <d:emphasis role="italic">polar</d:emphasis> of
        point <m>$P$</m>.
      </d:para>
      <d:para>
        If the line <m>$LM$</m> is the polar of a point <m>$P$</m> w.r.t. a parabola, then the point
        <m>$P$</m> is called the pole of the line <m>$LM$</m> w.r.t. the parabola.
      </d:para>
      <d:para>
        Let the parabola be <m>$y^2 = 4a x\ldots$</m> (1) Let <m>$P(x_1, y_1)$</m> be the given point. Let a
        line though <m>$P$</m> cut parabola in two points <m>$A$</m> and <m>$B$</m>.
      </d:para>
      <d:para>
        Let <m>$Q(\alpha, \beta)$</m> be the point of intersection of the tangents to the parabola at
        <m>$A$</m> and <m>$B$</m>.
      </d:para>
      <d:para>
        Clearly, <m>$AB$</m> is the chord of contact of point <m>$Q(\alpha, \beta)$</m> w.r.t. the
        parabola. Thereforem, equation of <m>$AB$</m> will be
      </d:para>
      <d:para>
        <m>$y\beta = 2a(x + \alpha)\ldots$</m> (2)
      </d:para>
      <d:para>
        Since this line passes through <m>$P(x_1, y_1)$</m>, therefore <m>$y_1\beta = 2a(x_1 + \alpha)$</m>.
      </d:para>
      <d:para>
        Hence, locus of <m>$Q(\alpha, \beta)$</m> i.e. equation of polar of point <m>$P$</m> w.r.t. the
        parabola is
      </d:para>
      <d:para>
        <m>\[ y y_1 = 2a(x + x_1) \tag{6.6}\]</m>
      </d:para>
      <d:para>
        According to the above definition of polar, <m>$AB$</m> will not be a part of the locus because no
        two tangents to the parabola can intersect on the line segment <m>$AB$</m>.
      </d:para>
      <d:para>
        So the correct definition is here. Let <m>$P(x_1, y_1)$</m> be any point. The line whose equation is
        that of the tangent to <m>$P$</m> to parabola whether <m>$P$</m> lies on the curve or not is called
        the polar of point <m>$P$</m> w.r.t. the parabola. According to this definition equation of polar of
        point <m>$P(x_1, y_1)$</m> w.r.t. the parabola is <m>$T = 0$</m> or <m>$y y_1 = 2a(x + x_1)$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Diameter of a Parabola</d:title>
      <d:para>
        Diameter of a conic is the locus of mid-points of a series of its parallel chords.
      </d:para>
      <d:figure xml:id="cs11">
        <d:title>Diameter a parabola</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_10.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let the parabola be <m>$y^2 = 4a x\ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$AB$</m> be one of the chords of a series of parallel chords having slope <m>$m$</m>. Let
        <m>$P(\alpha, \beta)$</m> be the mid-point of chord <m>$AB$</m>, then equation of <m>$AB$</m> will
        be <m>$T = S_1$</m>.
      </d:para>
      <d:para>
        <m>$\Rightarrow y\beta - 2a(x + \alpha) = \beta^2 - 4a\alpha \ldots$</m> (2)
      </d:para>
      <d:para>
        Slope of this line is <m>$frac{2a}{\beta}$</m>. But slope of the line <m>$AB$</m> is <m>$m$</m>,
        therefore <m>$\frac{2a}{\beta} = m \Rightarrow beta = \frac{2a}{m}$</m>.
      </d:para>
      <d:para>
        Hence, locus of <m>$P(\alpha, \beta)$</m> i.e. equation of diameter is <m>\[ y = \frac{2a}{m}
        \tag{6.7}\]</m>
      </d:para>
      <d:para>
        Clearly, this line is parallel to the axis of the parabola.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>The Optical Property of a Parabola</d:title>
      <d:para>
        The tangent at any point <m>$P$</m> on a parabola bisects the angle between the focal chord through
        <m>$P$</m> and the perpendicular from <m>$P$</m> on the directrix.
      </d:para>
      <d:para>
        Let the parabola be <m>$y^2 = 4a x \ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$P(at^2, 2at)$</m> be a point on the parabola and let <m>$PM \perp \text{ directrix}$</m>.
      </d:para>
      <d:figure xml:id="cs12">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_11.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Equation of the tangent to parabola at <m>$P$</m> is <m>$yt = x + at^2$</m>. Slope of this tangent
        <m>$PT$</m> is <m>$\frac{1}{t} \Rightarrow \tan\theta = \frac{1}{t}$</m>
      </d:para>
      <d:para>
        Slope of <m>$PS = \frac{2at}{a\left(t^2 - 1\right)} = \frac{\frac{2}{t}}{1 - \frac{1}{t^2}} =
        \frac{2\tan\theta}{1 - \tan^2\theta} = \tan2\theta$</m>.
      </d:para>
      <d:para>
        <m>$\Rightarrow \angle PSX = 2\theta \Rightarrow \angle TPS = \angle PSX - \angle PTS = 2\theta -
        \theta = \theta$</m>.
      </d:para>
      <d:para>
        Hence, <m>$\angle SPT = \angle MPT = \theta$</m>.
      </d:para>
      <d:para>
        Therefore <m>$PT$</m> bisects <m>$\angle MPS$</m>.
      </d:para>
      <d:para>
        The other property we know from physics is that rays coming from infinity are reflected by parabolas
        in such a manner that they pass through focus. We will prove that here.
      </d:para>
      <d:para>
        Let <m>$PN$</m> be normal to <m>$PT$</m>. In <m>$\triangle PTN, \angle TPN = 90^\circ$</m> and
        <m>$\angle PTN = \theta$</m>
      </d:para>
      <d:para>
        Since <m>$PR \parallel TN, \therefore \angle RPN = \angle TNP = 90^\circ - \theta$</m>
      </d:para>
      <d:para>
        Also, <m>$\angle SPN = 180^\circ - \angle PSN - \angle SNP = 180^\circ - 2\theta - \left(90^\circ -
        \theta\right) = 90^\circ - \theta$</m>
      </d:para>
      <d:figure xml:id="cs13">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_12.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        <m>$\therefore \angle SPN = \angle RPN$</m>.
      </d:para>
      <d:para>
        Thus, a ray parallel to the axis of parabola after reflection from parabola passes through the
        focus.
      </d:para>
    </d:sect2>
  </d:sect1>
  <d:sect1>
    <d:title>Ellipse</d:title>
    <d:para>
      An ellipse is the locus of the point, which moves in the plane so that the ratio of its distance from
      a fixed point, and a fixed line is a constant, which is less than one.
    </d:para>
    <d:figure xml:id="cs14">
      <d:title>Definition of an ellipse</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/6_13.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      The fixed point is called the <d:emphasis role="italic">focus</d:emphasis>, the constant ratio is
      called the <d:emphasis role="italic">eccentricity</d:emphasis>, and the fixed straight line is known
      as the directrix of the ellipse.
    </d:para>
    <d:para>
      In the figure above, <m>$S$</m> is the focus and <m>$MM'$</m> the directrix. Let <m>$P$</m>
      be any point on the ellipse, then <m>$\frac{PS}{PN} = e &lt; 1$</m>.
    </d:para>
    <d:para>
      Thus, we can find the equation of an ellipse when the coordinates of the focus, the equation of the
      directrix, and the eccentricity are known.
    </d:para>
    <d:sect2>
      <d:title>Equation of an Ellipse</d:title>
      <d:para>
        Let <m>$S$</m> be the focus and <m>$M M'$</m> be the directrix of the ellipse. Draw <m>$SH \perp
        MM'$</m>, wed divide <m>$SH$</m> internally and externally in the ratio <m>$e: 1(e &lt; 1)$</m> at
        points <m>$A$</m> and <m>$A'$</m> respectively, then
      </d:para>
      <d:figure xml:id="cs15">
        <d:title>Equation of an ellipse</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_14.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        <m>$\frac{AS}{AH} = e \ldots$</m> (1) and <m>$\frac{A'S}{A'H} = e\ldots$</m> (2)
      </d:para>
      <d:para>
        Clearly, <m>$A$</m> and <m>$A'$</m> will lie on the ellipse. Let <m>$AA' = 2a$</m>. Let <m>$O$</m>
        be the mid-point of <m>$AA'$</m>. We take <m>$O$</m> as the origin and <m>$OA'$</m> and <m>$OY$</m>
        as the <m>$x$</m> and <m>$y$</m> axes respectively.
      </d:para>
      <d:para>
        From (1), <m>$AS = e AH$</m> and from (2) <m>$A'S = e A'H$</m>.
      </d:para>
      <d:para>
        <m>$AS + A'S = e(AH + A'H) \Rightarrow 2a = (AH + AH + AA') = e(2Ah + 2a) \therefore A H =
        \frac{a}{e} - e$</m>.
      </d:para>
      <d:para>
        From (1) <m>$AS = e AH = a - ae \therefore O S= OA - AS = ae \therefore S = (-ae, 0)$</m>.
      </d:para>
      <d:para>
        and <m>$OH = OA + AH = \frac{a}{e}$</m>.
      </d:para>
      <d:para>
        Equation of <m>$MM'$</m> is <m>$x = -\frac{a}{e} \Rightarrow e x + a = 0$</m>.
      </d:para>
      <d:para>
        Let <m>$P(\alpha, \beta)$</m> be a point on ellipse. Draw <m>$PN \perp MM'$</m>
      </d:para>
      <d:para>
        Now <m>$\frac{PS}{PN} = e \Rightarrow PS^2 = e^2PN^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow (\alpha + ae)^2 + beta^2 = e^2\left(\frac{|e alpha + a|}{e}\right)^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \frac{\alpha^2}{a^2} + \frac{\beta^2}{a^2\left(1 - e^2\right)} = 1$</m>.
      </d:para>
      <d:para>
        Let <m>$b^2 = a^2\left(1 - e^2\right)$</m>
      </d:para>
      <d:para>
        Then the locus of <m>$P$</m> is the equation of the ellipse, which is
      </d:para>
      <d:para>
        <m>\[ \frac{x^2}{a^2} + \frac{y^2}{b^2} = 1 \tag{6.8}\]</m>
      </d:para>
      <d:para>
        It is clear that if <m>$(ae, 0)$</m> is the focus, and <m>$ex - a = 0$</m> is the directrix even
        then the ellipse will have the same equation.
      </d:para>
      <d:para>
        Thus, the ellipse <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m> has two foci and two directrices.
      </d:para>
      <d:para>
        A circle is a special limiting case of an ellipse whose major axis is <m>$2a$</m>, both foci
        approach the center and eccentricity approaches <m>$0$</m>.
      </d:para>
      <d:para>
        A typical ellipse is given below:
      </d:para>
      <d:figure xml:id="cs16">
        <d:title>An ellipse</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_15.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        As you can see an ellipse cuts <m>$x$</m>-axis at <m>$(-a, 0)$</m> and <m>$(a, 0)$</m>, cuts
        <m>$y$</m>-axis at <m>$(-b, 0)$</m> and <m>$(b, 0)$</m>. Equation of ellipse does not change when
        <m>$x$</m> or <m>$y$</m> are changed to <m>$-x$</m> and <m>$-y$</m>, hence, an ellipse is
        symmetrical about <m>$x$</m> and <m>$y$</m> axes.
      </d:para>
      <d:para>
        We have <m>$y = \pm \frac{b}{a} \sqrt{a^2 - x^2} \Rightarrow a^2 - x^2 \geq 0 \Rightarrow -a \leq x
        \leq a$</m>.
      </d:para>
      <d:para>
        Similarly, <m>$x = \pm \frac{a}{b} \sqrt{b^2 - y^2} \Rightarrow b^2 - x^2 \geq 0 \Rightarrow -b \leq
        y \leq b$</m>.
      </d:para>
      <d:para>
        We see that ellipse is bounded between <m>$-a \leq x \leq a$</m> and <m>$-b \leq y \leq b$</m> so an
        eclipse is a closed curve.
      </d:para>
      <d:para>
        <m>$\frac{dy}{dx} = -\frac{b^2x}{b^2y}$</m>. At <m>$(-a, 0)$</m> and <m>$(a, 0)$</m>, the slope is
        not defined, and hence, tangent at <m>$(-a, 0)$</m> and <m>$(a, 0)$</m> will be perpendicular to
        <m>$x$</m>-axis.
      </d:para>
      <d:para>
        At <m>$(0, -b)$</m> and <m>$(0, b), \frac{dy}{dx} = 0$</m>, therefore, at these points tangents are
        parallel to <m>$x$</m>-axis.
      </d:para>
      <d:para>
        If <m>$(x_1, y_1)$</m> lies on the curve, so does <m>$(-x_1, -y_1)$</m>. But <m>$(x_1, y_1)$</m> and
        <m>$(-x_1, -y_1)$</m> are points on the line through the origin and are equidistant from the
        origin. Hence, every chord through the origin <m>$C$</m> is bisected at <m>$C$</m>. The point
        <m>$C$</m> is, therefore, callled the center of the ellipse.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Second focus and second directrix of an ellipse</d:title>
      <d:figure xml:id="cs17">
        <d:title>An ellipse</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_16.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let <m>$P(x, y)$</m> be a point on the ellipse and let <m>$C$</m> be its center. Let the equation of
        the ellipse be <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>, where <m>$b^2 = a^2\left(1 -
        e^2\right)\ldots$ (1)</m>
      </d:para>
      <d:para>
        Here <m>$CA = CA' = a$</m> and <m>$e$</m> is the eccentricity of the ellipse and the point
        <m>$S$</m> and the line <m>$QN$</m> are the focus and directrix respectively.
      </d:para>
      <d:para>
        Let us take <m>$S'$</m> and <m>$Q'$</m> on the <m>$x$</m>-axis on that side of <m>$C$</m> which is
        opposite to the side of <m>$S$</m> such that <m>$CS' = ae$</m> and <m>$CQ' = \frac{a}{e}\ldots$</m> (2)
      </d:para>
      <d:para>
        Let <m>$Q;N' \perp CQ', PN' \perp Q'N'$</m> and <m>$PM \perp CX$</m>.
      </d:para>
      <d:para>
        Since <m>$P = (x, y)\therefore CM = x, PM = y$</m>. Join <m>$P$</m> and <m>$S$</m>. Clearly, <m>$PN'
        = MQ'$</m>
      </d:para>
      <d:para>
        Equation (1) may be rewritten as <m>$x^2\left(1 - e^2\right) + y^2 = a^2\left(1 - e^2\right)$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow x^2 + y^2 + a^2e^2 = a^2 + e^2x^2\Rightarrow (ae + x)^2 + y^2 =
        e^2\left(\frac{a}{e} + x\right)^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \left(CS' + CM\right)^2 + PM^2 = e^2\left(CQ' + CM\right)^2\Rightarrow S'M^2 + PM^2
        = e^2Q'M^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow S'P^2 = e^2N'P^2 \Rightarrow \frac{PS'}{PN'} = e$</m>.
      </d:para>
      <d:para>
        Hence, the distance of <m>$P$</m> from <m>$S' = e\times$</m> (the distance of <m>$P$</m> from
        <m>$Q'N'$</m>). Hence, <m>$S'$</m> is a focus and <m>$Q'N'$</m> is a directrix.
      </d:para>
      <d:para>
        Thus, it is clear that the ellipse has a second focus and a second directrix.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Some Terms</d:title>
      <d:figure xml:id="cs18">
        <d:title>An ellipse</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_17.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:orderedlist>
        <d:listitem>
          Center: In the figure <m>$C$</m> is the center. All chords
          passing through <m>$C$</m> is bisected at <m>$C$</m>.
        </d:listitem>
        <d:listitem>
          Foci: <m>$S$</m> and <m>$S'$</m> are the two foci of the ellipse and their coordinates are
          <m>$(ae, 0)$</m> annd <m>$(-ae, 0)$</m> respectively.
        </d:listitem>
        <d:listitem>
          Directrices: <m>$QN$</m> and <m>$Q'N'$</m> are the two directrices of the ellipse and their
          equations are <m>$x = \frac{a}{e}$</m> and <m>$x = -\frac{a}{e}$</m> respectively.
        </d:listitem>
        <d:listitem>
          Axes: The lines <m>$AA'$</m> and <m>$BB'$</m> are called the major and minor axes respectively of
          the ellipse.
          <d:para>
            <m>$CA = CA' = a$</m> and <m>$CB = CB' = b$</m>
          </d:para>
          <d:para>
            <m>$\because 0 &lt; e &lt; 1 \therefore 0 &lt; 1 - e^2 &lt; 1\therefore a^2(1 - e^2) &lt; a^2$</m>
          </d:para>
          <d:para>
            Thus, <m>$b^2 &lt; a^2$</m> i.e. <m>$b &lt; a$</m>.
          </d:para>
        </d:listitem>
        <d:listitem>
          Ordinate and double ordinates: Let <m>$P$</m> be a point on the ellipse. From <m>$P$</m> we draw
          <m>$PM \perp AA'$</m>. Produce <m>$PM$</m> to meet the ellipse at <m>$P'$</m>, then <m>$PM$</m> is
          called an ordinate and <m>$PMP'$</m> is called the double ordinate of point <m>$P$</m>.
        </d:listitem>
        <d:listitem>
          Latus rectum: <m>$LL'$</m> and <m>$MM'$</m> are the latus rectrum of the ellipse. These lines are
          perpendicular to the major axis <m>$AA'$</m> through the foci <m>$S$</m> and <m>$S'$</m>
        </d:listitem>
        <d:listitem>
          Length of latus rectum: Let <m>$LL' = 2k$</m>, then <m>$LS = k$</m> and <m>$L = (ae,
          k)$</m>. Since <m>$L(ae, k)$</m> lies on the ellipse
          <d:para>
            <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1 \therefore \frac{a^2e^2}{a^2} + \frac{k^2}{b^2} =
            1\Rightarrow \frac{k^2}{b^2} = 1 - e^2$</m>
          </d:para>
          <d:para>
            <m>$\Rightarrow k^2 = b^2\left(1 - e^2\right) = \frac{b^4}{a^2}\Rightarrow k =
            \frac{b^2}{a}$</m>.
          </d:para>
          <d:para>
            Hence, lenth of latus rectum <m>$LSL'$</m> is <m>$\frac{2b^2}{a}$</m>.
          </d:para>
        </d:listitem>
        <d:listitem>
          Vertices: The points where major axis of the ellipse meets it are called its vertices. In the
          given figure <m>$A$</m> and <m>$A'$</m> are the vertices.
        </d:listitem>
        <d:listitem>
          Diameter: A chord of the ellipse passing through the center is called the diameter of the
          ellipse. All such chords are bisected by the center.
        </d:listitem>
        <d:listitem>
          Focal chord: A chord of the ellipse passing through its focus is called a focal chord.
        </d:listitem>
      </d:orderedlist>
    </d:sect2>
    <d:sect2>
      <d:title>Position of a Point w.r.t. an Ellipse</d:title>
      <d:figure xml:id="cs19">
        <d:title>Position of a Point w.r.t. an Ellipse</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_18.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let <m>$D(\alpha, \beta)$</m> be any point. Let <m>$DM$</m> be perpendicular to <m>$AA'$</m>.
      </d:para>
      <d:para>
        <d:emphasis role="bold">Case I:</d:emphasis> When <m>$-a &lt; \alpha &lt; 1$</m> i.e. <m>$DM$</m>
        cuts the ellipse in real distinct points.
      </d:para>
      <d:para>
        Let the perpendicular <m>$DM$</m> cut the ellipse at <m>$R$</m> and the major axis at <m>$M$</m>.
      </d:para>
      <d:para>
        <m>$\because D = (\alpha, \beta)\therefore CM = \alpha, DM = \beta$</m>
      </d:para>
      <d:para>
        Let <m>$RM = y_1$</m> then <m>$R = (\alpha, y_1)$</m>.
      </d:para>
      <d:para>
        Since <m>$R$</m> lies on the ellipse <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>,
      </d:para>
      <d:para>
        <m>$\therefore \frac{\alpha^2}{a^2} + \frac{y_1^2}{b^2} = 1 \therefore \frac{y_1^2}{b^2} = 1 -
        \frac{\alpha^2}{a^2} \ldots$</m> (2)
      </d:para>
      <d:para>
        Now <m>$D$</m> lies outside, on or inside the ellipse according as <m>$MD >, =, &lt; MR$</m> or
        <m>$MD^2 >, =, &lt; MR^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \beta^2 >, =, &lt; y_1^2 \Rightarrow \frac{\beta^2}{b^2} >, =, &lt; \frac{y_1^2}{a^2}$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \frac{\beta^2}{b^2} >, = &lt; 1 - \frac{\alpha^2}{a^2}\Rightarrow
        \frac{\alpha^2}{a^2} + \frac{\beta^2}{b^2} - 1 >, =, &lt; 0$</m>.
      </d:para>
      <d:para>
        <d:emphasis role="bold">Case II:</d:emphasis> When <m>$\alpha\geq a$</m> or <m>$\alpha\leq -a$</m>.
      </d:para>
      <d:para>
        In this case <m>$\frac{\alpha^2}{a^2} + \frac{\beta^2}{b^2} - 1\geq 1 + \frac{\beta^2}{b^2} - 1 =
        \frac{\beta^2}{b^2} > 0$</m> where <m>$\beta \neq 0$</m>
      </d:para>
      <d:para>
        Hence in all cases <m>$(\alpha, \beta)$</m> is outside the ellipse if <m>$\frac{\alpha^2}{a^2} +
        \frac{\beta^2}{b^2} > 0$</m>; on the ellipse if <m>$\frac{\alpha^2}{a^2} +
        \frac{\beta^2}{b^2} = 0$</m>; and inside the ellipse if <m>$\frac{\alpha^2}{a^2} +
        \frac{\beta^2}{b^2} &lt; 0$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Parametric Equation of an Ellipse</d:title>
      <d:para>
        Clearly <m>$x = a\cos\theta$</m> and <m>$y = b\sin\theta$</m> satisfy the equation
        <m>$\frac{x^2}{a^2} + \frac{y^2}{b^1} = 1$</m> for all real values of <m>$\theta$</m>.
      </d:para>
      <d:para>
        Hence, these values, where <m>$\theta$</m> is the parameter is the parametric equation of the
        ellipse. Hence, coordinates of any point on the ellipse may be taken as <m>$(a\cos\theta,
        b\sin\theta)$</m>. This point is also called point '<m>$\theta$</m>'. This angle <m>$\theta$</m> is
        called the eccentric angle of the point <m>$(a\cos\theta, b\sin\theta)$</m> on the ellipse.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Auxilliary Circle and Geometrical Meaning of the Eccentric Angle of a Point</d:title>
      <d:para>
        Auxilliary Circle: The circle described on the major axis of an ellipse as diameter is called the
        auxilliary circle of the ellipse.
      </d:para>
      <d:figure xml:id="cs20">
        <d:title>An ellipse with auxilliary circle</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_19.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let the equation of the ellipse be <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>, then the
        equation of the auxilliary circle is <m>$x^2 + y^2 = a^2$</m>. Let <m>$P(a\cos\theta,
        b\sin\theta)$</m> be a point on the ellipse. Draw <m>$PM\perp AA'$</m>. Let <m>$PM$</m> cut the
        auxilliary cirle at <m>$Q$</m>.
      </d:para>
      <d:para>
        Let <m>$\angle QCA = \alpha$</m> then <m>$Q = (a\cos\alpha, a\sin\alpha)$</m>.
      </d:para>
      <d:para>
        But <m>$x$</m>-coordinate of <m>$Q = x$</m>-coordinate of <m>$P$</m>.
      </d:para>
      <d:para>
        <m>$\therefore a\cos\alpha = a\cos\theta \Rightarrow \alpha = \theta$</m>.
      </d:para>
      <d:para>
        Clearly, <m>$QM:PM = a\sin\alpha : b\sin\theta = a\sin\theta:b\sin\theta = a:b$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>An Important Property</d:title>
      <d:figure xml:id="cs21">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_20.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        <d:emphasis role="italic">The sum of the focal distances of any point on an ellipse is a constant
        and is equal to the length of the major axis of the ellipse.</d:emphasis>
      </d:para>
      <d:para>
        Let <m>$P(x, y)$</m> be any point on the ellipse <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>.
      </d:para>
      <d:para>
        Then by definition <m>$SP = ePN, S'P = ePN'\Rightarrow SP + S'P = e\left(PN + PN'\right) = eNN' =
        eQQ' = e\left(CQ + CQ'\right)$</m>
      </d:para>
      <d:para>
        But <m>$CQ = \frac{a}{e}$</m> and <m>$CQ' = \frac{a}{e} \therefore SP + S'P = 2a = AA'$</m>, where
        <m>$AA'$</m> is the length of the major axis of the ellipse.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Tangent of an Ellipse</d:title>
      <d:para>
        Let the equation of ellipse by <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1\ldots$</m> (1). Also, let <m>$P(x_1,
        y_1)$</m> and <m>$Q(x_2, y_2)$</m> be two points on the ellipse.
      </d:para>
      <d:para>
        Equation of the chord <m>$PQ$</m> is <m>$y - y_1 = \frac{y_2 - y_1}{x_2 - x_1}(x - x_1)\ldots$</m> (2)
      </d:para>
      <d:para>
        Since <m>$P$</m> and <m>$Q$</m> are on the ellipse, therefore, <m>$\frac{x_1^2}{a^2} +
        \frac{y_1^2}{b^2} = 1$</m> and <m>$\frac{x_2^2}{a^2} + \frac{y_2^2}{b^2} = 1$</m>
      </d:para>
      <d:para>
        Subtracting <m>$\frac{y_2 - y_1}{x_2 - x_1} = -\frac{b^2}{a^2}\left(\frac{x_2 + x_1}{y_2 +
        y_1}\right)$</m>
      </d:para>
      <d:para>
        Putting this in equation (2) gives us <m>$y - y_1 = -\frac{b^2}{a^2}\left(\frac{x_2 + x_1}{y_2 +
        y_1}\right)(x - x_1)$</m>
      </d:para>
      <d:para>
        Now, when <m>$Q\rightarrow P, x_2 \rightarrow x_1, y_2\rightarrow y_1$</m>, so the above equation becomes
      </d:para>
      <d:para>
        <m>$y - y_1 = -\frac{b^2}{a^2}.\frac{x_1}{y_1}(x - x_1)$</m>
      </d:para>
      <d:para>
        Simplifying gives the equation of tangent as <m>\[\frac{xx_1}{a^2} + \frac{yy_1}{b^2} = 1 \tag{6.9}\]</m>
      </d:para>
      <d:para>
        which is the required equation of the tangent to an ellipse at <m>$P(x_1, y_1)$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Normal of an Ellipse</d:title>
      <d:para>
        Let <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m> be an ellipse and <m>$P(x_1, y_1)$</m> be a point
        on it.
      </d:para>
      <d:para>
        We have derived the equation of tangent at <m>$P$</m> as <m>$\frac{xx_1}{a^2} + \frac{yy_1}{b^2} =
        1$</m>.
      </d:para>
      <d:para>
        The slope of normal at this point is <m>$\frac{a^2}{b^2}\times\frac{y_1}{x_1}$</m>.
      </d:para>
      <d:para>
        Thus, equation of normal is <m>$y - y_1 = \frac{a^2y_1}{b^2x_1(x - x_1)}$</m> i.e.
      </d:para>
      <d:para>
        <m>\[ \frac{x- x_1}{x_1/a^2} = \frac{y - y_1}{y_1/b^2} \tag{6.10}\]</m>
      </d:para>
      <d:para>
        Putting <m>$P(a\cos\theta, b\sin\theta)$</m> the equation becomes <m>\[ax\sec\theta - by\csc\theta =
        a^2 - b^2 \tag{6.11}\]</m>
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Intersection of a Line and an Ellipse</d:title>
      <d:para>
        Let the equation of the line be <m>$y = mx + c$</m> and that of an ellipse be <m>$\frac{x^2}{a^2} +
        \frac{y^2}{b^2} = 1$</m>.
      </d:para>
      <d:para>
        The points of intersection of the line and the ellipse can be obtained by solving the two equations
        simultaneously.
      </d:para>
      <d:para>
        The abscissae of the point of intersection is given by <m>$\frac{x^2}{a^2} + \frac{(mx + c)^2}{b^2}
        = 1$</m>
      </d:para>
      <d:para>
        i.e. <m>$\left(b^2 + a^2m^2\right)x^2 + 2a^2cmx + a^2\left(c^2 - b^2\right) = 0\ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$x_1, x_2$</m> be the roots of the quadratic equation (1). The line meets the ellipse in real
        and distinct points if the roots are real and different. The line is a tangent to the ellipse if
        <m>$x_1 = x_2$</m> and the line does not intersect with the ellipse if the roots are complex
        numbers.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of a Tangent in Terms of Its Slope</d:title>
      <d:para>
        Let the equation of the line be <m>$y = mx + c$</m> and that of an ellipse be <m>$\frac{x^2}{a^2} +
        \frac{y^2}{b^2} = 1$</m>.
      </d:para>
      <d:para>
        We have seen that roots of (1) should be equal for tangency. Thus, discriminant must be zero i.e.
      </d:para>
      <d:para>
        <m>$4a^4c^2m^2 - 4\left(b^2 + a^2m^2\right)a^2\left(c^2 - b^2\right) = 0$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow 4a^2\left[a^2c^2m^2 - b^2c^2 - a^2m^2c^2 + b^4 + a^2b^2m^2\right] = 0$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow b^2\left(-c^2 + b^2 + a^2m^2\right) = 0\Rightarrow c^2 = b^2 + a^2m^2 \Rightarrow c
        = \pm \sqrt{a^2m^2 + b^2}$</m>
      </d:para>
      <d:para>
        which is the required condition for tangency. Thus, the equation of the line is <m>$y = mx
        \pm\sqrt{a^2m^2 + b^2}$</m>, which are tangents to the ellipse for all values of <m>$m$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of the Chord of an Ellipse Whose Eccentric Angles are <m>$\theta$</m> and
      <m>$\phi$</m></d:title>
      <d:para>
        Let the ellipse be <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1\ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$P(a\cos\theta, b\sin\theta)$</m> and <m>$Q(a\cos\phi, b\sin\phi)$</m> be any two points on
        ellipse (1).
      </d:para>
      <d:para>
        Equation of chord <m>$PQ$</m> is <m>$y - b\sin\theta = \frac{b(\sin\theta - \sin\phi)}{a(\cos\theta
        - \cos\phi)}(x - a\cos\theta)$</m>
      </d:para>
      <d:para>
        <m>$= \frac{b}{a}.\frac{2\cos\frac{\theta + \phi}{2}\sin\frac{\theta - \phi}{2}}{2\sin\frac{\theta +
        \phi}{2}sin\frac{\phi - \theta}{2}}(x - a\cos\theta)$</m>
      </d:para>
      <d:para>
        <m>$\left(\frac{y - b\sin\theta}{b}\right)\sin\frac{\theta + \phi}{2} = -\left(\frac{x -
        a\cos\theta}{a}\right)\cos\frac{\theta + \phi}{2}$</m>
      </d:para>
      <d:para>
        Simplifying <m>$\frac{x}{a}\cos\frac{\theta + \phi}{2} + \frac{y}{b}\sin\frac{\theta + \phi}{2} =
        \cos\frac{\theta - \phi}{2}$</m>
      </d:para>
      <d:para>
        <d:emphasis role="bold">Note:</d:emphasis> When <m>$\phi\rightarrow \theta$</m>, chord <m>$PQ$</m>
        becomes tangent at <m>$P$</m>. Thus, equation of tangent at <m>$P$</m> is <m>$\frac{x}{a}\cos\theta
        + \frac{y}{b}\sin\theta = 1$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of the Chord of an Ellipse, Whose Middle Point is Given</d:title>
      <d:figure xml:id="cs22">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_21.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let the ellipse be <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1 \ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$P(x_1, y_1)$</m> be the the mid-point of a chord <m>$AB$</m> of the ellipse (1). Let
        <m>$m$</m> be the slope of chord <m>$AB$</m>. Then <m>$m = \frac{y_2 - y_3}{x_2 - x_3}\ldots$</m> (2)
      </d:para>
      <d:para>
        Since <m>$A$</m> and <m>$B$</m> are on the ellipse, therefore <m>$\frac{x_2^2}{a^2} +
        \frac{y_2^2}{b^2} = 1\ldots$</m> (3) and <m>$\frac{x_3^2}{a^2} + \frac{y_3^2}{b^2} = 1\ldots$</m> (4)
      </d:para>
      <d:para>
        Subtracting (3) and <m>$(4)$</m> gives us <m>$\frac{y_2 - y_3}{x_2 - x_3} =
        -\frac{b^2}{a^2}\frac{x_1}{y_1}$</m>
      </d:para>
      <d:para>
        Now equation of the chord <m>$AB$</m> is <m>$y - y_1 = -\frac{b^2}{a^2}\frac{x_1}{y_1}(x - x_1)$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \frac{xx_1}{a^2} + \frac{yy_1}{b^2} - 1 = \frac{x_1^2}{a^2} + \frac{y_1^2}{b^2} -
        1\Rightarrow T = S_1$</m>, where <m>$T$</m> and <m>$S_1$</m> have usual meanings.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Equation of Pair of Tangents to an Ellipse from an External Point</d:title>
      <d:figure xml:id="cs23">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_22.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let the given ellipse be <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1\ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$P(x_1, y_1)$</m> be an external point.
      </d:para>
      <d:para>
        Let <m>$Q(\alpha, \beta)$</m> be any point on the chord of the ellipse passing through <m>$P(x_1,
        y_1)$</m>. Coordinates of any point <m>$R$</m> on the chord dividing the line segment <m>$PQ$</m>,
        joining <m>$P(x_1, y_1)$</m> and <m>$Q(\alpha, \beta)$</m> in the ratio <m>$\lambda:1$</m> will be
        <m>$\left(\frac{\lambda\alpha + x_1}{\lambda + 1}, \frac{\lambda\beta + y_1}{\lambda +
        1}\right)$</m>.
      </d:para>
      <d:para>
        If this point <m>$R$</m> lies on the ellipse (1), then
      </d:para>
      <d:para>
        <m>$\frac{1}{a^2}\left(\frac{\lambda\alpha + x_1}{\lambda + 1}\right)^2 +
        \frac{1}{b^2}\left(\frac{\lambda\beta + y_1}{\lambda + 1}\right) = 1$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \frac{(\lambda\alpha + x_1)^2}{a^2} + \frac{(\lambda\beta + y_1)^2}{b^2} = (\lambda
        + 1)^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \left(\frac{\alpha^2}{a^2} + \frac{\beta^2}{b^2} - 1\right)\lambda^2 +
        2\left(\frac{x_1\alpha}{a^2} + \frac{y_1\beta}{b^2} - 1\right)\lambda + \left(\frac{x_1^2}{a^2} +
        \frac{y_1^2}{b^2} - 1\right) = 0\ldots$</m> (2)
      </d:para>
      <d:para>
        (2) is a quadratic equation in <m>$\lambda$</m>. If the chord through <m>$P(x_1, y_1)$</m> cuts the
        ellipse in two coincident points i.e. if the two values of <m>$\lambda$</m> are real and equal then
        the chord through <m>$P(x_1, y_1)$</m> will become tangent to the ellipse from <m>$P(x_1, y_1)$</m>.
      </d:para>
      <d:para>
        Thus, for the line <m>$PQ$</m> to become tangent to the ellipse (1)
      </d:para>
      <d:para>
        <m>$4\left(\frac{\alpha x_1}{a^2} + \frac{\beta y_1}{b^2} - 1\right)^2 = 4\left(\frac{\alpha^2}{a^2}
        + \frac{\beta^2}{b^2} - 1\right)\left(\frac{x_1^2}{a^2} + \frac{y_1^2}{b^2} - 1\right)$</m>
      </d:para>
      <d:para>
        Hence, locus of <m>$Q(\alpha, \beta)$</m> i.e. equation of pair of tangents from <m>$P(x_1,
        y_1)$</m> is
      </d:para>
      <d:para>
        <m>$\left(\frac{xx_1}{a^2} + \frac{yy_1}{b^2} - 1\right)^2 = \left(\frac{x^2}{a^2}
        + \frac{y^2}{b^2} - 1\right)\left(\frac{x_1^2}{a^2} + \frac{y_1^2}{b^2} - 1\right)$</m>
      </d:para>
      <d:para>
        This can be written as <m>$SS_1 = T^2$</m>, where <m>$S, S_1$</m> and <m>$T$</m> have usual meanings.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Pole and Polar of an Ellipse</d:title>
      <d:figure xml:id="cs24">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_23.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let <m>$A$</m> and <m>$B$</m> be any two points on the ellipse and a point <m>$P$</m> is given on
        this line outside the ellipse. Let the tangents at <m>$A$</m> and <m>$B$</m> meet at <m>$Q$</m>.
      </d:para>
      <d:para>
        Locus of <m>$Q$</m> is called the <d:emphasis role="italic">polar</d:emphasis> of point <m>$P$</m>
        w.r.t. the ellipse.
      </d:para>
      <d:para>
        We will see that polar of a  point w.r.t. an ellipse is a straight line.
      </d:para>
      <d:para>
        If line <m>$LM$</m> is the polar of the point <m>$P$</m> w.r.t. an ellipse, then point <m>$P$</m> is
        called the <d:emphasis role="italic">pole</d:emphasis> of the line <m>$LM$</m> w.r.t. the ellipse.
      </d:para>
      <d:sect3>
        <d:title>Equation of Polar of a Point</d:title>
        <d:para>
          Let the ellipse be <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1\ldots$</m> (1)
        </d:para>
        <d:para>
          Let <m>$P(x_1, y_1)$</m> be a given point. Let a line through <m>$P$</m> cut the ellipse (1) at two
          points <m>$A$</m> and <m>$B$</m>. Let <m>$Q(\alpha, \beta)$</m> be the point of intersection of the
          tangents to the ellipse at <m>$A$</m> and <m>$B$</m>.
        </d:para>
        <d:para>
          Clearly, <m>$AB$</m> is the chord of contact of point <m>$Q(\alpha, \beta)$</m> w.r.t. the ellipse
          (1). Therefore, equation of <m>$AB$</m> will be <m>$\frac{x\alpha}{a^2} + \frac{y\beta}{b^2} =
          1\ldots$</m> (2)
        </d:para>
        <d:para>
          Since line (2) passes through <m>$P(x_1, y_1)$</m>, therefore, <m>$\frac{x_1\alpha}{a^2} +
          \frac{y_1\beta}{b^2} = 1$</m>
        </d:para>
        Hence, locus of <m>$Q(\alpha, \beta)$</m> i.e. equation of the polar of point <m>$P(x_1, y_1)$</m> is
        <m>\[\frac{xx_1}{a^2} + \frac{yy_1}{b^2} = 1\tag{6.12} \]</m>
      </d:sect3>
      <d:sect3>
        <d:title>
          Properties of Pole and Polar
        </d:title>
        <d:orderedlist>
          <d:listitem>
            If the polar of <m>$P$</m> w.r.t. an ellipse passes through <m>$Q$</m>, then polar of <m>$Q$</m>
            w.r.t. the same ellipse will pass through <m>$P$</m>
          </d:listitem>
          <d:listitem>
            The point of intersection o faany two lines is the pole of the line joining the poles of the two
            lines.
          </d:listitem>
        </d:orderedlist>
      </d:sect3>
    </d:sect2>
    <d:sect2>
      <d:title>Diameter of an Ellipse</d:title>
      <d:figure xml:id="cs25">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_24.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        The locus of the middle points of a system of parallel chords of an ellipse is called a diameter of
        the ellipse.
      </d:para>
      <d:para>
        Let the ellipse be <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1\ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$AB$</m> be one chord of a system of parallel chords of slope <m>$m$</m>.
      </d:para>
      <d:para>
        Let <m>$P(\alpha,\beta)$</m> be the middle point of chord <m>$AB$</m>, then equation of <m>$AB$</m> is
      </d:para>
      <d:para>
        <m>$T = S_1\Rightarrow \frac{x\alpha}{a^2} + \frac{y\beta}{b^2} - 1 = \frac{\alpha^2}{a^2} +
        \frac{\beta^2}{b^2} - 1$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \frac{x\alpha}{a^2} + \frac{y\beta}{b^2} = \frac{\alpha^2}{a^2} +
        \frac{\beta^2}{b^2}\ldots$</m> (2)
      </d:para>
      <d:para>
        Slope of line (2), <m>$m = -\frac{b^2\alpha}{a^2\beta}$</m>
      </d:para>
      <d:para>
        Thus, locus of <m>$P$</m> i.e. equation the diameter is <m>\[ y = -\frac{b^2}{a^2}x\tag{6.13} \]</m>
      </d:para>
      <d:para>
        <d:emphasis role="bold">Note:</d:emphasis> Diameter of an ellipse passes through its center. Thus, a
        diameter of an ellipse is its chord passing through the center.
      </d:para>
      <d:para>
        <d:emphasis role="bold">Conjugate Diameters:</d:emphasis> Two diameters of an ellipse are said to be
        conjugate diameters if each bisects the chords parallel to the other.
      </d:para>
      <d:para>
        Clearly, if slope of a pair of conjugate diameters be <m>$m_1$</m> and <m>$m_2$</m>, then <m>$m_1m_2
        = -\frac{b^2}{a^2}$</m>, provided <m>$m_1$</m> and <m>$m_2$</m> are defined.
      </d:para>
      <d:para>
        Major and minor axes of an ellipse are also a pair of conjugate diameters.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Eccentric Angles of the Ends of a Pair of Conjugate Diameters Differ by a Right
      Angle</d:title>
      <d:para>
        Let <m>$POP'$</m> and <m>$QOQ'$</m> be a pair of conjugate diameters.
      </d:para>
      <d:figure xml:id="cs26">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_25.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let the eccentric angles of <m>$P$</m> and <m>$Q$</m> be <m>$\theta$</m> and <m>$\phi$</m>, then
        <m>$P = (a\cos\theta, b\sin\theta)$</m> and <m>$Q = (a\cos\phi, b\sin\phi)$</m>.
      </d:para>
      <d:para>
        Slope of <m>$POP' = \frac{b}{a}\tan\theta$</m> and slope of <m>$QOQ' = \frac{b}{a}\tan\phi$</m>.
      </d:para>
      <d:para>
        Since <m>$POP'$</m> and <m>$QOQ'$</m> are conjugate diameters, therefore,
        <m>$-\frac{b^2}{a^2}\tan\theta\tan\phi = -\frac{b^2}{a^2}\Rightarrow \tan\theta\tan\phi = -1$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \cos\theta\cos\phi + \sin\theta\sin\phi = \cos(\theta - \phi) = 0 \Rightarrow \theta
        - \phi = \pm\frac{\pi}{2}$</m>.
      </d:para>
      <d:para>
        Thus, if eccentric angle of <m>$P$</m> is <m>$\theta$</m>, then eccentric angles of <m>$Q, P',
        Q'$</m> will be <m>$\theta + \frac{\pi}{2}, \theta + \pi$</m> and <m>$\theta + \frac{3\pi}{2}$</m>
        respectively.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Director Circle of an Ellipse</d:title>
      <d:para>
        Locus of the point of intersection of mutually perpendicular tangents to an ellipse is a circle
        called <d:emphasis role="italic">director circle</d:emphasis> of the ellipse.
      </d:para>
      <d:para>
        Let the ellipse be <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1\ldots$</m> (1)
      </d:para>
      <d:para>
        Equation of any tangent to ellipse (1)  is <m>$y = mx + \sqrt{a^2m^2 + b^2}\ldots$</m> (2)
      </d:para>
      <d:para>
        Equestion of a tangent to ellipse (1) perpendicular to line (2) is <m>$my + x = \sqrt{a^2 +
        b^2m^2}\ldots$</m> (3)
      </d:para>
      <d:para>
        In order to find the locus of the point of intersection of line (2) and (3), we need to eliminate
        <m>$m$</m>.
      </d:para>
      <d:para>
        Squaring (2) and (3) and adding gives us <m>$\left(1 + m^2\right)\left(x^2 + y^2\right) = \left(1 +
        m^2\right)\left(a^2 + b^2\right)$</m>
      </d:para>
      <d:para>
        <m>\[\Rightarrow x^2 + y^2 = a^2 + b^2\tag{6.14}\]</m>
      </d:para>
      <d:para>
        which is the equation of the director circle of the ellipse (1).
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Focal Distances</d:title>
      <d:figure xml:id="cs27">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_20.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        The focal distances of the point <m>$P(x, y)$</m> on the ellipse <m>$\frac{x^2}{a^2} +
        \frac{y^2}{b^2} = 1$</m> are <m>$a + ex$</m> and <m>$a - ex$</m>.
      </d:para>
      <d:para>
        Since <m>$P$</m> lies on the ellipse, therefore, <m>$PS' = ePM' = e\frac{|ex + a|}{e} = |ex + a| = a
        + ex$</m>
      </d:para>
      <d:para>
        Again <m>$PS + ePM = e\frac{|ex - a|}{e} = |ex - a| = a - ex[\because |ex| &lt; a]$</m>
      </d:para>
      <d:para>
        Cleaerly, <m>$PS + PS' = 2a$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Four Normals on an Ellipse from a Point</d:title>
      <d:para>
        Let the ellipse be <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1\ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$P(\alpha, \beta)$</m> be a point in the <m>$xy$</m>-plane.
      </d:para>
      <d:para>
        Equation of any normal to the ellipse is <m>$ax\sec\theta - by\csc\theta = a^2 - b^2\ldots$</m> (2)
      </d:para>
      <d:para>
        If line (2) passes through <m>$P(\alpha, \beta)$</m>, then
      </d:para>
      <d:para>
        <m>$a\alpha\sec\theta - b\beta\csc\theta = a^2 - b^2\ldots$</m> (3)
      </d:para>
      <d:para>
        <m>$\Rightarrow a\alpha\left(\frac{1 + \tan^2\frac{\theta}{2}}{1 - \tan^2\frac{\theta}{2}}\right) +
        b\beta\left(\frac{1 + \tan^2\frac{\theta}{2}}{2\tan\frac{\theta}{2}}\right) = a^2 - b^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow 2a\alpha\left(1 + \tan^2\frac{\theta}{2}\right)\tan\frac{\theta}{2} - b\beta\left(1
        - \tan^4\frac{\theta}{2}\right) = 2\left(a^2 - b^2\tan\frac{\theta}{2}\left(1 -
        tan^2\frac{\theta}{2}\right)\right)$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow b\beta t^4 - 2\left(a^2 - b^2 + a\alpha\right)t^3 - 2\left(a^2 - b^2 -
        a\alpha\right)t - b\beta = 0$</m>, where <m>$t = \tan\frac{\theta}{2}$</m>
      </d:para>
      <d:para>
        The above equation will give <m>$4$</m> values of <m>$t$</m>. Corresponding to these four values of
        <m>$t$</m>, we will get four points on the ellipse the normals to which will pass through
        <m>$P(\alpha, \beta)$</m>.
      </d:para>
      <d:para>
        Hence, in general four normals can be drawn from any to an ellipse. These four points are called
        <d:emphasis role="italic">co-normal points</d:emphasis>.
      </d:para>
      <d:para>
        <d:emphasis role="bold">Corollary:</d:emphasis> If <m>$\alpha, \beta, \gamma, \delta$</m> be the
        eccentric angles of four points on the ellipse such that the normals at these points are concurrent,
        then <m>$\alpha + \beta + \gamma + \delta$</m> is an odd multiple of <m>$\pi$</m>.
      </d:para>
      <d:para>
        Let the normals be concurrent at <m>$P(\alpha, \beta)$</m>, then from previous section the equation
        <m>$\Rightarrow b\beta t^4 - 2\left(a^2 - b^2 + a\alpha\right)t^3 - 2\left(a^2 - b^2 -
        a\alpha\right)t - b\beta = 0$</m> will have four roots, which are  <m>$\tan\frac{\alpha}{2},
        \tan\frac{\beta}{2}, \tan\frac{\gamma}{2}$</m> and <m>$\tan\frac{\delta}{2}$</m>.
      </d:para>
      <d:para>
        From Vieta's relations <m>$\displaystyle\sum\tan\frac{\alpha}{2}\tan\frac{\beta}{2} = 0$</m> and
        <m>$\displaystyle\prod\tan\frac{\alpha}{2} = -1$</m>
      </d:para>
      <d:para>
        Thus, <m>$1 - \displaystyle\sum\tan\frac{\alpha}{2}\tan\frac{\beta}{2} + \prod\tan\frac{\alpha}{2} =
        0$</m>
      </d:para>
      <d:para>
        Now <m>$\tan\frac{\alpha + \beta + \gamma + \delta}{2} = \frac{\displaystyle\sum\tan\frac{\alpha}{2}
        - \sum\tan\frac{\alpha}{2}\tan\frac{\beta}{2}\tan\frac{\gamma}{2}}{1 -
        \displaystyle\sum\tan\frac{\alpha}{2}\tan\frac{\beta}{2} + \prod\tan\frac{\alpha}{2}}$</m>
      </d:para>
      <d:para>
        The denominator of above equation is <m>$0$</m>, and hence, <m>$\alpha + \beta + \gamma +
        \delta$</m> is an odd multiple of <m>$\pi$</m>.
      </d:para>
    </d:sect2>
  </d:sect1>
  <d:sect1>
    <d:title>Hyperbola</d:title>
    <d:sect2>
      <d:title>Definition</d:title>
      <d:para>
        A hyperbola is the locus of the point which moves such that the ratio of its distance from a fixed
        point and a fixed straight line is a constant(greater than unity).
      </d:para>
      <d:figure xml:id="cs28">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_26.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        The fixed point is called the <d:emphasis role="italic">focus</d:emphasis>, the fixed line the
        <d:emphasis role="italic">directrix</d:emphasis> and the constant ratio the <d:emphasis
        role="italic">eccentricity</d:emphasis> of the hyperbola. Eccentricity is denoted by <m>$e$</m>.
      </d:para>
      <d:para>
        In the given figure <m>$S$</m> is the focus and <m>$MM'$</m> the directrix. Let <m>$P$</m> be any
        given point on the hyperbola then <m>$\frac{PS}{PN} = e > 1$</m>.
      </d:para>
      <d:para>
        Equation of the hyperbola can be obtained if the coordinates of its focus, equation of its directrix
        and the eccentricity are known.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Standard Equation of the Hyperbola</d:title>
      <d:para>
        We will find the standard equation of hyperbola as <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1$</m>.
      </d:para>
      <d:figure xml:id="cs29">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_27.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let <m>$S$</m> be the focus and <m>$MM'$</m> the directrix of the hyperbola. Draw <m>$SH\perp MM'$</m>.
      </d:para>
      <d:para>
        We divide <m>$SH$</m> internally and externally in the ratio <m>$e:1(e > 1)$</m> at points
        <m>$A$</m> and <m>$A'$</m> respectively, then
      </d:para>
      <d:para>
        <m>$\frac{AS}{AH} = e\ldots$</m> (1) and <m>$\frac{A'S}{A'H} = e\ldots$</m> (2)
      </d:para>
      <d:para>
        Clearly, <m>$A$</m> and <m>$A'$</m> will lie on the parabola. Let <m>$AA' = 2a$</m>. Let <m>$O$</m>
        be the middle point of <m>$AA'$</m>. We take <m>$O$</m> as the origin and <m>$OS$</m> and
        <m>$OY$</m> as <m>$x$</m> and <m>$y$</m> axes respectively.
      </d:para>
      <d:para>
        From (1), <m>$AS = eAH$</m> and from (3), <m>$A'S = eA'H\Rightarrow A'S - AS = e\left(A'H - AH\right)$</m>.
      </d:para>
      <d:para>
        <m>$\Rightarrow AA' = e\left(AA' - AH - AH\right) = e(2a - 2AH)\Rightarrow 2a = 2e(a -
        AH)\Rightarrow \frac{a}{e} = a - AH$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow AH = a - \frac{a}{e}\ldots$</m> (3)
      </d:para>
      <d:para>
        From (1), <m>$AS = eAH = ae - a \therefore OS = OA + AS = a + ae - a = ae \therefore S = (ae,
        0)\ldots$</m> (4)
      </d:para>
      <d:para>
        Also <m>$OH = OA - AH = a - \left(a - \frac{a}{e}\right) = \frac{a}{e}$</m>
      </d:para>
      <d:para>
        Therefore, equation of <m>$MM'$</m> is <m>$x = \frac{a}{e}$</m> or <m>$ex - a = 0\ldots$</m> (5)
      </d:para>
      <d:para>
        Let <m>$P(\alpha, \beta)$</m> be a point on the hyperbola. We draw <m>$PN\perp MM'$</m>.
      </d:para>
      <d:para>
        Then <m>$\frac{PS}{PN} = e$</m> or <m>$PS^2 = e^2PN^2\Rightarrow (\alpha - ae)^2 + \beta^2 =
        e^2\left(\frac{|e\alpha - a|}{e}\right)^2\ldots$</m> (6)
      </d:para>
      <d:para>
        <m>$\Rightarrow (\alpha - ae)^2 + \beta^2 = (e\alpha - a)^2 \Rightarrow \left(e^2 - 1\right)\alpha^2
        - \beta^2 = \left(e^2 - a\right)a^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \frac{\alpha^2}{a^2} - \frac{\beta^2}{a^2\left(e^2 - 1\right)} = 1\Rightarrow
        \frac{\alpha^2}{a^2} - \frac{\beta^2}{b^2} = 1$</m>, where <m>$b^2 = (e^2 - 1)a^2$</m>
      </d:para>
      <d:para>
        Hence locus of <m>$P(\alpha, \beta)$</m> i.e. equation of the hyperbola is <m>\[\frac{x^2}{a^2} -
        \frac{y^2}{b^2} = 1\tag{6.15}\]</m>
      </d:para>
      <d:para>
        <d:emphasis role="bold">Note:</d:emphasis> From (6) it is clear than if focus of the hyperbola is
        <m>$(-ae, 0)$</m> and directrix is <m>$x = -\frac{a}{e}$</m>, then also equation of the hyperbola
        will be same. Thus, hyperbola has two foci; <m>$(ae, 0)$</m> and <m>$(-ae, 0)$</m> with two
        directrices <m>$x = \frac{a}{e}$</m> and <m>$x = -\frac{a}{e}$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Graph of a Hyperbola</d:title>
      <d:para>
        Let the equation of the hyperbola is <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1\ldots$</m>(1)
      </d:para>
      <d:para>
        The graph is given below:
      </d:para>
      <d:figure xml:id="cs30">
        <d:title>Graph of a hyperbola</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_28.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        (i) When <m>$y = 0, x = \pm a$</m>. Therefore, hyperbola cuts the <m>$x$</m>-axis at <m>$(a, 0)$</m>
        and <m>$(-a, 0)$</m>
      </d:para>
      <d:para>
        (ii) When <m>$x = 0, y^2 = -b^2$</m>, which is not possible. Hence, the hyperbola does not cut the
        <m>$y$</m>-axis.
      </d:para>
      <d:para>
        (iii) When <m>$y$</m> is replaced by <m>$-y$</m>, the equation of hyperbola does not change, hence
        it is symmetrical about <m>$x$</m>-axis.
      </d:para>
      <d:para>
        (iv) When <m>$x$</m> is replaced by <m>$-x$</m>, the equation of hyperbola does not change, hence
        it is symmetrical about <m>$y$</m>-axis.
      </d:para>
      <d:para>
        (v) From (1), <m>$y = \pm\frac{b}{a}\sqrt{x^2 - a^2}$</m>. Since <m>$y$</m> is real, therefore,
        <m>$x^2 - a^2 \geq 0\Rightarrow x\leq -a$</m> or <m>$x\geq a$</m>.
      </d:para>
      <d:para>
        Hence, no part of the parabola will lie between the lines <m>$x = -a$</m> and <m>$x = a$</m>.
      </d:para>
      <d:para>
        When <m>$x\rightarrow \infty, y\rightarrow \pm\infty$</m> and when <m>$x\rightarrow -\infty,
        y\rightarrow \pm\infty$</m>
      </d:para>
      <d:para>
        Also, <m>$x = \pm\frac{a}{b}\sqrt{y^2 + b^2}$</m>. Hence, <m>$x$</m> is reall for all real values of
        <m>$y$</m>. Thus, <m>$-\infty &lt; y &lt; \infty$</m>
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Terms Related to Hyperbola</d:title>
      <d:para>
        Given below are some terms related to hyperbola.
      </d:para>
      <d:figure xml:id="cs31">
        <d:title>Conjugate axes and latus rectum of a hyperbola</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_29.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:orderedlist>
        <d:listitem>
          <d:emphasis role="bold">Vertices and transverse axis:</d:emphasis> If the line perpendicular to
          the directrix from focus cuts the hyperbola at two points <m>$A$</m> and <m>$A'$</m>, then
          <m>$A$</m> and <m>$A'$</m> are called the vertices and <m>$AA'$</m> the transverse axis of the
          hyperbola.
        </d:listitem>
        <d:listitem>
          <d:emphasis role="bold">Conjugate axis:</d:emphasis> The hyperbola does not meet at
          <m>$y$</m>-axis. However, we take two points <m>$B_1$</m> and <m>$B_2$</m> on <m>$y$</m>-axis, on
          opposite side of center <m>$O$</m> of the hyperbola such that <m>$OB_1 = OB_2 =
          b$</m>. <m>$B_1B_2$</m> is called the conjugate axis of the parabola.
        </d:listitem>
        <d:listitem>
          <d:para>
            <d:emphasis role="bold">Latus rectum:</d:emphasis> The chord of the hyperbola through its focus
            and perpendicular to the transverse axis is called the latus rectum.
          </d:para>
          <d:para>
            Let <m>$L_1 = (ae, \beta)$</m>. Then since <m>$L_1$</m> lies on the hyperbola <m>$\frac{x^2}{a^2}
            - \frac{y^2}{b^2} = 1$</m>, therefore, <m>$e^2 - \frac{\beta^2}{b^2} = 1\Rightarrow
            \frac{\beta^2}{b^2} = e^2 - 1$</m>
          </d:para>
          <d:para>
            <m>$\therefore \beta^2 = b^2\left(e^2 - 1\right)\Rightarrow \beta^2 = b^2\left(\frac{b^2}{a^2}\right)$</m>
          </d:para>
          <d:para>
            <m>$\beta = \pm\frac{b^2}{a}$</m>. Hence, length of latus rectum <m>$L_1L_1' = 2|\beta| =
            \frac{2b^2}{a}$</m>.
          </d:para>
        </d:listitem>
      </d:orderedlist>
    </d:sect2>
    <d:sect2>
      <d:title>An Important Property</d:title>
       <d:figure xml:id="cs32">
        <d:title></d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_30.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        The difference of the focal distances of any point on a hyperbola is a constant and is equal to the
        length of the transverse axis of the hyperbola.
      </d:para>
      <d:para>
        Let <m>$P(x, y)$</m> be any point on the hyperbola <m>$\frac{x^2}{a^2} - \frac{y^2}{b^1} = 1
        \ldots$</m> (1)
      </d:para>
      <d:para>
        Now <m>$SP = ePN = eQM\ldots$</m> (2) and <m>$S'P = ePN' = eMQ'\ldots$</m> (3)
      </d:para>
      <d:para>
        Subtracting (2) from (3) gives us <m>$S'P - SP = e(MQ' - QM)\ldots$</m> (4)
      </d:para>
      <d:para>
        But <m>$Q'M = QC + CM = \frac{a}{e} + x$</m> and <m>$QM = CM - CQ = x - \frac{a}{e}$</m>
      </d:para>
      <d:para>
        Hence from (4), <m>$S'P - SP = 2a = $</m> length of the transverse axis <m>$A_1A_2$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Conjugate Hyperbolas</d:title>
      <d:figure xml:id="cs33">
        <d:title>Conjugate hyperbolas</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_31.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        The equation <m>$\frac{y^2}{b^2} - \frac{x^2}{a^2} = 1$</m> is another standard form of equation of
        a hyperbola.
      </d:para>
      <d:para>
        The transverse and conjugate axes of this hyperbola coincide with the conugate and transverse axes
        of the hyperbola <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1$</m>.
      </d:para>
      <d:para>
        Such hyperbolas are said to be conjugate to one another.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Tanngent to a Hyperbola</d:title>
      <d:para>
        ELet the hyperbola be <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1\ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$P = (x-1, y_1)$</m>. From (1), <m>$\frac{dy}{dx} = \frac{b^2}{a^2}\frac{x_1}{y_1}$</m> at
        <m>$P$</m>.
      </d:para>
      <d:para>
        Equation of tangent at <m>$P(x_1, y_1)$</m> is <m>$y - y_1 = \frac{b^2}{a^2}\frac{x_1}{y_1}(x -
        x_1)$</m>
      </d:para>
      <d:para>
        <m>\[\Rightarrow \frac{xx_1}{a^2} - \frac{yy_1}{b^2} = 1\tag{6.16}\]</m>
      </d:para>
      <d:para>
        which is the equation of a tangent to a parabola at <m>$(x_1, y_1)$</m>.
      </d:para>
      <d:para>
        Thus, equation of tangent at <m>$(a\sec\theta, b\tan\theta)$</m> is given by
        <m>$\frac{x\sec\theta}{a} - \frac{y\tan\theta}{b} = 1$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Normal to a Parabola</d:title>
      <d:para>
        Let the hyperbola be <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1\ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$P = (x-1, y_1)$</m>. From (1), <m>$\frac{dy}{dx} = \frac{b^2}{a^2}\frac{x_1}{y_1}$</m> at
        <m>$P$</m>.
      </d:para>
      <d:para>
        Equation of normal at <m>$P(x_1, y_1)$</m> is <m>$y - y_1 = -\frac{a^2}{b^2}\frac{y_1}{x_1}(x -
        x_1)$</m>
      </d:para>
      <d:para>
        <m>\[\Rightarrow \frac{x - x_1}{\frac{x_1}{a^2}} = - \frac{y - y_1}{\frac{y_1}{b^2}}\tag{6.17}\]</m>
      </d:para>
      <d:para>
        which is the equation of a normal to a parabola at <m>$(x_1, y_1)$</m>.
      </d:para>
      <d:para>
        Thus, equation of normal at <m>$(a\sec\theta, b\tan\theta)$</m> is given by <m>$ax\cos\theta +
        by\cot\theta = a^2 + b^2$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Condition for a Line <m>$y = mx + c$</m> to be a Tangent to a Hyperbola</d:title>
      <d:para>
        Let the hyperbola be <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1\ldots$</m> (1)
      </d:para>
      <d:para>
        Let given line be <m>$y = mx + c\ldots$</m> (2)
      </d:para>
      <d:para>
        Solving gives us <m>$\frac{x^2}{a^2} - \frac{(mx + c)^2}{b^2} = 1$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \left(b^2 - a^2m^2\right)x^2 - 2a^2mcx - a^2\left(c^2 + b^2\right) = 0\ldots$</m> (3)
      </d:para>
      <d:para>
        Line (2) will be tangent if roots of (3) are equal i.e. <m>$D = 0$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow 4a^4m^2c^2 + 4a^2\left(b^2 - a^2m^2\right)(c^2 + b^2) = 0$</m>
      </d:para>
      <d:para><m>$\Rightarrow a^2m^2c^2 + b^2c^2 - a^2c^2m^2 + b^4 - a^2b^2m^2 = 0$</m></d:para>
      <d:para>
        <m>$\Rightarrow b^2c^2 + b^4 - a^2b^2m^2 = 0 \Rightarrow c = \pm\sqrt{a^2m^2 - b^2}$</m>
      </d:para>
      <d:para>
        which is the required condition.
      </d:para>
      <d:para>
        Thus, we find two tangents <m>$y = mx + \sqrt{a^2m^2 - b^2}$</m> and <m>$y = mx - \sqrt{a^2m^2 -
        b^2}$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Aymptote of a Hyperbola</d:title>
      <d:para>
        An asymptote of a conic is a straight line which meets the curve in two points which are at infinity
        but which itself does not lie entirely at infinity. A hyperbola has two asymptotes.
      </d:para>
      <d:para>
        Let the hyperbola be <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1\ldots$</m> (1)
      </d:para>
      <d:para>
        Let <m>$P(x_1, y_1)$</m> be a point on hyperbola (1), then <m>$\frac{x_1^2}{a^2} - \frac{y_1^1}{b^2}
        = 1\ldots$</m> (2)
      </d:para>
      <d:para>
        Equation of tangent to hyperbola (1) at <m>$P$</m> is <m>$\frac{xx_1}{a^2} - \frac{yy_1}{b^2} = 1\ldots$</m>(3)
      </d:para>
      <d:para>
        <m>$\Rightarrow \frac{xx_1}{a^2} - \frac{y}{b^2}\left(\pm\frac{b}{a}\sqrt{x_1^2 - a^2}\right) = 1$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow \frac{x}{a^2}\mp\frac{y}{ab}\sqrt{1 - \frac{a^2}{x_1^2}} =
        \frac{1}{x_1}\ldots$</m> (4)
      </d:para>
      <d:para>
        When <m>$x_1\rightarrow\infty$</m>, line (4) becomes <m>$\frac{x}{a^2}\mp\frac{y}{ab}\Rightarrow
        \frac{x}{a} \mp \frac{y}{b} = 0$</m>.
      </d:para>
      <d:para>
        Thus, asymptotes of hyperbola (1) are <m>$y = \pm\frac{b}{a}x\ldots$</m> (5)
      </d:para>
      <d:para>
        Combined equation of asymptotes is <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 0\ldots$</m> (6)
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Rectangular or Equilateral Hyperbola</d:title>
      <d:figure xml:id="cs34">
        <d:title>Rectangular hyperbola</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/6_32.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        For rectangular hyperbola <m>$a = b$</m>, therefore, equation of rectangular hyperbola is <m>$x^2 -
        y^2 = a^2$</m>.
      </d:para>
      <d:para>
        We see that asymptotes are right angle each inclined to <m>$x$</m>-axis at an angle of
        <m>$45^\circ$</m>.
      </d:para>
      <d:para>
        Rotating the axes through an angle of <m>$45^\circ$</m> in clockwise direction makes the equation of
        rectangular hyperbola as
      </d:para>
      <d:para>
        <m>$\left[x\cos\left(-45^\circ\right) - y\sin\left(-45^\circ\right)\right]^2 -
        \left[x\sin\left(-45^\circ\right) + y\cos\left(-45^\circ\right)\right]^2 = a^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow xy = \frac{a^2}{2} = c^2$</m>, where <m>$c^2 = \frac{a^2}{2}$</m>.
      </d:para>
      <d:para>
        Thus, equation of a rectangular hyperbola when its asymptotes are taken as axes is <m>\[xy =
        c^2\tag{6.18}\]</m>
      </d:para>
      <d:para>
        <d:emphasis role="bold">Note:</d:emphasis> If we rotate the axes through the same aangle in
        anti-clockwise direction, then equation of rectangular hyperbola will be <m>$xy = -\frac{a^2}{2}$</m>.
      </d:para>
      <d:para>
        If equation of a rectangular hyperbola is <m>$xy = c^2$</m>, then the equation of its conjugate
        hyperbola will be <m>$xy = -c^2$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Parametric Equation of a Rectangular Hyperbola</d:title>
      <d:para>
        Parametric equation of the rectangular hyperbola <m>$xy = c^2$</m> is <m>$x = ct, y = \frac{c}{t}$</m>.
      </d:para>
      <d:para>
        Thus, coordinates of any points on the rectangular hyperbola <m>$xy = c^2$</m> may be taken as
        <m>$\left(ct, \frac{c}{t}\right)$</m>.
      </d:para>
      <d:para>
        The point <m>$\left(ct, \frac{c}{t}\right)$</m> is also called the point '<m>$t$</m>'.
      </d:para>
      <d:orderedlist>
        <d:listitem>
          Equation of the chord of the hyperbola <m>$xy = c^2$</m> joining points <m>$t_1$</m> and
          <m>$t_2$</m> is <m>$x + t_1t_2y = c(t_1 + t-2)$</m>.
        </d:listitem>
        <d:listitem>
          The equation of the tangent to hyperbola <m>$xy = c^2$</m> at '<m>$t$</m>' is <m>$x + t^2y =
          2ct$</m>.
        </d:listitem>
        <d:listitem>
          The equation of the normal to hyperbola <m>$xy = c^2$</m> at '<m>$t$</m>' is <m>$y - t^2x =
          \frac{c}{t}\left(1 - t^4\right)$</m> or <m>$t^3x - ty - ct^4 + c = 0$</m>.
        </d:listitem>
      </d:orderedlist>
    </d:sect2>
    <d:sect2>
      <d:title>Some Equations Related to Hyperbolas</d:title>
      <d:orderedlist>
        <d:listitem>
          Equation of the chord of the hyperbola <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1$</m> whose middle
          point is <m>$(x_1, y_1)$</m> is <m>$T = S_1$</m> i.e. <m>$\frac{xx_1}{a^2} - \frac{yy_1}{b^2} - 1
          = \frac{x_1^2}{a^2} - \frac{y_1^2}{b^2} - 1$</m>.
        </d:listitem>
        <d:listitem>
          Point <m>$(x_1, y_1)$</m> lies inside, in or outside the hyperbola <m>$\frac{x^2}{a^2} -
          \frac{y^2}{b^2} = 1$</m> according as <m>$\frac{x_1^2}{a^2} - \frac{y_1^2}{b^2} &lt;, =$</m> or
          <m>$> 1$</m>.
        </d:listitem>
        <d:listitem>
          Equation of pair of tangents from point <m>$(x_1, y_1)$</m> to the hyperbola <m>$\frac{x^2}{a^2} -
          \frac{y^2}{b^2} = 1$</m> is <m>$SS_1 = T^2$</m> i.e. <m>$\left(\frac{x^2}{a^2} - \frac{y^2}{b^2} -
          1\right)\left(\frac{x_1^2}{a^2} - \frac{y_1^2}{b^2} - 1\right) = \left(\frac{xx_1}{a^2} -
          \frac{yy_1}{b^2} - 1\right)^2$</m>
        </d:listitem>
        <d:listitem>
          Equation of chord of contact of point <m>$(x_1, y_1)$</m> w.r.t. the hyperbola is <m>$T = 0$</m>
          or <m>$\frac{xx_1}{a^2} - \frac{yy_1}{b^2} = 1$</m>.
        </d:listitem>
        <d:listitem>
          Equation of chord of the hyperbola <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1$</m> joining points
          <m>$(a\sec\theta_1, b\tan\theta_1)$</m> and <m>$(a\cos\theta_1, b\tan\theta_1)$</m> is
          <m>$\frac{x}{x}\cos\left(\frac{\theta_1 - \theta_2}{2}\right) -
          \frac{y}{b}\sin\left(\frac{\theta_1 + \theta_2}{2}\right) = \cos\frac{\theta_1 + \theta_2}{2}$</m>.
        </d:listitem>
      </d:orderedlist>
    </d:sect2>
    <d:sect2>
      <d:title>Diameter of a Conic</d:title>
      <d:para>
        It is the locus of the middle points of a conic of all parallel chords. In order to find the
        equation of any diameter, slope of parallel chords must be known. The locus of the middle points of
        a system of parallel chords of the hyperbola <m>$\frac{x^2}{a^2} - \frac{y^2}{a^1} = 1$</m> with
        slope <m>$m$</m> is <m>$y = \frac{b^2}{a^2m}x$</m>.
      </d:para>
      <d:para>
        Thus, the equation of the diameter of the hyperbola <m>$\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1$</m>,
        which is the locus of middle points of parallel chords having slope <m>$m$</m> is <m>$y =
        \frac{b^2}{a^2m}x$</m>.
      </d:para>
    </d:sect2>
  </d:sect1>
  <d:sect1>
    <d:title>Problems</d:title>
    <d:orderedlist>
      <d:listitem>
        <!-- 1 -->
        Find the equation of the parabola whose focus is <m>$(1, 1)$</m> and the directrix is <m>$x + y + 1
        = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 2 -->
        Find the equation of the parabola whose focus is <m>$(1, -1)$</m> and vertex is <m>$(2, 1)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 3 -->
        Find the equation of the parabola whose focus is <m>$(-1, 2)$</m> annd directrix is <m>$x - 2y - 15
        = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 4 -->
        Find the equation of the parabola whose focus is <m>$(2, 3)$</m> annd directrix is <m>$x - 2y - 6
        = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 5 -->
        Find the equation of the parabola whose focus is <m>$(-1, 1)$</m> and the directrix is <m>$x + y + 1
        = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 6 -->
        Find the equation of the parabola whose focus is <m>$(5, 3)$</m> and the directrix is <m>$3x - 4y +
        1 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 7 -->
        Find the equation of the parabola whose focus is <m>$\left(\frac{5}{4}, -1\right)$</m> and the
        directrix is <m>$4x - 13 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 8 -->
        Find the equation of the parabola having the vertex at <m>$(0, 1)$</m> and the focus at <m>$(0,
        0)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 9 -->
        Prove that the equation of the parabola whose focus is <m>$(0, 0)$</m> and tangent at the vertex is
        <m>$x - y + 1 = 0$</m> is <m>$x^2 + y^2 + 2xy - 4x + 4y - 4 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 10 -->
        Find the equation of the parabola if the focus is at <m>$(-6, -6)$</m> and vertex is at <m>$(-2,
        2)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 11 -->
        Find the equation to the parabola whose axis is parallel to <m>$y$</m>-axis and which passes through
        the points <m>$(0, 4), (1, 9)$</m>, and <m>$(-2, 6)$</m> and determine its latus rectum.
      </d:listitem>
      <d:listitem>
        <!-- 12 -->
        Find the vertex, focus, directrix and length of the latus rectum of the parabola <m>$y^2 - 4y - 2x -
        8 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 13 -->
        Find the vertex, axis, focus, directrix, tangent at the vertex and length of the latus recturm of
        the parabola <m>$2y^2 + 3y - 4x - 3 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 14 -->
        Find the vertex, focus, axis and latus rectum of the parabola <m>$4y^2 + 12x - 20y + 67 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 15 -->
        Find the focus and directrix of the parabola <m>$3x^2 + 12x + 8y = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 16 -->
        Find the equation of the circle described on the line segment joining the foci of the parabola
        <m>$x^2 = 4ay$</m> and <m>$y^2 = 4a(x - a)$</m> as diameter.
      </d:listitem>
      <d:listitem>
        <!-- 17 -->
        For the parabola <m>$y^2 = 4ax$</m> find the extremeties of a double ordinate of length
        <m>$8a$</m>. Prove that the lines from the vertex to its extremeties are at right angle.
      </d:listitem>
      <d:listitem>
        <!-- 18 -->
        Find the vertex, axis, focus and latus rectum of the parabola whose parametric equations are <m>$x =
        u\cos\alpha t, y = u\sin\alpha t - \frac{1}{2}gt^2$</m>, where <m>$u, \alpha, g$</m> are constants.
      </d:listitem>
      <d:listitem>
        <!-- 19 -->
        Find the equation of normals to the parabola <m>$y^2 = 4ax$</m> at the ends of latus rectum. If the
        normals again meet the parabola at <m>$Q, Q'$</m> prove that <m>$QQ' = 12a$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 20 -->
        Prove that through any point three normals can be drawn to a parabola and the algebraic sum of the
        ordinates of the three points is zero.
      </d:listitem>
      <d:listitem>
        <!-- 21 -->
        Find the locus of the point of intersection of the tangents to the parabola <m>$y^2 = 4ax$</m> which
        includes an angle <m>$\alpha$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 22 -->
        Prove that the tangent at the extremeties of any focal chord intersect at right angles on the
        directrix.
      </d:listitem>
      <d:listitem>
        <!-- 23 -->
        On the parabola <m>$y^2 = 4ax$</m>, three points <m>$E, F, G$</m> are taken so that their ordinates
        are in G.P. Prove that the tangents at <m>$E$</m> and <m>$G$</m> intersect on the ordinate
        <m>$F$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 24 -->
        Find the common tangents of <m>$x^2 + y^2 = 2a^2$</m> and <m>$y^2 = 4ax$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 25 -->
        Find the angle at which the parabolas <m>$y^2 = 4ax$</m> and <m>$x^2 =4by$</m> cut each other.
      </d:listitem>
      <d:listitem>
        <!-- 26 -->
        Show that the length of the tangent to the parabola <m>$y^2 = 4ax$</m> intercepted between two its
        point of contact and the axis of the parabola is bisected by the tangent at the vertex.
      </d:listitem>
      <d:listitem>
        <!-- 27 -->
        Show that the normal at a point <m>$\left(at_1^2, 2at_1\right)$</m> on the parabola <m>$y^2 =
        4ax$</m> cuts the curve again at the point whose parameter <m>$t_2 = -t_1 - \frac{2}{t_1}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 28 -->
        <m>$P, Q$</m> are the points '<m>$t_1$</m>', '<m>$t_2$</m>' on the parabola <m>$y^2 = 4ax$</m>. If
        the normals at <m>$P, Q$</m> meet on the parabola at <m>$R$</m>, show that <m>$t_1t_2 = 2$</m>. Also
        find the locus of the mid-point of <m>$PQ$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 29 -->
        Find the equation of the tangents to the parabola <m>$y^2 =8x$</m> inclined at <m>$45^\circ$</m> to
        the <m>$x$</m>-axis. Find also the point of contact.
      </d:listitem>
      <d:listitem>
        <!-- 30 -->
        Find the equations of the tangents to the parabola <m>$y^2 = 4ax$</m> at the ends of its latus
        rectum. Also show that they are perpendicular to each other.
      </d:listitem>
      <d:listitem>
        <!-- 31 -->
        Find the equation of the tangents to the parabola <m>$y^2 = 16x$</m> which are parallel and
        perpendicular respectively to the line <m>$2x - y + 5 = 0$</m>. Also find the coordinates of their
        point of contact.
      </d:listitem>
      <d:listitem>
        <!-- 32 -->
        The tangents to the parabola <m>$y^2 = 4ax$</m> at <m>$P\left(at_1^2, 2at_1\right)$</m> and
        <m>$Q\left(at_2^2, 2at_2\right)$</m> intersect at <m>$R$</m>. Prove that the area of the
        <m>$\triangle PQR$</m> is <m>$\frac{1}{2}a^2(t_1 - t_2)^3$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 33 -->
        Prove that the oridinate of the point of intersection of two tangents to the parabola <m>$y^2 =
        4ax$</m> is the A.M. between the ordinates of the point of contact of the tangents.
      </d:listitem>
      <d:listitem>
        <!-- 34 -->
        Show that the locus of the points of intersection of the mutually perpendicular tangents to a
        parabola is the directrix of parabola.
      </d:listitem>
      <d:listitem>
        <!-- 35 -->
        If the tangents at two points to a parabola are at right angle, show that the line joining the two
        points passes through the focus.
      </d:listitem>
      <d:listitem>
        <!-- 36 -->
        Find the point of intersection of the tangents to the parabola <m>$y^2 = 4ax$</m> at the points
        <m>$t_1$</m> and <m>$t_2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 37 -->
        A tangent to the parabola <m>$y^2 = 8x$</m> makes an angle of <m>$45^\circ$</m> with the straight
        line <m>$y = 3x + 5$</m>. Find the equation of the tangents and its point of contact.
      </d:listitem>
      <d:listitem>
        <!-- 38 -->
        For what value of <m>$k$</m> does the line <m>$x + y + 1 = 0$</m> touch the parabola <m>$y^2 =
        kx$</m>? Also find the point of contact.
      </d:listitem>
      <d:listitem>
        <!-- 39 -->
        Find the locus of the point of intersection of the tangents to the parabola <m>$y^2 = 4ax$</m> which
        include an angle <m>$45^\circ$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 40 -->
        Find the equation of the normal to the parabola <m>$y^2 = 4x$</m> at the point <m>$(9, -6)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 41 -->
        Find the equation of the normals to the parabola <m>$y^2 = 4x$</m> which pass through <m>$(3,
        0)$</m>. Find also the coordinates of the feet of the normals.
      </d:listitem>
      <d:listitem>
        <!-- 42 -->
        Find the equation of the normal to the parabola <m>$y^2 = 4x$</m> which is perpendicular to the line
        <m>$x + 3y + 1 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 43 -->
        Find the condition that the line <m>$\frac{x}{a} + \frac{y}{b}$ = 1</m> may be a normal to the
        parabola <m>$y^2 = 4px$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 44 -->
        Prove that the normal at <m>$\left(am^2,\,-2am\right)$</m> to the parabola <m>$y^2 = 4ax$</m>
        intersects the parabola again at an angle <m>$\tan^{-1}\left(\frac{m}{2}\right),\,m > 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 45 -->
        Prove that from any point <m>$P\left(at^2,\, 2at\right)$</m> on the parabola <m>$y^2 = 4ax$</m>, two
        normals can be drawn and their feet <m>$Q$</m> and <m>$R$</m> have the parameters satisfying the
        equation <m>$\lambda^2 + \lambda t + 2 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 46 -->
        If the normals to the parabola <m>$y^2 = 4ax$</m> at three points <m>$P,\;Q$</m> and <m>$R$</m> meet
        at <m>$A$</m> and <m>$S$</m> be the the focus, then prove that <m>$SP.SQ.SR = a.SA^2$</m>, where
        <m>$a > 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 47 -->
        Prove that the normal chord to a parabola at the point whose ordinate is equal to the abscissa,
        subtends a right angle at the focus.
      </d:listitem>
      <d:listitem>
        <!-- 48 -->
        Find the locus of the point of intersection of the two normals to the parabola <m>$y^2 = 4ax$</m>
        which are at right angles to one another.
      </d:listitem>
      <d:listitem>
        <!-- 49 -->
        The normals to the parabola <m>$y^2 = 4ax$</m> from a point <m>$P$</m> meet the axis at
        <m>$A,\,B\,C$</m>. If <m>$B$</m> is the middle point of <m>$AC$</m>, find the locus of <m>$P$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 50 -->
        Prove that the area of the triangle formed by the normals to the parabola <m>$y^2 = 4ax$</m> at
        points <m>$t_1, t_2$</m> and <m>$t_3$</m> is <m>$\frac{1}{2}a^2[(t_1 - t_2)(t_2 - t_3)(t_3 -
        t_1)](t_1 + t_2 + t_3)^2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 51 -->
        Find the condition that the line <m>$lx + my + n = 0$</m> may touch the parabola <m>$y^2 = 4ax$</m>
        and find the coordinates of the point of contact.
      </d:listitem>
      <d:listitem>
        <!-- 52 -->
        Show that the line <m>$x\cos\alpha + y\sin\alpha = p$</m> touches the parabola <m>$y^2 = 4ax$</m> if
        <m>$p\cos\alpha + a\sin^2\alpha = 0$</m> and that the point of contact is
        <m>$\left(a\tan^2\alpha,\,-2a\tan\alpha\right)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 53 -->
        Prove that the line <m>$x\cos\alpha + y\sin\alpha = p$</m> touches the parabola <m>$y^2 = 4a(x +
        a)$</m> if <m>$p\cos\alpha + a = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 54 -->
        Prove that the line <m>$lx + my + n = 0$</m> touches the parabola <m>$y^2 = 4a(x - b)$</m> if
        <m>$am^2 = nl - bl^2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 55 -->
        Show that <m>$2y = x + 4a$</m> is tangent to the parabola <m>$y^2 = 4ax$</m>. What are the
        coordinates of the point of contact?
      </d:listitem>
      <d:listitem>
        <!-- 56 -->
        If from the vertex of the parabola <m>$y^2 = 4ax$</m> a pair of chords be drawn at right angles to
        one another and with these chords as adjacent sides a rectangle be drawn, prove that the locus of
        the vertex of the farther angle of the rectangle is the parabola <m>$y^2 = 4a(x - 8a)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 57 -->
        Prove that the circle circumscribing the triangle formed by any three tangents to a parabola passes
        through the focus.
      </d:listitem>
      <d:listitem>
        <!-- 58 -->
        Prove that the locus of the middle points of all chords of the parabola <m>$y^2 = 4ax$</m> which are
        drawn through the vertex is the parabola <m>$y^2 = 2ax$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 59 -->
        If the point <m>$\left(at_1^2,\,2at_1\right)$</m> is one extremity of a focal chord of the parabola
        <m>$y^2 = 4ax$</m>, find the coordinates of the other extremity and show that the length of the
        chord is a <m>$\left(t_1 + \frac{1}{t_1}\right)^2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 60 -->
        Show that the equation of the chord of the parabola <m>$y^2 = 4ax$</m> through the points
        <m>$(x_1,y_1)$</m> and <m>$(x_2,y_2)$</m> on it is <m>$(y - y_1)(y - y_2) = y^2 - 4ax$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 61 -->
        Find the equation of the parabola the extremeties of whose latus rectum are <m>$(1, 2)$</m> and
        <m>$(1, -4)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 62 -->
        Find the equation of the parabola the extremeties of whose latus rectum are <m>$(3, 5)$</m> and
        <m>$(3, -3)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 63 -->
        Find the euqation of the parabola whose axis is the <m>$x$</m>-axis and which passes
        through the points <m>$(3, 2)$</m> and <m>$(-2, -1)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 64 -->
        Find the equation of the parabola with its axis parallel to <m>$x$</m>-axis and which passes through
        the points <m>$(1, 2), (-1, 3)$</m> and <m>$(-2, 1)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 65 -->
        Find the axis, tangent at the vertex, focus, directrix and latus rectum of the parabola <m>$9y^2 -
        16x - 12y - 57 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 66 -->
        Find the locus of a point whose sum of the distances from the origin and the line <m>$x = 2$</m> is
        <m>$4$</m> units. Sketch the path.
      </d:listitem>
      <d:listitem>
        <!-- 67 -->
        Find the vertex, focus, directrix, axis, tangent at the vertex and the latus rectum of the parabola
        <m>$(y - 1)^2 = 4(x - 2)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 68 -->
        Find the vertex, focus, directrix, axis, tangent at the vertex and the latus rectum of the parabola
        <m>$5x^2 + 30x + 2y + 59 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 69 -->
        Find the center and radius of the smaller of the two circles that touch the parabola <m>$75y^2 =
        64(5x - 3)$</m> at <m>$\left(\frac{6}{5}, \frac{8}{5}\right)$</m> and the <m>$x$</m>-axis.
      </d:listitem>
      <d:listitem>
        <!-- 70 -->
        A ray of light is coming along the line <m>$y = b$</m> from the positive direction of
        <m>$x$</m>-axis and strikes a concave mirror whose intersection with the <m>$xy$</m>-plane is a
        parabola <m>$y^2 = 4ax$</m>. Find the equation of the reflected ray and show that it passes through
        the focus of the parabola. Both <m>$a$</m> and <m>$b$</m> are positive.
      </d:listitem>
      <d:listitem>
        <!-- 71 -->
        Points <m>$A, B$</m> and <m>$C$</m> lie on the parabola <m>$y^2 = 4ax$</m>. The tangents to the
        parabola at <m>$A, B$</m> and <m>$C$</m> taken in pairs, intersect at points <m>$P, Q$</m> and
        <m>$R$</m>. Determine the ratio of the areas of the triangles <m>$ABC$</m> and <m>$PQR$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 72 -->
        The ordinates of the points <m>$P$</m> and <m>$Q$</m> on the parabola <m>$y^2 = 12x$</m> are in the
        ratio <m>$1:2$</m>. Find the locus of the points of intersection of the normals to the parabola at
        <m>$P$</m> and <m>$Q$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 73 -->
        Three normals are drawn from the point <m>$(14, 7)$</m> to the parabola <m>$y^2 - 16x - 8y =
        0$</m>. Find the coordinates of the feet of the normals.
      </d:listitem>
      <d:listitem>
        <!-- 74 -->
        Find the locus of the point of intersectioon of those normals to the parabola <m>$x^2 = 8y$</m>
        which are at right angles to each other.
      </d:listitem>
      <d:listitem>
        <!-- 75 -->
        Prove that the locus of the point of intersection of the tangents at the extremeties of any chord of
        the parabola <m>$y^2 = 4ax$</m> which subtend a right aangle at the vertex is <m>$x + 4x = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 76 -->
        Show that the tangent at any point of a parabola bisects the angle between the focal chord through
        the point and the perpendicular on the directrix from the point.
      </d:listitem>
      <d:listitem>
        <!-- 77 -->
        Show that the portion of the tangent to a parabola cut off between the directrix anad the curve
        subtends a right angle at the focus.
      </d:listitem>
      <d:listitem>
        <!-- 78 -->
        Show that the locus of the foot of the perpendicular from the focus on the tangent at any point of
        the parabola is the tangent at the vertex.
      </d:listitem>
      <d:listitem>
        <!-- 79 -->
        Prove that any tangent to a parabola <m>$y^2 = 4ax$</m> and perpendicular to it from the focus meet
        on the tangent at the vertex.
      </d:listitem>
      <d:listitem>
        <!-- 80 -->
        Show that the locus of the points of intersection of tangents to <m>$y^2 = 4ax$</m> which intercept
        a constant length <m>$d$</m> on the directrix is <m>$\left(y^2 - 4ax\right)(x + a)^2 = d^2x^2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 81 -->
        Prove that the middle point of the intercept made on a tangent to a parabola by the tangents at two
        points <m>$P$</m> and <m>$Q$</m> lies on the tangent which is parallel to <m>$PQ$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 82 -->
        If the tangent to the parabola <m>$y^2 = 4ax$</m> meets the axis in <m>$T$</m> and the tangent at
        the vertex in <m>$Y$</m> and the rectangle <m>$TAYG$</m> is completed, show that the locus of
        <m>$G$</m> is <m>$y^2 + ax = 0$</m> where <m>$A$</m> is the vertex of <m>$y^2 = 4ax$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 83 -->
        Show that the orthoccenter of the triangle formed by the tangents at the points <m>$t_1, t_2,
        t_3$</m> to the parabola <m>$y^2 = 4ax$</m> is <m>$(-a, a(t_1 + t_2 + t_3 + t_1t_2t_3))$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 84 -->
        Show that any three tangents to a parabola whose slopes are in H.P. enclose a triangle of constant
        area.
      </d:listitem>
      <d:listitem>
        <!-- 85 -->
        Prove that any tangent to a parabola and the perpendicular on it from the focus meet on the tengent
        at vertex.
      </d:listitem>
      <d:listitem>
        <!-- 86 -->
        Prove that the circumcircle of the triangle formed by any three tangents to parabola <m>$y^2 =
        4ax$</m> passes through the focus.
      </d:listitem>
      <d:listitem>
        <!-- 87 -->
        Equilateral triangles are circumscribed by the parabola <m>$y^2 = 4ax$</m>. Prrove that their
        angular points lie on the conic <m>$(3x + a)(x + 3a) = y^2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 88 -->
        Prove that the locus of the middle points of all tangents drawn from points on the directrix to the
        parabola <m>$y^2 = 4ax$</m> is <m>$y^2(2x + a) = a(3x + a)^2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 89 -->
        Two parabolas have the same vertex and their axes are at right angles. Prove that their common
        tangent touches each at the end of a latus rectum.
      </d:listitem>
      <d:listitem>
        <!-- 90 -->
        Two parabolas have the same vertex and their axes are at right angles. Prove that they cut again at
        an angle <m>$\tan^{-1}\frac{3}{4}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 91 -->
        If a tangent to the parabola <m>$y^2 = 4a(x + a)$</m> be at right angles to a tangent to the
        parabola <m>$y^2 = 4b(x + b)$</m>, show that the locus of the point of intersection of the tangents
        is the line <m>$x + a + b = 0$</m>, which is the common chord of the two parabolas.
      </d:listitem>
      <d:listitem>
        <!-- 92 -->
        A tangent to the parabola <m>$y^2 = 8x$</m> makes an angle <m>$45^\circ$</m> with straight line
        <m>$y = 3x + 5$</m>. Find its equation and its point of contact.
      </d:listitem>
      <d:listitem>
        <!-- 93 -->
        Two lines are drawn at right angles, one being a tangent to <m>$y^2 = 4ax$</m> and the other to
        <m>$x^2 = 4by$</m>. Show that the locus of their point of intersection is the curve <m>$(ax +
        by)(x^2 + y^2) + (bx - ay)^2 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 94 -->
        Find the equation of the common tangent to the parabola <m>$y^2 = 4ax$</m> and <m>$x^2 = 4by$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 95 -->
        If the tangents at point <m>$P$</m> and <m>$Q$</m> on the parabola <m>$y^2 = 4ax$</m> meet at
        <m>$R$</m> and <m>$S$</m> is the focus, prove that <m>$SR^2 = SP.SQ$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 96 -->
        If <m>$A\left(at_1^2, 2at_1\right)$</m> and <m>$B\left(at_2^2, 2at_2\right)$</m> be the two points
        on the parabola <m>$y^2 = 4ax$</m> and <m>$AB$</m> cuts the <m>$x$</m>-axis at <m>$C$</m> such that
        <m>$AB:AC = 3:1$</m>, show that <m>$t_2 = -2t_1$</m>. Also show that the locus of the point of
        intersection of the tangents at <m>$A$</m> and <m>$B$</m>is <m>$2y^2 = -ax$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 97 -->
        Through the vertex <m>$O$</m> of the parabola <m>$y^2 = 4ax$</m> two chords <m>$OA$</m> and
        <m>$OB$</m> are drawn and the circle on <m>$OA$</m> and <m>$OB$</m> as diameters intersect in
        <m>$C$</m>. If <m>$m-1, m_2$</m> and <m>$m_3$</m> be the slope of tangents to parabolas at
        <m>$A$</m> and <m>$B$</m> and line <m>$OC$</m> respectively, show that <m>$m_1 + m_2 + 2m_1m_2m_3 =
        0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 98 -->
        Find the locus of a point such that the two of the three normals drawn from it on the parabola
        <m>$y^2 = 4ax$</m> are perpendicular.
      </d:listitem>
      <d:listitem>
        <!-- 99 -->
        If a chord <m>$PQ$</m> of the parabola <m>$y^2 = 4ax$</m> subtends a right angle at the vertex, show
        that the locus of the point of intersection of normals at <m>$P$</m> and <m>$Q$</m> is <m>$y^2 =
        16a(x - 6a)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 100 -->
        If three normals from a point to the parabola <m>$y^2 = 4ax$</m> cut the axis in points whose
        distances from the vertex are in A.P., prove that the point lie on the curve <m>$27ay^2 = 2(x -
        2a)^3$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 101 -->
        If the normal at <m>$t_1$</m> on the parabola <m>$y^2 = 4x$</m> meets the curve again at
        <m>$t_2$</m>, find the relation between <m>$t_1$</m> and <m>$t_2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 102 -->
        The tangents at <m>$P$</m> and <m>$Q$</m> on <m>$y^2 = 4ax$</m> intersect at <m>$T$</m>. The normals
        at <m>$P$</m> and <m>$Q$</m> intersect at <m>$R$</m> on the curve. Show that the locus of the
        center of the circumcircle of <m>$\triangle TPQ$</m> is the curve <m>$2y^2 = a(x - a)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 103 -->
        Find the locus of the points of intersection of those normals to the parabola <m>$x^2 = 8y$</m>
        which are at right angles to each other.
      </d:listitem>
      <d:listitem>
        <!-- 104 -->
        In the parabola <m>$y^2 = 4ax$</m> the tangents at the point <m>$P$</m>, whose abscissa is equal to
        the latus rectum meets the axis in <m>$T$</m> and the normal at <m>$P$</m> cuts the parabola again
        in <m>$Q$</m>, prove that <m>$PY: PQ = 4:5$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 105 -->
        Prove that the feet of the normals drawn from the point <m>$(h, k)$</m> to the parabola <m>$y^2 =
        4ax$</m> lie on the curve <m>$xy - (h - 2a)y - 2ak = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 106 -->
        Show that the locus of points such that two of the three normals to the parabola <m>$y^2 = 4ax$</m>
        from them coincide is <m>$27ay^2 = 4(x - 2a)^3$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 107 -->
        The normals at two points <m>$P$</m> and <m>$Q$</m> of a parabola intersect in a point <m>$R$</m> on
        the curve. Show that the centroid of the <m>$\triangle PQR$</m> lies on the axis of the parabola.
      </d:listitem>
      <d:listitem>
        <!-- 108 -->
        Show that the area of the triangle formed by joining the foot of the normals from <m>$(h, k)$</m> to
        the parabola <m>$y^2 = 4ax$</m> is <m>$\sqrt{4a(h - 2a)^3 - 27a^2k^2}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 109 -->
        From the point whhere any normal to the parabola <m>$y^2 = 4ax$</m> meets the axis is drawn a line
        perpendicular to this normal; prove that this normal always touches an equal parabola <m>$y^2 + 4a(x
        - 2a) = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 110 -->
        Show that at the point of intersection of two mutually perpendicular normals to a parabola <m>$y^2 =
        4ax$</m>, the abscissa is never smaller than <m>$3a$</m>. What is the ordinate when the abscissa is
        smaller?
      </d:listitem>
      <d:listitem>
        <!-- 111 -->
        If a normal to a parabola makes an angle <m>$\phi$</m> with the axis, show that it will cut the
        curve again at an angle <m>$\tan^{-1}\left(\frac{1}{2}\tan\phi\right)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 112 -->
        Find the equation of the normal to the parabola <m>$y^2 = 4ax$</m> such that the chord intercepted
        upon it by the curve subtends a right angle at the vertex.
      </d:listitem>
      <d:listitem>
        <!-- 113 -->
        If the normals at two point <m>$P$</m> and <m>$Q$</m> of a parabola intersect on the
        curve, show that the line <m>$PQ$</m> cuts the axis at a fixed point.
      </d:listitem>
      <d:listitem>
        <!-- 114 -->
        Find the locus of the point of intersection of the normals of the parabola <m>$y^2 = 4ax$</m> at the
        extremeties of a focal chord.
      </d:listitem>
      <d:listitem>
        <!-- 115 -->
        <d:para>
          Tangents are drawn from the point <m>$(\alpha, \beta)$</m> to the parabola <m>$y^2 = 4ax$</m>, show
          that the length of their chord of contact is <m>$\frac{1}{|a|}\sqrt{\left(\beta^2 -
          4a\alpha\right)\left(\beta^2 + 4a^2\right)}$</m>. Also show that the area of the triangle formed by
          the tangents from <m>$(\alpha, \beta)$</m> to parabola <m>$y^2 = 4ax$</m> and the chord of contact
          is <m>$\frac{\left(\beta^2 - 4a\alpha\right)^\frac{3}{2}}{2a}$</m>.
        </d:para>
        <d:para>
          Prove that the area of the triangle formed by the tangents at points <m>$t_1$</m> and <m>$t_2$</m>
          on the parabola <m>$y^2 = 4ax$</m> with the chord joining these two points is
          <m>$\frac{a^2}{2}|t_1 - t_2|^3$</m>.
        </d:para>
      </d:listitem>
      <d:listitem>
        <!-- 116 -->
        Prove that the circle described on any focal chord of a parabola as diameter touches the directrix.
      </d:listitem>
      <d:listitem>
        <!-- 117 -->
        Show that the tangents at the extremeties of a focal chord of a parabola intersect at right angles
        on the directrix.
      </d:listitem>
      <d:listitem>
        <!-- 118 -->
        A circle on any focal chord of a parabola as diameter cuts the curve again in <m>$P$</m> and
        <m>$Q$</m>. Show that <m>$PQ$</m> passes through a fixed point.
      </d:listitem>
      <d:listitem>
        <!-- 119 -->
        If <m>$l, l'$</m> be the lengths of the segments of any focal chord of the parabola <m>$y^2 =
        2ax$</m>, prove that <m>$l, a, l'$</m> are in H.P.
      </d:listitem>
      <d:listitem>
        <!-- 120 -->
        The middle point of a variable chord <m>$AB$</m> of the parabola <m>$y^2 = 4ax$</m> lies on the line
        <m>$y = \lambda a$</m>, where <m>$\lambda$</m> is a constant, then find the equation of the locus of
        the points of intersection of the tangents at <m>$A$</m> and <m>$B$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 121 -->
        If the line <m>$lx + my + na = 0$</m> meets the parabola <m>$y^2 = 4ax$</m> in <m>$P, Q$</m> and if
        the lines joining <m>$P, Q$</m> to the focus meet the parabola in <m>$R, T$</m>, show that the
        equation of <m>$RT$</m> is <m>$nx - my + la = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 122 -->
        Circles are described on any two focal chords of a parabola, prove that their common chord passes
        througy the vertex.
      </d:listitem>
      <d:listitem>
        <!-- 123 -->
        Show that the tangent at one extremity of a focal chord of a parabola is parallel to the normal at
        the other extremity.
      </d:listitem>
      <d:listitem>
        <!-- 124 -->
        The tangents and normals at the ends of a focal chord of a parabola meet in <m>$P$</m> and
        <m>$M$</m> respectively. Show that <m>$PM$</m> is parallel to the axis.
      </d:listitem>
      <d:listitem>
        <!-- 125 -->
        Show that the locus of the middle point of chords of the parabola <m>$y^2 = 4ax$</m> which subtends
        a right angle at the vertex is <m>$y^2 = 2a(x - 4a)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 126 -->
        Show that all chords of a parabola which subtend a right angle at the vertex pass through a fixed
        point on the axis of the curve.
      </d:listitem>
      <d:listitem>
        <!-- 127 -->
        Show that the locus of the middle point of all chords of all chords of the parabola <m>$y^2 =
        4ax$</m> passing thrugh a fixed point <m>$(h, k)$</m> is <m>$y^2 - ky = 2a(x - h)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 128 -->
        Prove that the normal at any point of a parabola bisects the angle between the focal radius and the
        diameter through that point.
      </d:listitem>
      <d:listitem>
        <!-- 129 -->
        A rod of length <m>$2l$</m> slides with its ends on the parabola <m>$y^2 = 4ax$</m>. Prove that the
        mid-point of the rod traces the curve <m>$\left(4ax - y^2\right)\left(y^2 + 4a^2\right) =
        4a^2l^2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 130 -->
        Show that if <m>$r_1$</m> and <m>$r_2$</m> be the lengths of perpendicular chords of a parabola
        drawn through the vertex, then <m>$\left(r_1r_2\right)^\frac{4}{3} = 16a^2\left(r_1^{\frac{2}{3}} +
        r_2^{\frac{2}{3}}\right)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 131 -->
        Show that the locus of the middle point of a variable chord of the parabola <m>$y^2 = 4ax$</m>, such
        that the focal distances of its extremities are in the ratio <m>$2:1$</m> is <m>$9\left(y^2 -
        2ax\right)^2 = 4a^2(2x - a)(4x + a)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 132 -->
        Show that the equation of the circle described on the chord intercepted by the parabola <m>$y^2 =
        4ax$</m> on the line <m>$y = mx + c$</m> as diameter is <m>$m^2\left(x^2 + y^2\right) + 2x(mc - 2a)
        - 4amy + 4amc + c^2 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 133 -->
        If the sum of the tangents of the base angles of a triangle described on a given base be constant,
        show that the locus of its vertex is a parabola.
      </d:listitem>
      <d:listitem>
        <!-- 134 -->
        Prove that the locus of the centers of equilateral triangles inscribed in the parabola <m>$y^2 =
        4ax$</m> is <m>$9y^2 = 4a(x - 8a)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 135 -->
        Find the equation of the ellipse having its center at the point <m>$(2, -3)$</m>, one focus at
        <m>$(3, -3)$</m> annd one vertex at <m>$(4, -3)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 136 -->
        Find the equation of the ellipse whose foci are <m>$(2, 3)$</m>, <m>$(-2, 3)$</m> and whose
        semi-minor axis is <m>$\sqrt{5}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 137 -->
        Find the equation to the ellipse whose one focus is <m>$(-1, 1)$</m>m directrix is <m>$x - y + 3 =
        0$</m> and eccentricity is <m>$\frac{1}{2}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 138 -->
        Find the equation to the ellipse whose one focus is <m>$(2, 1)$</m>, the directrix is <m>$2x - y + 3
        = 0$</m> and the eccentricity is <m>$\frac{1}{\sqrt{2}}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 139 -->
        Find the equation to the ellipse whose center is <m>$(-2, 3)$</m> and whose semi-axes are <m>$3$</m>
        and <m>$2$</m> when the major axis is parallel to the <m>$y$</m>-axis.
      </d:listitem>
      <d:listitem>
        <!-- 140 -->
        The distance between the foci of an ellipse is <m>$10$</m> and its latus rectum is <m>$15$</m>; find
        its equation referred to its axes as axes of coordinates.
      </d:listitem>
      <d:listitem>
        <!-- 141 -->
        Find the equation of ellipse in the standard form whose minor axis is equal to the distance between
        the foci and whose latus rectum is <m>$10$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 142 -->
        The eccentricity of an ellipse is <m>$\frac{1}{2}$</m> and the distance between its foci is
        <m>$4$</m> units. If the major and minor ases of the ellipse are respectively qalonf the <m>$x$</m>
        and <m>$y$</m> axes, find the equation of the ellipse.
      </d:listitem>
      <d:listitem>
        <!-- 143 -->
        Find the equation to the ellipse whose foci are <m>$(4, 0)$</m> and <m>$(-4, 0)$</m> and
        eccentricity is <m>$\frac{1}{3}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 144 -->
        Find the equation of the ellipse, referred to its axes as the <m>$x, y$</m> axes respectively, which
        passes through the point <m>$(-3, 1)$</m> and has the eccentricity <m>$\sqrt{\frac{2}{5}}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 145 -->
        Find the centere, the length of the axes, eccentricity and the foci of the ellipse <m>$12x^2 + 4y^2
        + 24x - 16y + 25 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 146 -->
        Find the equation of the ellipse with its center at <m>$(1, 2)$</m>, one focus at <m>$(6, 2)$</m>
        and passing through the point <m>$(4, 6)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 147 -->
        Show that the follwing equation represents an ellipse and find its center and eccentricity <m>$8x^2
        + 6y^2 - 16x + 12y + 13 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 148 -->
        Find the center, the lengths of the axes and the eccentricity of the ellipse <m>$2x^2 + 3y^2 - 4x -
        12y + 13 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 149 -->
        Find the eccentric angle of a point on the ellipse <m>$x^2 + 3y^2 = 6$</m> at a distance <m>$2$</m>
        units from the center of the ellipse.
      </d:listitem>
      <d:listitem>
        <!-- 150 -->
        Find the eccentricity of an ellipse if its latus rectum is equal to one-half of its major axis.
      </d:listitem>
      <d:listitem>
        <!-- 151 -->
        If the normal at an end of a latus rectum of an ellipse passes through an end of the minor axis,
        show that <m>$e^4 + e^2 = 1$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 152 -->
        Find the condition that the line <m>$lx + my = n$</m> may be a normal to th ellipse
        <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 153 -->
        Prove that in the ellipse <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m> the locus of the middle
        point of the portions of tangents included between the axes is the curve <m>$\frac{a^2}{x^2} +
        \frac{b^2}{y^2} = 4$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 154 -->
        Show that the product of the perpendiculars drawn from the foci upon any tangent to an ellipse is
        constant.
      </d:listitem>
      <d:listitem>
        <!-- 155 -->
        Find the equation of the tangent to the ellipse <m>$x^2 + 2y^2 = 4$</m> at the points where ordinate
        is <m>$1$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 156 -->
        Find the condition that the line <m>$lx + my + n = 0$</m> may touch the ellipse <m>$4x^2 + 25y^2 =
        100$</m> and find the point of contact.
      </d:listitem>
      <d:listitem>
        <!-- 157 -->
        Find the condition that the line <m>$x\cos\alpha + y\sin\alpha = p$</m> may be a tangent to the
        ellipse <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>. Also find the point of contact.
      </d:listitem>
      <d:listitem>
        <!-- 158 -->
        The distance of a point on the ellipse <m>$x^2 + 3y^2 = 6$</m> from the center is <m>$2$</m>. Find
        the eccentric angle of the point in the first quadrant. Also find the equation of the tangent of the
        point.
      </d:listitem>
      <d:listitem>
        <!-- 159 -->
        Find the equation of the tangent of the ellipse <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m> which
        makes equal intercepts on the axes.
      </d:listitem>
      <d:listitem>
        <!-- 160 -->
        Show that the line <m>$y = x + \sqrt{\frac{5}{6}}$</m> touches the ellipse <m>$2x^2 + 3y^2 =
        1$</m>. Find the coordinates of the point of contact.
      </d:listitem>
      <d:listitem>
        <!-- 161 -->
        Find the equation of the tangents to the ellipse <m>$4x^2 + 3y^2 = 5$</m> which are parallel to the
        straight line <m>$y = 3x + 7$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 162 -->
        If a tangent to the ellispe <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m> meets the major and minor
        axes in <m>$P$</m> and <m>$Q$</m> respectively, prove that <m>$\frac{a^2}{CP^2} + \frac{b^2}{CQ^2} =
        1$</m>, where <m>$C$</m> is the center of the ellipse.
      </d:listitem>
      <d:listitem>
        <!-- 163 -->
        Find the locus of the foot of the perpendicular drawn from the center upon any tangent to the ellipse
        <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 164 -->
        If the eccentric angles of the end of a focal chord of the ellipse <m>$\frac{x^2}{a^2} +
        \frac{y^2}{b^2} = 1$</m> be <m>$\theta$</m> and <m>$\psi$</m>, show that
        <m>$\tan\frac{\theta}{2}\tan\frac{\psi}{2} = \frac{e - 1}{e + 1}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 165 -->
        Find the equation of the normal to the ellipse <m>$x^2 + 4y^2 = 9$</m> at the point where the
        eccentric angle is <m>$\frac{\pi}{4}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 166 -->
        Find the equation of normals to the ellipse <m>$x^2 + 4y^2 = 25$</m> at the points where ordinate is
        <m>$2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 167 -->
        Find the condition that the line <m>$lx + my + n = 0$</m> may touch the ellipse <m>$\frac{x^2}{a^2}
        + \frac{y^2}{a^2} = 1$</m> and find the point of contact.
      </d:listitem>
      <d:listitem>
        <!-- 168 -->
        Find the equation of the tangent to the ellipse <m>$x^2 + 4y^2 = 25$</m> at the point whose ordinate
        is <m>$2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 169 -->
        Find the equation of the normal to the ellipse <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m> at the
        ends of latus rectum in the first quadrant.
      </d:listitem>
      <d:listitem>
        <!-- 170 -->
        Find the points wherea the normals to the ellipse <m>$x^2 + 3y^2 = 27$</m> are parallel to the line
        <m>$6x - 5y = 2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 171 -->
        Find the equation that the line <m>$x\cos\alpha + y\sin\alpha = p$</m> be a normal to the ellipse
        <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 172 -->
        Find the equation of the ellipse whose vertices are <m>$(5, 0)$</m> and <m>$(-5, 0)$</m> and foci
        are <m>$(4, 0)$</m> and <m>$(-4, 0)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 173 -->
        Find the equation of the ellipse whose foci are <m>$(3, 0)$</m> and <m>$(-3, 0)$</m> and which
        passes through <m>$(4, 1)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 174 -->
        Find the equation of the ellipse which passes through the points <m>$(-1, 4)$</m> and <m>$(4,
        3)$</m> and having major and minor axes along <m>$x$</m> and <m>$y$</m> axes respectively and center
        at the origin.
      </d:listitem>
      <d:listitem>
        <!-- 175 -->
        If the angle between the lines joining the foci of any ellipse to an extremity of the minor axis is
        <m>$90^\circ$</m>, find the eccentricity. Find also the equation of ellipse if the major axis is
        <m>$2\sqrt{2}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 176 -->
        An archway is in the shape of a semi ellipse; the road level being the major axis. If the breadth of
        the road is <m>$30$</m> ft. and a mam <m>$6$</m> ft. tall just touches the top when <m>$2$</m>
        ft. from the side, find the greatest height of the arch.
      </d:listitem>
      <d:listitem>
        <!-- 177 -->
        Find the locus of the foot of the perpendicular drawn from either focus upon any tangent to the
        ellipse <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 178 -->
        Prove that the product of the perpendiculars from the foci on any tangent to an ellipse to the
        square of the semi-minor axis.
      </d:listitem>
      <d:listitem>
        <!-- 179 -->
        Find the locus of the point of intersection of tangents to ellipse <m>$\frac{x^2}{a^2} +
        \frac{y^2}{b^2} = 1$</m> which makes an angle <m>$\theta$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 180 -->
        Find the locus of the vertices of equilateral triangle circumscribing the ellipse
        <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 181 -->
        The tangent at the point <m>$\alpha$</m> on the ellipse <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} =
        1$</m> meets the auxilliary circle in two points which subtend a right angle at the center. Show
        that the eccentricity of the ellipse is <m>$\frac{1}{\sqrt{1 + \sin^2\alpha}}$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 182 -->
        Find the point of intersection of the tangents at points <m>$\alpha$</m> and <m>$\beta$</m> of the
        ellipse <m>$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 183 -->
        An ellipse of semi axes <m>$a, b$</m> slides between two perpendicular lines, prove that the locus
        of the foci is <m>$\left(x^2 + y^2\right)\left(x^2y^2 + b^4\right) = 4a^2x^2y^2$</m>, the two lines
        being taken as the axes of coordinates.
      </d:listitem>
      <d:listitem>
        <!-- 184 -->
        An ellipse slides between two lines at right angles to one another. Show that the locus of its
        center is a circle.
      </d:listitem>
      <d:listitem>
        <!-- 185 -->
        Prove that the tangent and normal at any point of an ellipse bisect the external and internal angles
        between the focal distances of the point.
      </d:listitem>
    </d:orderedlist>
  </d:sect1>
</d:chapter>
