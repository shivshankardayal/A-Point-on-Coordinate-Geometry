<?xml version="1.0" encoding="utf-8"?>
<d:chapter version="5.0" xmlns:d="http://docbook.org/ns/docbook"
           xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink"
           xmlns:mml="http://www.w3.org/1998/Math/MathML">
  <?dbhtml filename="circles/index.html" ?>
  <d:title>Circles</d:title>
  <d:para>
    Circles are special cases of ellipses(we will study these the Chapter 7). For circles the
    length of major and minor axes of an ellipse are equal. Equivalently, a circle can be
    defined as locus of a point whose distance from a fixed center remains constant.
  </d:para>
  <d:para>
    Consider a point <m>$(h, k)$</m> as the center, the distance called <d:emphasis
    role="italic">radius</d:emphasis> as <m>$a$</m> and the point <m>$(x, y)$</m> then the general equation
    of the locus can be written as
  </d:para>
  <d:figure xml:id="circle-1">
    <d:title>A circle</d:title>
    <d:mediaobject>
      <d:imageobject>
        <d:imagedata fileref="../images/5_1.webp"/>
      </d:imageobject>
    </d:mediaobject>
  </d:figure>
  <m>\[(x - h)^2 + (y - k)^2 = a^2\tag{5.1}\]</m>
  <d:para>
    If we take the origin as the center then this equation reduces to
  </d:para>
  <m>\[x^2 + y^2 = a^2\tag{5.2}\]</m>
  <d:figure xml:id="circle-2">
    <d:title>A circle with Center at Origin</d:title>
    <d:mediaobject>
      <d:imageobject>
        <d:imagedata fileref="../images/5_2.webp"/>
      </d:imageobject>
    </d:mediaobject>
  </d:figure>
  <d:para>
    In the last chapter we found the condition for general equation of second degree to
    represent a pair of straight lines. Here, we will find the condition for it to represent a
    circle.
  </d:para>
  <d:para>
    Recall that general equaiton of second degree in <m>$x$</m> and <m>$y$</m> is given by <m>$ax^2 + 2hxy +
    by^2 + 2gx + 2fy + c = 0$</m> and equation of circle is <m>$(x - \alpha)^2 + (y - \beta)^2 = r^2$</m>.
  </d:para>
  <d:para>
    Comparing the coefficients, we find that, <m>$\frac{1}{a} = \frac{1}{b} = \frac{0}{h} =
    -\frac{\alpha}{g} = -\frac{\beta}{f} = \frac{\alpha^2 + \beta^2 - r^2}{c^2}$</m>, hence, coeff. of
    <m>$x^2$</m> should be equal to coefficient of <m>$y^2$</m> and coeff. of <m>$xy$</m> should be zero.
  </d:para>
  <d:para>
    Thus, general equation in second degree which represents a circle is <m>$x^2 + y^2 + 2gx + 2fy + c =
    0$</m><m>\[(x + g)^2 + (y + f^2) = g^2 + f^2 -c\tag{5.3}\]</m>
  </d:para>
  <d:para>
    which will have center at <m>$(-g, -f)$</m> and radius equal to <m>$\sqrt{g^2 + f^2 - c}$</m>.
  </d:para>
  <d:sect1>
    <d:title>Special Cases</d:title>
    <d:para>
      <d:emphasis role="bold">Case I:</d:emphasis> The circle passes through the origin.
    </d:para>
    <d:figure xml:id="circle-3">
      <d:title>A circle through Origin</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_3.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      The equation of the circle is <m>$(x - \alpha)^2 + (y - \beta)^2 = a^2$</m>. Since it passes through
      the origin, therefore, <m>$(0 - \alpha)^2 + (0 - \beta)^2 = a^2$</m> <m>\[\alpha^2 + \beta^2 =
      a^2\tag{5.4}\]</m>
    </d:para>
    <d:para>
      If we consider general equation then we find that <m>$c = 0$</m>, thus the equation of circle is
      given by <m>\[x^2 + y^2 + 2gx + 2fy = 0\tag{5.5}\]</m>
    </d:para>
    <d:para>
      <d:emphasis role="bold">Case II:</d:emphasis> The circle touches the <m>$x$</m>-axis.
    </d:para>
    <d:figure xml:id="circle-4">
      <d:title>A circle touching x-axis</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_4.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      We see that <m>$CL = |\beta|$</m> so we can write that <m>\[(x - \alpha)^2 + (y - \beta)^2 =
      \beta^2\tag{5.6}\]</m>
    </d:para>
    <d:para>
      In case we write it in form of general equation then <m>$g^2 + f^2 - c = f^2$</m>. Thus, the
      equation of circle becomes
    </d:para>
    <m>\[x^2 + y^2 + 2gx + 2fy + g^2 = 0\tag{5.7}\]</m>
    <d:para>
      <d:emphasis role="bold">Case III: </d:emphasis> The circle touches the <m>$y$</m>-axis.
    </d:para>
    <d:para>
      We see that <m>$CL = |\alpha|$</m> so we can write that <m>\[(x - \alpha)^2  + (y - \beta)^2 =
      \alpha^2\tag{5.8}\]</m>
      In case we write it in form of general equation then <m>$g^2 + f^2 - c = g^2$</m>. Thus, the
      equation of circle becomes <m>\[x^2 + y^2 + 2gx + 2fy + f^2 = 0\tag{5.9}\]</m>
    </d:para>
    <d:figure xml:id="circle-5">
      <d:title>A circle touching y-axis</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_5.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      <d:emphasis role="bold">Case III:</d:emphasis> The circle touches both the axes.
    </d:para>
    <d:para>
      We first consider the case of first quadrant.
    </d:para>
    <d:figure xml:id="circle-6">
      <d:title>A circle touching both the axes</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_6.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      In this case <m>$\alpha = \beta = a$</m>, and hence, the equation becomes <m>\[(x - a)^2 + (y - a)^2 =
      a^2\tag{5.10}\]</m>
    </d:para>
    <d:para>
      Similarly, in 2nd, 3rd and 4th quadrant the equation would be <m>$(x + a)^2 + (y - a)^2 = a^2$</m>,
      <m>$(x + a)^2 + (y + a)^2 = a^2$</m>, and <m>$(x - a)^2 + (y + a)^2 = a^2$</m>.
    </d:para>
  </d:sect1>
  <d:sect1>
    <d:title>Circle on a Diameter</d:title>
    <d:para>
      We will find equation of a circle one of whose diameters has endpoints as <m>$(x_1, y_1)$</m> and
      <m>$(x_2, y_2)$</m>.
    </d:para>
    <d:figure xml:id="circle-7">
      <d:title>A circle about a diameter</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_7.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      Let <m>$A(x_1, y_1)$</m> and <m>$B(x_2, y_2)$</m> be the endpoints of the diameter <m>$Ab$</m> of the
      circle as shown. Also, let <m>$P(\alpha, \beta)$</m> on the circle. We know from geometry that
      <m>$AB$</m> will subtend a right-angle on <m>$P$</m>.
    </d:para>
    <d:para>
      Slope of <m>$AP = \frac{\beta - y_1}{\alpha - x_1}$</m>, and slope of <m>$BP = \frac{\beta -
      y_2}{\alpha - x_2}$</m>
    </d:para>
    <d:para>
      Since these two lines are perpendicular to each other, we have <m>$\frac{\beta - y_1}{\alpha -
      x_1}.\frac{\beta - y_2}{\alpha - x_2} = -1$</m>
    </d:para>
    <d:para>
      <m>$\Rightarrow (\alpha - x_1)(\alpha - x_2) + (\beta - y_1)(\beta - y_2) = 0$</m>
    </d:para>
    <d:para>
      Thus, equation of circle would be <m>\[(x - x_1)(x - x_2) + (y - y_1)(y - y_2) = 0\tag{5.11}\]</m>
    </d:para>
  </d:sect1>
  <d:sect1>
    <d:title>Parametric Form of a Circle</d:title>
    <d:para>
      We have the equation of the circle as <m>$(x - \alpha)^2 + (y - \beta)^2 = a^2$</m> then any point on
      the circle in parametric form can be given by <m>$(\alpha + a\cos\theta, \beta + a\sin\theta)$</m>.
    </d:para>
    <d:para>
      If the center of the circle is at the origin i.e. the equation is <m>$x^2 + y^2 = a^2$</m> then the
      point's coordiantes changes to <m>$(a\cos\theta, b\sin\theta)$</m>. The point is often referred as
      point <d:quote><m>$\theta$</m></d:quote>.
    </d:para>
  </d:sect1>
  <d:sect1>
    <d:title>Position of a Point w.r.t. a Circle</d:title>
    <d:para>
      Consider any point <m>$(x_1, y_1)$</m>. For any circle the point will be inside, outside or on the
      circle as <m>$(x_1 - \alpha)^2 + (y_1 - \beta)^2 > a^2, &lt; a^2$</m> or <m>$= a^2$</m>.
    </d:para>
    <d:para>
      For general second degree equation the condition would be <m>$(x_1 + g)^2 + (y_1 + f)^2 > g^2 + f^2 -
      c, &lt; g^2 + f^2 - c$</m> or <m>$= g^2 + f^2 - c$</m>.
    </d:para>
  </d:sect1>
  <d:sect1>
    <d:title>Intersection of a Line and a Circle</d:title>
    <d:para>
      We know that one of the equations of a straight line is <m>$\frac{x - \alpha}{\cos\theta} = \frac{y -
      \beta}{\sin\theta}$</m>. This equation represents a line passing through a point <m>$P(\alpha,
      \beta)$</m> making an angle of <m>$\theta$</m> with positive direction of <m>$x$</m>-axis. Let this
      ratio be equal to <m>$r$</m>, where <m>$r$</m> is the algebraic distance of the point <m>$(x, y)$</m>
      from <m>$P(\alpha, \beta)$</m>. So the coordiantes on this line are given by <m>$(\alpha +𝑟\cos\theta,
      \beta + 𝑟\sin\theta)$</m>.
    </d:para>
    <d:para>
      If this point lies on the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m>, then <m>$(\alpha +
      r\cos\theta)^2 + (\beta + r\sin\theta)^2 + 2g(\alpha + r\cos\theta) + 2g(\beta + r\sin\theta)  + c =
      0$</m> i.e.
      <m>\[r^2 + 2r(\alpha\cos\theta + \beta\sin\theta + g\cos\theta + f\sin\theta) + \alpha^2 + \beta^2 +
      2g\alpha + 2f\beta + c = 0\tag{5.12}\]</m>
    </d:para>
    <d:para>
      This is a quadratic equaiton in <m>$r$</m>, and hence, the line through <m>$P$</m> meets the circle at
      two points <m>$A$</m> and <m>$N$</m>. Then we see that <m>$PA.PB = \alpha^2 + \beta^2 + 2g\alpha +
      2f\beta + c$</m>, which is independent of <m>$\theta$</m>, i.e. the direction of the line.
    </d:para>
    <d:para>
      This we know from geometry that from a point <m>$P$</m> a secant drawn to cut the circle in
      two points <m>$A$</m> and <m>$B$</m>, the product of the distances <m>$PA$</m> and <m>$PB$</m> is
      constant.
    </d:para>
  </d:sect1>
  <d:sect1>
    <d:title>Tangents and Normals</d:title>
    <d:figure xml:id="arc-1">
      <d:title>Tanngents and normals</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_8.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      Consider two points <m>$P$</m> and <m>$Q$</m> on a curve. The position to which the line <m>$PQ$</m>
      tends as <m>$Q$</m> becomes closer to the point <m>$P$</m> i.e. the limiting position of chord
      <m>$PQ$</m> as <m>$Q$</m> tends to <m>$P$</m> along the curve is called the <d:emphasis
      role="italic">tangent</d:emphasis> to the curve at point <m>$P$</m>. The point <m>$P$</m> is called
      the point of contact of the tangent.
    </d:para>
    <d:para>
      We will find equation of a tangent to a circle at a point <m>$(x_1, y_1)$</m> on the circle <m>$x^2 +
      y^2 = a^2$</m>.
    </d:para>
    <d:figure xml:id="tangent-1">
      <d:title>Tanngents on a circle</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_9.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      Let <m>$Q(x_2, y_2)$</m> be another point on the circle. Then the equation of <m>$PQ$</m> is <m>$y -
      y_1 = \frac{y_2 - y_1}{x_2 - x_1}(x - x_1)$</m>
    </d:para>
    <d:para>
      Since both <m>$P$</m> and <m>$Q$</m> lie on the same circle, therefore, <m>$x_1^2 + y_1^2 = a^2$</m>
      and <m>$x_2^2 + y_2^2 = a^2$</m>
    </d:para>
    <d:para>
      Subtracting gives us <m>$\frac{y_1 - y-2}{x_1 - x_2} = -\frac{x_1 + x_2}{y_1 + y_2}$</m>
    </d:para>
    <d:para>
      Substituting this in the equation of <m>$PQ$</m> gives us <m>$y - y_1 = -\frac{x_1 + x_2}{y_1 + y_2}(x
      - x_1)$</m>
    </d:para>
    <d:para>
      As <m>Q$\rightarrow P, x_2\rightarrow x_1, y_2\rightarrow y_1$</m>, and thus,
    </d:para>
    <d:para>
      <m>$y - y_1 = -\frac{x_1}{y_1}(x - x-1) \Rightarrow xx_1 + yy_1 = x_1^2 + y_1^2$</m>. Thus
    </d:para>
    <m>\[xx_1 + yy_1 = a^2\tag{5.13}\]</m>
    <d:para>
      is the equation of tangent at <m>$P$</m> on the circle.
    </d:para>
    <d:para>
      <d:emphasis role="bold">Aliter:</d:emphasis> We know that the tangent to a circle at a point in
      perpendicular to the radius through that point. The center of our circle is at the origin. Thus, slope
      of the radius is <m>$\frac{y_1}{x_1}$</m>.
    </d:para>
    <d:para>
      So the slope of the tangent will be <m>$-\frac{x_1}{y_1}$</m>. Thus, equation of tangent will be <m>$y
      - y_1 = -\frac{x_1}{y_1}(x - x_1)\Rightarrow xx_1 + yy_1 = a^2$</m>.
    </d:para>
    <d:para>
      <d:emphasis role="bold">Aliter(Using Calculus):</d:emphasis> The equation of our circle is <m>$x^2 +
      y^2 = a^2$</m>.
    </d:para>
    <d:para>
      Differentiating w.r.t. 𝑥 gives us <m>$2x + 2y\frac{dy}{dx} = 0 \Rightarrow \frac{dy}{dx} =
      -\frac{x}{y}$</m>
    </d:para>
    <d:para>
      Thus, slope at point <m>$P(x_1, y_1)$</m> is <m>$\frac{dy}{dx} = -\frac{x_1}{y_1}$</m>. Thus, equation
      of tangent will be <m>$y - y_1 = -\frac{x_1}{y_1}(x - x_1)\Rightarrow xx_1 + yy_1 = a^2$</m>.
    </d:para>
    <d:para>
      Now we will find tanegent to the circle represented by the equation <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m>.
    </d:para>
    <d:para>
      Like the first method we see that both <m>$P(x_1, y_1)$</m> and <m>$Q(x_2, y_2)$</m> lie on the
      circle. Thus, we can write
    </d:para>
    <d:para>
      <m>$x_1^2 + y_1^2 + 2gx_1 + 2fy_1 + c = 0$</m> and <m>$x_2^2 + y_2^2 + 2gx_2 + 2fy_2 + c = 0$</m>
    </d:para>
    <d:para>
      Subtracting <m>$\frac{y_1 - y_2}{x_1 - x_2} = -\frac{x_1 + x_2 + 2g}{y_1 + y_2 + 2f}$</m>
    </d:para>
    <d:para>
      We know that equation of the line <m>$PQ$</m> is <m>$y - y_1 = \frac{y_2 - y_1}{x_2 - x_1}(x -
      x_1)$</m>
    </d:para>
    <d:para>
      Substituting we get <m>$y - y_1 = -\frac{x_1 + x_2 + 2g}{y_1 + y_2 + 2f}(x - x_1)$</m>
    </d:para>
    <d:para>
      As <m>Q$\rightarrow P, x_2\rightarrow x_1, y_2\rightarrow y_1$</m>, and thus,
    </d:para>
    <d:para>
      <m>$y - y_1 = -\frac{x_1 + g}{y_1 + f}(x - x-_1)\Rightarrow xx_1 + yy_1 + gc + fy = x_1^2 + y_1^2 +
      gx_1 + fy_1$</m> <m>\[xx_1 + yy_1 + g(x + x_1) + f(y + y_1) + c = 0\tag{5.1.4}\]</m>
    </d:para>
    <d:para>
      <d:emphasis role="bold">Aliter:</d:emphasis> Center of the circle is <m>$(-g, -f)$</m> and slope of
      radius is <m>$\frac{y_1 - (-f)}{x_1 - (-g)} = \frac{y_1 + f}{x_1 + g}$</m>
    </d:para>
    <d:para>
      So slope of tangent would be <m>$-\frac{x_1 + g}{y_1 + f}$</m>. Thus, equation of tangent is <m>$y -
      y_1 = -\frac{x_1 + g}{y_1 + f}(x - x_1)$</m>
    </d:para>
    <d:para>
      Proceeding like earlier the equation of tangent is found to be <m>$xx_1 + yy_1 + g(x + x_1) + f(y +
      y_1) + c = 0$</m>.
    </d:para>
    <d:para>
      You are encourage to try the third method using calculus for this as well.
    </d:para>
    <d:para>
      Now we will find equation of the normal at <m>$P(x_1, y_1)$</m> for the circle <m>$x^2 + y^2 =
      a^2$</m>. Normal is defined as perpendicular to tangent and it always passes through the center
      of the circle.
    </d:para>
    <d:figure xml:id="normal-1">
      <d:title>Normals of a circle</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_9.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      We have found the equation of tangent as <m>$xx_1 + yy_1 = a^2$</m>, and thus, its slope is
      <m>$-\frac{x_1}{y_1}$</m>, this makes the slope of the normal as <m>$\frac{y_1}{x_1}$</m>.
    </d:para>
    <d:para>
      Since the normal pass through <m>$C(0, 0)$</m>, therefore, its equationis given by <m>$y =
      \frac{y_1}{x_1}x$</m> i.e. <m>\[xy_1 - x_1y = 0\tag{5.15}\]</m>
    </d:para>
    <d:para>
      You are implored to prove this by other techniques as well.
    </d:para>
    <d:para>
      Similarly for the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m>, the equation of the normal at
      <m>$(x_1, y_1)$</m> is found to be <m>\[\frac{x - x_1}{x_1 + g} = \frac{y - y_1}{y_1 +
      f}\tag{5.16}\]</m>
    </d:para>
  </d:sect1>
  <d:sect1>
    <d:title>Condition that a Line Touches a Circle</d:title>
    <d:para>
      We will find the condition for a line <m>$y = mx + c$</m> to touch the circle <m>$x^2 + y^2 =
      a^2$</m>.
    </d:para>
    <d:para>
      Since the line touches the circle, therefore, <m>$x^2 + (mx + c)^2 = a^2 \Rightarrow \left(1 +
      m^2\right)x^2 + 2mcx + \left(c^2 - a^2\right) = 0$</m>.
    </d:para>
    <d:para>
      Since the line touches at one point the above equation will have a repeated root and
      the discriminant must be zero for that. Thus,
    </d:para>
    <d:para>
      <m>$4m^2c^2 - 4\left(1 + m^2\right)\left(c^2 - a^2\right) = 0 \Rightarrow c = \pm a\sqrt{1 +
      m^2}$</m>. Thus, the required condition is <m>\[c = \pm a\sqrt{1 + m^2}\tag{5.17}\]</m>
    </d:para>
    <d:para>
      Thus, general equation of tangent is <m>$y = mx \pm a\sqrt{1 + m^2}$</m>.
    </d:para>
    <d:para>
      The equation of the line is <m>$y = mx + c$</m> and equation of tangent is <m>$xx_1 + yy_1 =
      a^2$</m>. Since both equations represnet tangents at <m>$(x_1, y_1)$</m>, therefore, comparing the
      coefficients <m>$\frac{x_1}{-m} = \frac{y_1}{1} = \frac{a^2}{c} \Rightarrow x_1 = -\frac{a^2m}{c}, y_1
      = \frac{a^2}{c}$</m>
    </d:para>
    <d:para>
      Thus, point of contact is <m>$\left(-\frac{a^2m}{c}, \frac{a^2}{c}\right)$</m>.
    </d:para>
    <d:sect2>
      <d:title>Two Tangents to a Circle</d:title>
      <d:para>
        We will prove that from a point outside the circle two tangents can always be drawn to the circle.
      </d:para>
      <d:para>
        Let the circle be <m>$x^2 + y^2 = a^2$</m>. We know that the equation of tangent is <m>$y = mx +
        a\sqrt{1 + m^2}$</m>
      </d:para>
      <d:para>
        Let the external point be <m>$(x_1, y_1)$</m>. Since the tangent passes through it, therefore,
        <m>$y_1  = mx_1 + a\sqrt{1 + m^2}\Rightarrow (y_1 - mx_1)^2 = a^2\left(1 + m^2\right)$</m>, which is
        a quadratic equation in <m>$m$</m>. Thus, it will give two values of <m>$m$</m>, and hence, two
        different tangents can be drawn from the same point.
      </d:para>
      <d:para>
        If we check the nature of roots then discriminant is given by <m>$x_1^2 + y_1^2 - a^2$</m> i.e. if
        the point is external then two real 𝑚s mean two different tangent.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Length of a Tangent</d:title>
      <d:para>
        We will find the length of a tangent from an external point to a circle.
      </d:para>
      <d:para>
        Let the point be <m>$P(x_1, y_1)$</m> and the circle be <m>$x^2 + y^2 = a^2$</m>. From point
        <m>$P$</m> draw a tangent to the circle, whihc touches the circle at <m>$T$</m>. <m>$CT$</m> will be
        perpendicular to <m>$PT$</m>.
      </d:para>
      <d:figure xml:id="length-tangent">
        <d:title>Length of a tangent to a circle</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/5_10.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Thus, in right-angle <m>$\triangle CPT$</m>, we have <m>$ PT^2 = CP^2 - CT^2 = x_1^2 + y_1^2 -
        a^2$</m>. Thus, <m>\[PT = \sqrt{x_1^2 + y_1^2 - a^2}\tag{5.18}\]</m>
      </d:para>
      <d:para>
        Similarly if the circle is <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> and the center is <m>$(-g,
        -f)$</m> then <m>$PT^2 = (x_1 + g)^2 + (y_1 + f)^2 - \left(g^2 + f^2 - c\right)$</m>
      </d:para>
      <m>\[PT = \sqrt{x_1^2 + y_1^2 + 2gx_1 + 2fy_1 + c}\tag{5.19}\]</m>
    </d:sect2>
    <d:sect2>
      <d:title>Pair of Tangents</d:title>
      <d:para>
        We will find the equation of pair of tangents drawn from an external point to a circle.
      </d:para>
      <d:figure xml:id="pair-tangent">
        <d:title>Pair of a tangent to a circle</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/5_11.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Let the point be <m>$P(x_1, y_1)$</m> from which we draw two tangents <m>$PA$</m> and <m>$PB$</m> to
        the circle, touching the circle at <m>$A$</m> and <m>$B$</m> respectively.
      </d:para>
      <d:para>
        Let <m>$R(\alpha, \beta)$</m> be any point on any one of the tangents, say <m>$PA$</m>. Then the
        locus of <m>$R(\alpha, \beta)$</m> will be the required equation of the pair of tangents to the
        circle from point <m>$(x_1, y_1)$</m>.
      </d:para>
      <d:para>
        Equation of <m>$PR$</m> is <m>$y - y_1 = \frac{\beta - y_1}{\alpha - x_1}(x - x_1)\Rightarrow
        y(\alpha - x_1) - x(\beta - y_1) - \alpha y_1 + \beta x_1 = 0$</m>
      </d:para>
      <d:para>
        Now <m>$AC = a\Rightarrow \frac{|\beta x_1 - \alpha y_1|}{\sqrt{(\alpha - x_1)^2 + (\beta - y_1)^2}}
        = a\Rightarrow \frac{(\beta x_1 - \alpha y_1)^2}{(\alpha - x_1)^2 + (\beta - y_1)^2} = a^2$</m>
      </d:para>
      <d:para>
        So locus of <m>$R(\alpha, \beta)$</m> is <m>$(yx_1 - xy_1)^2 = a^2\left[(x - x_1)^2 + (y - y_1)^2\right]$</m>
      </d:para>
      <m>\[\left(x_1^2 + y_1^2 - a^2\right)\left(x^2 + y^2 - a^2\right) = \left(xx_1 + yy_1 -
      a^2\right)^2\tag{5.20}\]</m>
      <d:para>
        This is the required equation of pair of tangents drawn from <m>$(x_1, y_1)$</m>. If circle is
        denoted by <m>$S$</m> then the pair of tangents is given by <m>$SS_1 = T^2$</m>.
      </d:para>
      <d:para>
        For the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> the pair of tangents is given by
        <m>\[\begin{split}\left(x_1^2 + y_1^2 + 2gx_1 + 2fy_1 + c\right)\left(x^2 + y^2 + 2gx + 2fy + c\right) =\\
        \left[xx_1 + yy_1 + g(x + x_1) + f(y + y_1) + c\right]^2\end{split}\tag{5.21}\]</m>
      </d:para>
    </d:sect2>
  </d:sect1>
  <d:sect1>
    <d:title>Chord of Contact of Tangents</d:title>
    <d:figure xml:id="chord-tangent">
      <d:title>Chord of contact of tangents to a circle</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_12.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      Let <m>$P$</m> be a point outside a circle. From <m>$P$</m> two tangents <m>$PA$</m> and <m>$PB$</m>
      can be drawn to touch the circle at <m>$A$</m> and <m>$B$</m>, respectively just like last
      section. The chord <m>$AB$</m> is called the <d:emphasis role="italic">chord of contact</d:emphasis>
      for point <m>$P$</m>.
    </d:para>
    <d:para>
      We will now find the equation of this chord of contact.
    </d:para>
    <d:para>
      Let <m>$A = (x_2, y_2)$</m> and <m>$B = (x_3, y_3)$</m> as shown in the figure.
    </d:para>
    <d:para>
      Since these points lie on the tangents, therefore, <m>$xx_2 + yy_2 = a^2$</m> and <m>$xx_3 + yy_3 =
      a^2$</m>.
    </d:para>
    <d:para>
      Since both pass through <m>$(x_1, y_1)$</m>, therefore, <m>$x_1x_2 + y_1y_2 = a^2$</m> and <m>$x_1x_3
      + y_1y_3 = a^2$</m>.
    </d:para>
    <d:para>
      Thus, we can say that <m>$xx_1 + yy_1 = a^2$</m> passes through <m>$A$</m> and <m>$B$</m>. Hence, the
      equation of line <m>$AB$</m> is this equation.
    </d:para>
    <d:para>
      Similarly, for the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> the equation of chord of contact is
      given by <m>$xx_1 + yy_1 + g(x + x_1) + f(y + y_1) + c = 0$</m>.
    </d:para>
  </d:sect1>
  <d:sect1>
    <d:title>Poles and Polars</d:title>
    <d:para>
      If from a point <m>$P$</m> any straight line is drawn to meet the circle in <m>$Q$</m> and <m>$R$</m>
      and tangents at <m>$Q$</m> and <m>$R$</m> meet at <m>$T$</m> then locus of <m>$T$</m> is called the
      <d:emphasis role="italic">polar</d:emphasis> of <m>$P$</m> w.r.t to the circle and <m>$P$</m> is
      called the <d:emphasis role="italic">pole</d:emphasis> of its polar i.e. <m>$T$</m>.
    </d:para>
    <d:figure xml:id="pole-polar">
      <d:title>Pole and polar of a circle</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_13.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      The point <m>$P$</m> can be inside or outside the circle. The diagram shows one such <m>$P$</m> inside
      the circle through which the line <m>$QR$</m> passes and tangents meets at <m>$T(\alpha, \beta)$</m>.
    </d:para>
    <d:para>
      Clearly, <m>$QR$</m> is the chord of contact of <m>$T$</m> whose equation is <m>$x\alpha + y\beta =
      a^2$</m>, but it passes through <m>$P(x_1, y_1)$</m>, therefore, <m>$x_1\alpha + y_1\beta = a^2$</m>
    </d:para>
    <d:para>
      Putting <m>$(x, y)$</m> instead of <m>$(\alpha, \beta)$</m> we get the locus of <m>$T$</m> as
      <m>\[xx_1 + yy_1 = a^2\tag{5.22}\]</m>
    </d:para>
    <d:para>
      This is the required equation of the polar of point <m>$P(x_1, y_1)$</m> and as can be seen from
      the equation it is a straight line.
    </d:para>
    <d:para>
      Similarly, for circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> the equation of the polar is given by
      <m>\[xx_1 + yy_1 + g(x + x_1) + f(y + y_1) + c = 0\tag{5.23}\]</m>
    </d:para>
    <d:sect2>
      <d:title>Coordinates of a Pole</d:title>
      <d:para>
        Now we will find coordinates of pole of a line.
      </d:para>
      <d:para>
        Consider a line <m>$lx + my + n = 0$</m> whose pole is to be found w.r.t. the circle <m>$x^2 + y^2 =
        a^2$</m>.
      </d:para>
      <d:para>
        Let the pole be <m>$(x_1, y_1)$</m>. Now the equation of polar of point <m>$(x_1, y_1)$</m>
        w.r.t. to the circle is given by <m>$xx_1 + yy_1 = a^2$</m>.
      </d:para>
      <d:para>
        Comapring coefficients with the given line <m>$\frac{l}{x_1} = \frac{m}{y_1} =
        -\frac{n}{a^2}\Rightarrow x_1 = -\frac{a^2l}{n}, y_1 = -\frac{a^2m}{n}$</m>
      </d:para>
      <d:para>
        Hence, coordinate of the pole is given by <m>$\left(-\frac{a^2l}{n}, -\frac{a^2m}{n}\right)$</m>.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Properties of Poles and Polars</d:title>
      <d:itemizedlist>
        <d:listitem>
          If the polar of a point <m>$P$</m> w.r.t. to a circle passes through <m>$Q$</m> then the polar of
          <m>$Q$</m> w.r.t. the circle will pass through <m>$P$</m>.
          <d:para>
            Let the equation of the circle is <m>$x^2 + y^2 = a^2$</m>. Also, let <m>$P = (x_1, y_1)$</m>
            and <m>$Q = (x_2, y_2)$</m>.
          </d:para>
          <d:para>
            Now equations of the polars of these two points will be <m>$xx_1 + yy_1 = a^2$</m> and <m>$xx_2
            + yy_2 = a^2$</m>.
          </d:para>
          <d:para>
            If the polar of <m>$P$</m> passes through <m>$Q$</m> then <m>$x_1x_2 + y_1y_2 = a^2$</m>.
          </d:para>
          <d:para>
            Thus, we have proven our assumption. The points <m>$P$</m> and <m>$Q$</m> are called <d:emphasis
            role="italic">conjugate points</d:emphasis>.
          </d:para>
        </d:listitem>
        <d:listitem>
          <d:para>
            f the pole of a line w.r.t. a circle lies on another line, then the pole of the other line
            w.r.t. the same circle will lie on the first line.
          </d:para>
          <d:para>
            Let the circle be <m>$x^2 + y^2 = a^2$</m> and the lines be <m>$lx + my + n = 0$</m> and <m>$l'x
            + m'y + n' = 0$</m>.
          </d:para>
          <d:para>
            Let the pole of first line w.r.t to circle be <m>$(x_1, y_1)$</m>. Then the polar will be
            <m>$xx_1 + yy_1 = a^2$</m>. Comapring coefficients we arrive at <m>$(x_1, y_1) =
            \left(-\frac{a^2l}{n}, -\frac{a^2m}{n}\right)$</m>.
          </d:para>
          <d:para>
            This point will lie on the second line, therefore, <m>$-\frac{a^2ll'}{n} - \frac{a^2mm'}{n} + n'
            = 0\Rightarrow a^2ll' + a^2mm' - nn' = 0$</m>.
          </d:para>
          <d:para>
            Similarly we let pole of the second line w.r.t. to the circle be <m>$(x_2, y_2)$</m>. Then the
            polar will be <m>$xx_2 + yy_2 = a^2$</m>. Comapring coefficients we arrive at <m>$(x_2, y_2) =
            \left(-\frac{a^2l'}{n'}, -\frac{a^2m'}{n'}\right)$</m>
          </d:para>
          <d:para>
            This point will lie on the first line, therefore, <m>$a^2ll' + a^2mm' - nn' = 0$</m>.
          </d:para>
          <d:para>
            Thus, the points lies on corresponding lines. Such lines are called <d:emphasis
            role="italic">conjugate lines</d:emphasis>.
          </d:para>
        </d:listitem>
        <d:listitem>
          <d:para>
            If the polars of two point <m>$P$</m> and <m>$Q$</m> w.r.t. a circle meet at <m>$R$</m>, then
            <m>$R$</m> is the pole of the line <m>$PQ$</m>.
          </d:para>
          <d:para>
            Let <m>$P(x_1, y_1)$</m> and <m>$Q(x_2, y_2)$</m> be two points. Then the polar <m>$AB$</m> and
            <m>$CD$</m> of <m>$P$</m> and <m>$Q$</m> w.r.t. to the circle <m>$x^2 + y^2 = a^2$</m> will be
            <m>$xx_1 + yy_1 = a^2$</m> and <m>$xx_2 + yy_2 = a^2$</m>.
          </d:para>
          <d:para>
            According to question they meet at <m>$R$</m>. Solving the two equations, we have
            <m>$\frac{x}{y_1 - y_2} = \frac{y}{x_2 - x_1} = -\frac{a^2}{x_1y_2 - x_2y_1}$</m>, which gives
            <m>$R$</m>.
          </d:para>
          <d:para>
            We have to prove that polar of <m>$R$</m> w.r.t. the circle is line <m>$PQ$</m>. The polar of
            <m>$R$</m> w.r.t. the circle is <m>$x\left(-\frac{a^2(y_1 - y_2)}{x_1y_2 - x_2y_1}\right) +
            y\left(-\frac{a^2(x_1 - x_2)}{x_1y_2 - x_2y_1}\right) = a^2$</m>
          </d:para>
          <d:para>
            <m>$\Rightarrow \frac{y - y_1}{y_1 - y_2} = \frac{x - x_1}{x_1 - x_2}$</m>, which is the line
            <m>$PQ$</m>.
          </d:para>
        </d:listitem>
      </d:itemizedlist>
    </d:sect2>
  </d:sect1>
  <d:sect1>
    <d:title>Equation of a Chord</d:title>
    <d:para>
      We will find equation of a chord whose midpoint is given.
    </d:para>
    <d:para>
      Let the equation of the circle be <m>$x^2 + y^2 = a^2$</m> with center <m>$C(0, 0)$</m>.
    </d:para>
    <d:para>
      Let <m>$PQ$</m> be the chord whose mid-point is <m>$L(x_1, y_1)$</m>. Slope of line <m>$CL =
      -\frac{x_1}{y_1}$</m>
    </d:para>
    <d:para>
      Since <m>$PQ\perp CL$</m>, herefore, slope of <m>$PQ = \frac{y_1}{x_1}$</m>. Equation of <m>$PQ$</m>
      is <m>$y - y_1 = \frac{y_1}{x_1}(x - x_1)$</m>
    </d:para>
    <d:figure xml:id="chord-circle">
      <d:title>Chord of a circle</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_14.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      Thus, equation of the chord is given by <m>\[xx_1 + yy_1 = x_1^2 + y_1^2\tag{5.24}\]</m> Similarly the
      equation of chord for the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> is given by <m>\[xx_1 + yy_1 +
      g(x + x_1) + f(y + y_1) + c = x_1^2 + y_1^2 + 2gx_1 + 2fy_1 + c\tag{5.25}\]</m>
    </d:para>
  </d:sect1>
  <d:sect1>
    <d:para>
      The angle between two circles is the angle between their tangents at their point of intersection. Let
      <m>$P$</m> be a point of intersection of two circles. Let <m>$PT$</m> and <m>$PT'$</m> be the tangents
      to the two circles at the point of intersection. Then the angle is defined at <m>$\angle TPT'$</m> or
      <m>$\pi - \angle TPT'$</m>. If <m>$C_1$</m> and <m>$C_2$</m> be the centers of the two circles then
    </d:para>
    <d:para>
      <m>$PC_1\perp PT$</m> and <m>$PC_2\perp PT' \therefore \angle TPT' = \angle C_1PC_2$</m> or <m>$\pi -
      C_1PC_2$</m>.
    </d:para>
    <d:title>Intersection of Circles</d:title>
    <d:figure xml:id="intersection-circle">
      <d:title>Intersecrtion of circles</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_15.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:sect2>
      <d:title>Orthogonal Circles</d:title>
      <d:para>
        Two circles are said to intersect orthogonally if they intersect at right angles. We will find the
        condition for two circles <m>$x^2 + y^2 + 2g_1x + 2f_1y + c_1 = 0$</m> and <m>$x^2 + y^2 + 2g_2x +
        2f_2y + c_2 = 0$</m> are orthogonal.
      </d:para>
      <d:para>
        Let <m>$A$</m> and <m>$B$</m> be the center of these circles and <m>$r_1$</m> and <m>$r_2$</m> be
        their radii respectively. Then
      </d:para>
      <d:figure xml:id="orthogonal-circle">
        <d:title>Orthogonal circles</d:title>
        <d:mediaobject>
          <d:imageobject>
            <d:imagedata fileref="../images/5_16.webp"/>
          </d:imageobject>
        </d:mediaobject>
      </d:figure>
      <d:para>
        Since the circles are orthogonal <m>$PC_1^2 + PC_2^2 = C_1C_2^2 \Rightarrow r_1^2 + r_2^2 = C_1C_2^2$</m>
      </d:para>
      <d:para>
        <m>$\Rightarrow g_1^2 + f_1^2 - c_1 + g_2^2 + f_2^2 - c_2 = (g_2 - g_1)^2 + (f_2 - f_1)^2$</m>
        <m>\[2g_1g_2 + 2f_1f_2 = c_1 + c_2\tag{5.26}\]</m>
      </d:para>
    </d:sect2>
  </d:sect1>
  <d:sect1>
    <d:title>Radical Axis</d:title>
    <d:figure xml:id="radical-axis">
      <d:title>Radical axis of two circles</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_17.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      The radical axis of two circles is the locus of a point which moves so that the length
      of the tangents drawn from it to the two points are equal.
    </d:para>
    <d:para>
      We will find equation of radical axis of two circles <m>$x^2 + y^2 + 2g_1x + 2f_1y + c_1 = 0$</m>
      and <m>$x^2 + y^2 + 2g_2x + 2f_2y + c_2 = 0$</m>.
    </d:para>
    <d:para>
      Let <m>$P(\alpha, \beta)$</m> be any point from which the tangents from it to these circles are equal.
      Let the tangents from this point to the two circles be <m>$PA$</m> and <m>$PB$</m>.
    </d:para>
    <d:para>
      Now <m>$PA = \sqrt{\alpha^2 + \beta^2 + 2g_1\alpha + 2f_1\beta + c_1}$</m> and <m>$PB
      =\sqrt{\alpha^2 + \beta^2 + 2g_2\alpha + 2f_2\beta + c_2} $</m>
    </d:para>
    <d:figure xml:id="radical-axis-2">
      <d:title>Radical axis of two circles</d:title>
      <d:mediaobject>
        <d:imageobject>
          <d:imagedata fileref="../images/5_18.webp"/>
        </d:imageobject>
      </d:mediaobject>
    </d:figure>
    <d:para>
      We have <m>$PA = PB \Rightarrow 2(g_1 - g_2)\alpha + 2(f_1 - f_2)\beta + c_1 - c_2 = 0$</m>
    </d:para>
    <d:para>
      Hence, locus of <m>$P$</m> is  <m>\[2(g_1 - g_2)x + 2(f_1 - f_2)y + c_1 - c_2 = 0\tag{5.27}\]</m>
      which is a straight line and equation of radical axis.
    </d:para>
    <d:sect2>
      <d:title>Properties of Radical Axes</d:title>
      <d:itemizedlist>
        <d:listitem>
          The radical axis of two circles is perpendicular to the line of centers.
          <d:para>
            Let the two circles be <m>$x^2 + y^2 + 2g_1x + 2f_1y + c_1 = 0$</m> and <m>$x^2 + y^2 + 2g_2x +
            2f_2y + c_2 = 0$</m>.
          </d:para>
          <d:para>
            Let <m>$A$</m> and <m>$B$</m> be the centers of these two circles. Then slope of <m>$AB$</m> is
            <m>$\frac{f_2 - f_1}{g_2 - g_1}$</m>, which is negative reciprocal of <m>$2(g_1 - g_2)x + 2(f_1
            - f_2)y + c_1 - c_2 = 0$</m>.
          </d:para>
          <d:para>
            Thus, the radical axis of two circles is perpendicular to the line of centers.
          </d:para>
        </d:listitem>
        <d:listitem>
          The radical axis of three circles takes two at a time are concurrent.
          <d:para>
            Consider three circles, which are <m>$x^2 + y^2 + 2g_1x + 2f_1y + c_1 = 0, x^2 + y^2 + 2g_2x +
            2f_2y + c_2 = 0$</m> and <m>$x^2 + y^2 + 2g_3x + 2f_3y + c_3 = 0$</m>.
          </d:para>
          <d:para>
            So radical axes will be <m>$2(g_1 - g_2)x + 2(f_1 - f_2)y + c_1 - c_2 = 0, 2(g_2 - g_3)x + 2(f_2
            - f_3)y + c_2 - c_3 = 0$</m> and <m>$2(g_3 - g_1)x + 2(f_3 - f_1)y + c_3 - c_1 = 0$</m>.
          </d:para>
          <d:para>
            Adding these we get equality, hence, the three lines are concurrent. The point where
            these radical axes meet is called the <d:emphasis role="italic">radical center</d:emphasis> of
            the three circles.
          </d:para>
        </d:listitem>
        <d:listitem>
          The radical axis of the two circles bisect their common tangent.
          <d:para>
            Let <m>$AB$</m> be one of the common tangents meeting the raidcal axis at <m>$P$</m>, then since
            <m>$P$</m> lies on the radical axis, hence, by definition of radical axis <m>$PA = PB$</m>.
          </d:para>
          <d:para>
            Thus, the radical axis bisect the common tangent.
          </d:para>
        </d:listitem>
        <d:listitem>
          he locus of the cetner of a circle cutting two given circles orthogonally is the radical
          axis of the two circles.
          <d:para>
            Consider three circles, which are <m>$x^2 + y^2 + 2g_1x + 2f_1y + c_1 = 0, x^2 + y^2 + 2g_2x +
            2f_2y + c_2 = 0$</m> and <m>$x^2 + y^2 + 2g_3x + 2f_3y + c_3 = 0$</m>. Let them cut each other
            orthogonally.
          </d:para>
          <d:para>
            <m>$2g_1g_2 + 2f_1f_2 = c_1 + c_2$</m> and <m>$2g_1g_3 + 2f_1f_3 = c_1 + c_3$</m>
          </d:para>
          <d:para>
            Subtracting we get <m>$2g_1(g_2 - g_3) + 2f_1(f_2 - f_3) = c_2 - c_3$</m>
          </d:para>
          <d:para>
            Thus, locus of the center is <m>$2(g_1 - g_2)x + 2(f_1 - f_2)y + c_1 - c_2 = 0$</m> which is the
            radical axis of the two circles being cut orthogonally.
          </d:para>
        </d:listitem>
      </d:itemizedlist>
    </d:sect2>
  </d:sect1>
  <d:sect1>
    <d:title>Coaxial Circles</d:title>
    <d:para>
      A system of circles is said to be coaxial if each pair of circles of the system has the same radical
      axis.
    </d:para>
    <d:para>
      Consider two circles <m>$x^2 + y^2 + 2g_1x + 2f_1y + c_1 = 0$</m> and <m>$x^2 + y^2 + 2g_2x + 2f_2y +
      c_2 = 0$</m>.
    </d:para>
    <d:para>
      Equation of radical axis will be <m>$2(f_1 - f_2)x + 2(f_1 - f_2)y+ c_1 - c_2 = 0$</m>.
    </d:para>
    <d:para>
      We know that the radical axis of two circles is perpendicular to the line joining their
      centers, therefore, if we take the line joining the centers as <m>$x$</m>-axis and radical axis as
      <m>$y$</m>-axis, then
    </d:para>
    <d:para>
      <m>$f_1 = f_2 = 0$</m> and equation of radical axis will be <m>$c_1 - c_2 = 0 \Rightarrow  c_1 =
      c_2$</m>.
    </d:para>
    <d:para>
      Thus, constant term in all the circles must be same for them to be coaxial circles.
    </d:para>
    <d:sect2>
      <d:title>Limiting Points of a Coaxial System</d:title>
      <d:para>
        Consider a system of coaxial circles having equation <m>$x^2 + y^2 + 2gx + c = 0$</m>, where
        <m>$c$</m> is a constant and <m>$g$</m> is a parameter.
      </d:para>
      <d:para>
        The radius is <m>$\sqrt{g^2 - c}$</m>. If <m>$g = \pm\sqrt{x}$</m> then the radius vanishes and the
        circle becomes point circle.
      </d:para>
      <d:para>
        Thus, there are two circles of the system whose radii are zero, centers of these circles are
        <m>$(\sqrt{c}, 0)$</m> and <m>$(-\sqrt{c}, 0)$</m>.
      </d:para>
      <d:para>
        These points are called <d:emphasis role="italic">limiting points</d:emphasis> of the system.
      </d:para>
    </d:sect2>
    <d:sect2>
      <d:title>Circles through the Points of Intersection of a Circle and a Line</d:title>
      <d:para>
        Consider a circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> and a line <m>$lx + my + n = 0$</m>.
      </d:para>
      <d:para>
        Consider the equation <m>$x^2 + y^2 + 2gx + 2fy + c + \lambda(lx + my + n) = 0$</m> which is also
        the equation of a circle.
      </d:para>
      <d:para>
        The coordinates which satisfy the considered circle and line also satisfy this equation
        of circles. Thus, point of intersection of the considered circle and line lies on this new
        circle.
      </d:para>
      <d:para>
        Similarly we can say that <m>$x^2 + y^2 + 2gx + 2fy + c + \lambda(x^2 + y^2 + 2g_1x + 2f_1y + c) =
        0$</m> is the locus of points of intersection of two circles.
      </d:para>
    </d:sect2>
  </d:sect1>
  <d:sect1>
    <d:title>Problems</d:title>
    <d:orderedlist>
      <d:listitem>
        <!-- 1 -->
        Find the center and the radius of the circle <m>$3x^2 + 3y^2 - 8x - 10y + 3 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 2 -->
        Prove that the radii of the circles <m>$x^2 + y^2 = 1, x^2 + y^2 - 2x - 6y = 6$</m> and <m>$x^2 +
        y^2 - 4x - 12y = 9$</m> are in A.P.
      </d:listitem>
      <d:listitem>
        <!-- 3 -->
        Find the area of an equilateral triangle inscribed in the circle <m>$x^2 + y^2 + 2gx + 2fy + c =
        0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 4 -->
        Find the center and the radius of the circle <m>$x^2 + y^2 + 6x - 8y = 24$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 5 -->
        Find the center and the radius of the circle <m>$5x^2 + 5y^2 + 4x - 8y = 16$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 6 -->
        Find the center and the radius of the circle <m>$x^2 + y^2 - 6x - 2y = 6$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 7 -->
        What will be the radius and center of the circle <m>$\frac{1}{2}\left(x^2 + y^2\right) + x\cos\theta
        + y\sin\theta - 4 = 0$</m>?
      </d:listitem>
      <d:listitem>
        <!-- 8 -->
        Prove that the centers of the circles <m>$x^2 + y^2 = 1, x^2 + y^2 + 6x - 2y - 1 = 0$</m> and
        <m>$x^2 + y^2 - 12x + 4y = 1$</m> are collinear.
      </d:listitem>
      <d:listitem>
        <!-- 9 -->
        Prove that the radii of the circles <m>$x^2 + y^2 = 1, x^2 + y^2 - 2x - 6y = 6$</m> and <m>$x^2 +
        y^2 - 4x - 12y = 9$</m> are in A.P.
      </d:listitem>
      <d:listitem>
        <!-- 10 -->
        Prove that the radii of the circles <m>$x^2 + y^2 = 4, 4x^2 + 4y^2 - 8x - 24y + 15 = 0$</m> and
        <m>$x^2 + y^2 - 4y - 5 = 0$</m> are in A.P.
      </d:listitem>
      <d:listitem>
        <!-- 11 -->
        Prove that the circles <m>$x^2 + y^2 - 9 = 4r^2, r = 1, 2, 3$</m> cut off equal intercepts between
        the circles on the line <m>$3x + 4y + 15 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 12 -->
        Find the equation of the circle whose center is <m>$(1, 2)$</m> and which passes through the point
        <m>$(4, 6)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 13 -->
        If the equations of two diameters of a circle are <m>$x + y = 6$</m> and <m>$x + 2y = 4$</m> and the
        radius of the circle is <m>$10$</m>, find the equation of the circle.
      </d:listitem>
      <d:listitem>
        <!-- 14 -->
        Find the equation of the circle whose center is <m>$(3, 4)$</m> and which touches the line
        <m>$5x + 12y = 1$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 15 -->
        Find the equation of a circle which passes through the point <m>$(2, 0)$</m> and whose center is the
        limit of the point of intersection of the lines <m>$3𝑥 + 5𝑦 = 1$</m> and <m>$(2 + c)x + 5c^2y =
        1$</m> as <m>$c$</m> tends to <m>$1$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 16 -->
        A circle has radius of <m>$3$</m> units and its center lies on the line <m>$y = x - 1$</m>. Find the
        equation of the circle if it passes through <m>$(7, 3)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 17 -->
        Find the equation of the circle, which touches the axes and whose center lies on the line <m>$x - 2y
        = 3$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 18 -->
        A circle of radius <m>$2$</m> lies in the first quadrant and touches both the axes of
        coordinates. Find the equation of the circle with center at <m>$(6, 5)$</m> and touching the above
        circle externally.
      </d:listitem>
      <d:listitem>
        <!-- 19 -->
        Find the equation of the circles whose center is <m>$(3, -1)$</m> and which cut off an intercept of
        length 6 from <m>$2x - 5y + 18 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 20 -->
        Find equations of the circles touching <m>$y$</m>-axis at <m>$(0, 3)$</m> and making intercept of
        <m>$8$</m> units on the <m>$x$</m>-axis.
      </d:listitem>
      <d:listitem>
        <!-- 21 -->
        Find the equation of the circle having the pair of lines <m>$x^2 + 2xy + 3x + 6y = 0$</m> as its
        normals and having the size just sufficient to contain the circle <m>$x(x - 4) + yuy(y - 3) =
        0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 22 -->
        A circle of raidus <m>$5$</m> units touches the coordinate axes in the first quadrant. If the circle
        makes one complete roll on the <m>$x$</m>-axis along the positive direction of <m>$x$</m>-axis, find
        its equation in the new position.
      </d:listitem>
      <d:listitem>
        <!-- 23 -->
        The circle <m>$x^2 + y^2 - 4x - 8y + 16 = 0$</m> rolls up the tangent to it at <m>$\left(2 +
        \sqrt{3}, 3\right)$</m> by <m>$2$</m> units, assuming the <m>$x$</m>-axis as horizontal, find the
        equation of the circle in the new position.
      </d:listitem>
      <d:listitem>
        <!-- 24 -->
        Find the equations of the circles touching the lines <m>$y = 0$</m> and <m>$y = \sqrt{3}(𝑥 + 1)$</m>
        and having the centers at a distance <m>$1$</m> from the origin.
      </d:listitem>
      <d:listitem>
        <!-- 25 -->
        Find the equation of the circle passing through the point <m>$(2, 8)$</m> touching the lines <m>$4x
        - 3y + 24 = 0$</m> and <m>$4x + 3y - 42 = 0$</m> and having <m>$x$</m> coordinate of the center of
        the circle numerically less than or equal to <m>$8$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 26 -->
        Find the equation of the circle whose center is <m>$(1, -5)$</m> and radius is <m>$7$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 27 -->
        Find the equation of the circle whose center is <m>$(-1, -2)$</m> and diameter is <m>$25$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 28 -->
        If the equation of two diameters of a circle are <m>$2x + y = 6$</m> and <m>$3x + 2y = 4$</m> and
        the radius is <m>$10$</m>, find the equation of the circle.
      </d:listitem>
      <d:listitem>
        <!-- 29 -->
        Find the equation of the circle which passes through the point of intersection of <m>$3x - 2y - 1 =
        0$</m> and <m>$4x + y - 27 = 0$</m> and whose center is <m>$(2, 3)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 30 -->
        Find the equation of the circle whose center is <m>$(1, 2)$</m> and which passes through the point
        of intersection of <m>$3x + y = 14$</m> and <m>$2x + 5y = 18$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 31 -->
        ind the equation of the circle passing through the center of the circle <m>$x^2 + y^2 - 4x - 6y =
        8$</m> and being concentric with the circle <m>$x^2 + y^2 - 2x - 8y = 5$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 32 -->
        Find the equation of the circle passing through the point of intersection of <m>$x + 3y = 0$</m> and
        <m>$2x - 7y = 0$</m> and whose center is the point of intersection of lines <m>$x + y + 1 = 0$</m>
        and <m>$x - 2y + 4 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 33 -->
        Find the equation of the circle whose radius is <m>$5$</m> and the center lies on the positive side
        of <m>$x$</m>-axis at a distance <m>$5$</m> from the origin.
      </d:listitem>
      <d:listitem>
        <!-- 34 -->
        Find the equation of the circle which passes through the points <m>$(-1, 2)$</m> and <m>$(3,
        -2)$</m> and whose center lies on the line <m>$x - 2y = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 35 -->
        Find the equation of the circle which passes through the point <m>$(1, -2)$</m> and <m>$(4, -3)$</m>
        and whose center lies on the line <m>$3x + 4y = 7$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 36 -->
        Find the equation of the circle whose radius is <m>$5$</m> and which touches the circle
        <m>$x^2 + y^2 - 2x - 4y - 20 = 0$</m> at the point <m>$(5, 5)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 37 -->
        Find the equation of the circle whose center is <m>$(1, -3)$</m> and which touches the line
        <m>$2x - y - 4 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 38 -->
        Write down the equation of a circle concentric with the circle <m>$x^2 + y^2 - 4x + 6y - 17 = 0$</m>
        and tangent to the line <m>$3x - 4y + 7 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 39 -->
        Find the equation of the circle of radius <m>$5$</m> and touching the line <m>$3x - 4y + 5 = 0$</m>
        at <m>$(1, 2)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 40 -->
        If the raidus of the circle is 5 and the equations of the twp normals to the circle are <m>$3x - 5y
        + 2 = 0$</m> and <m>$x + 2y = 3$</m>, find the equation of the circle.
      </d:listitem>
      <d:listitem>
        <!-- 41 -->
        Find the equation of the circle which touches the <m>$y$</m>-axis at a distance <m>$4$</m> from the
        origin towards the positive side of <m>$y$</m>-axis and cuts of an intercept <m>$6$</m> on the
        <m>$x$</m>-axis.
      </d:listitem>
      <d:listitem>
        <!-- 42 -->
        Find the equation of the circle which touches both the axes and whose radius is <m>$a$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 43 -->
        Find the equation of the circle passing through the point <m>$(h, k)$</m> and touching the
        <m>$y$</m>-axis at origin.
      </d:listitem>
      <d:listitem>
        <!-- 44 -->
        Find the equation of the circle touching the axis of <m>$x$</m> at the origin and touching
        the line <m>$3y = 4x + 24$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 45 -->
        What is the parametric equation of the circle <m>$x^2 + y^2 = 16$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 46 -->
        Find the equation of the circle which touches the line <m>$2x - y = 1$</m> at <m>$(1, 1)$</m> and
        the line <m>$2x + y = 4$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 47 -->
        Find the euqation of the circle touching the lines <m>$4x - 3y = 30$</m> and <m>$4x - 3y + 10 =
        0$</m> having the center on the line <m>$2x + y = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 48 -->
        A circle of raidus <m>$4$</m> units touches the coordinate axes in the first quadrant. Find the
        equation of its image w.r.t. the line mirror <m>$y = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 49 -->
        The equation of a circle is <m>$x^2 + y^2 + 16x - 24y + 183 = 0$</m>. Find the equation of
        the image of this circle by the line mirror <m>$4x + 7y + 13 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 50 -->
        The circle <m>$(x - a)^2 + (y - a)2 = a^2$</m> is rolled on the 𝑥-axis in the positive direction
        through one complete revolution. Find the equation of the circle in the new position.
      </d:listitem>
      <d:listitem>
        <!-- 51 -->
        The center of a circle is <m>$(1, 1)$</m> and its radius is <m>$5$</m> units. If the center is
        shifted on the line <m>$x - y = 0$</m> through a distance of <m>$\sqrt{2}$</m> units, find the
        equation of the circle in the new position. How many such circles are possible?
      </d:listitem>
      <d:listitem>
        <!-- 52 -->
        Find the equation of the circles which pass through the origin and cut off equal chords of
        <m>$\sqrt{2}$</m> units from the straight lines <m>$y = x$</m> and <m>$y = -x$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 53 -->
        Find the center of the circle which is inscribed in the triangle formed by the lines
        <m>$3x + 4y - 15 = 0, 3x - 4y - 7 = 0$</m> and <m>$12x + 5y - 115 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 54 -->
        Show that the equation of the circle which touches the coordinate axes and whose center lies on the
        straight line <m>$lx + my + n = 0$</m> is <m>$(l \pm m)^2\left(x2 + y^2\right) \pm 2n(l \pm m)(x
        + y) + n^2 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 55 -->
        Of the two concentric circles the smaller one has the equation <m>$x^2 + y^2 = 4$</m>. If the
        intercept on the line <m>$x + y = 2$</m> made between the two circles is <m>$1$</m>, find the
        equation of the larger circle.
      </d:listitem>
      <d:listitem>
        <!-- 56 -->
        Is <m>$(3, 2)$</m> an interior point or an exterior point of the circle <m>$x^2 + y^2 - 2x + y =
        0$</m>? If inerior, find the equation of the circle centered on it and of maximum area contained in
        the given in the circle. If exterior, find the circle of maximum radius centered on it containing
        the given circle.
      </d:listitem>
      <d:listitem>
        <!-- 57 -->
        Find the equation of the circle with minimum raidus which contains the three circles <m>$x^2 + y^2 -
        4y - 5 = 0, x^2 + y^2 + 12x + 4y + 31 = 0$</m> and <m>$x^2 + y^2 + 6x + 12y + 36 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 58 -->
        Find the equation of the circle whose diameter is the line joining the points <m>$(-4, 3)$</m> and
        <m>$(12, -1)$</m>. Find also the intercept made by it on <m>$y$</m>-axis.
      </d:listitem>
      <d:listitem>
        <!-- 59 -->
        The sides of a square are <m>$x = 1, x = 3, y = 2$</m> and <m>$y = 4$</m>. Find the equation of the
        circle drawn on the diagonals of the square as its diameter.
      </d:listitem>
      <d:listitem>
        <!-- 60 -->
        Find the equation of the circle on the line joining the origin and <m>$(2, -4)$</m> as diameter.
      </d:listitem>
      <d:listitem>
        <!-- 61 -->
        Find the equation of the circle, the endpoints of whose diameter are <m>$(2, -3)$</m> and <m>$(-2,
        4)$</m>. Find its center and radius.
      </d:listitem>
      <d:listitem>
        <!-- 62 -->
        Find the equation of the circle drawn on the intercept between the axes made by the line <m>$3x + 4y
        = 12$</m> as a diameter.
      </d:listitem>
      <d:listitem>
        <!-- 63 -->
        Find the equation of the circle the endpoints of whose diameter are the centers of the circles
        <m>$x^2 + y^2 + 6x - 14y = 1$</m> and <m>$x^2 + y^2 - 4x + 1 - y = 2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 64 -->
        The sides of a square are <m>$x = 6, x = 9, y = 3$</m> and <m>$y = 6$</m>. Find the equation of a
        circle drawn on the diagonal of the square as its diameter.
      </d:listitem>
      <d:listitem>
        <!-- 65 -->
        Find the equation of the circle circumscribing the rectangle whose sides are <m>$x - 3y = 4, 3x + y
        = 22, x - 3y = 14$</m> and <m>$3x + y = 62$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 66 -->
        The abscissa if two points <m>$A$</m> and <m>$B$</m> are the roots of the equation <m>$x^22 + 2x -
        a^2 = 0$</m> and the oridinates are the roots of the equation <m>$y^2 + 4y - b^2 = 0$</m>. Find the
        equation of the circle with <m>$AB$</m> as its diameter. Also find the coordinates of the center and
        the length of the raidus of the circle.
      </d:listitem>
      <d:listitem>
        <!-- 67 -->
        If <m>$(4, 1)$</m> be one endpoint of the diameter of the circle <m>$x^22 + y^2 - 2x + 6y - 15 =
        0$</m>, find the coordinates of the other endpoint of the diagonal.
      </d:listitem>
      <d:listitem>
        <!-- 68 -->
        ind the equation of the circumcircle of the quadrilateral formed by the four lines
        <m>$ax + by \pm c = 0$</m> and <m>$bx - ay \pm c = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 69 -->
        Find the equation of the circle which passes through the points <m>$(1, -2)$</m> and <m>$(4, -3)$</m>
        and whose center lies on the line <m>$3x + 4y = 7$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 70 -->
        Find the equation of the circle passing through the origin and the points where the straight line
        <m>$3x + 4y = 12$</m> meets the coordinate axes.
      </d:listitem>
      <d:listitem>
        <!-- 71 -->
        Show that the cyclic quadrilateral is formed by the lines <m>$5x + 3y = 9, x = 3y, 2x = y$</m> and
        <m>$x + 4y + 2 = 0$</m> taken in order. Find the equation of the circle.
      </d:listitem>
      <d:listitem>
        <!-- 72 -->
        Find the equation of a circle passing through the points <m>$(1, 2)$</m> and <m>$(3, 4)$</m> and
        touching the line <m>$3x + y - 3 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 73 -->
        A circle touches both the <m>$x$</m>-axis and the line <m>$4x - 3y + 4 = 0$</m>. Its center is in the
        third quadrant anf lies on the line <m>$x - y - 1 = 0$</m>. Find the equation of the circle.
      </d:listitem>
      <d:listitem>
        <!-- 74 -->
        Find the equation of the circle passing through the points <m>$(1, 0), (0, 1)$</m> and <m>$(1, -2)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 75 -->
        Find the equation of the circle passing through the points <m>$(0, 0), (a, 0)$</m> and <m>$(0, b)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 76 -->
        Find the equation of the circle which passes through the origin and cuts off chords of length
        <m>$4$</m> and <m>$6$</m> on the positive side of <m>$x$</m>-axis and <m>$y$</m>-axis respectively.
      </d:listitem>
      <d:listitem>
        <!-- 77 -->
        Find the equation of the circumcircle of the triangle formed by the lines <m>$y = x, y = 2x$</m> and
        <m>$y = 3x + 2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 78 -->
        Find the incenter of the triangle whose sides are <m>$7x - y + 11 = 0, x + y - 15 = 0$</m> and
        <m>$7x + 17y + 65 = 0$</m>. Find the equation of the incircle.
      </d:listitem>
      <d:listitem>
        <!-- 79 -->
        Find the equation of the circles passing through the origin and cutting off equal intercepts
        <m>$1$</m> from the lines <m>$3x = 4y$</m> and <m>$4x = 3y$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 80 -->
        Find the equation of the circle described on the common chord of the circles <m>$x^2 + y^2 - 4x - 5 =
        0$</m> and <m>$x^2 + y^2 + 8y + 7 = 0$</m> as diameter.
      </d:listitem>
      <d:listitem>
        <!-- 81 -->
        Show that the circle on the chord <m>$x\cos\alpha + y\sin\alpha - p = 0$</m> of the circle <m>$x^2 +
        y^2 = a^2$</m> as diameter is <m>$x^2 + y^2 - a^2 - 2p(x\cos\alpha + y\sin\alpha - p) = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 82 -->
        Prove that the equation <m>$x^2 + y^2 - 4 + k\left(y - mx - 2\sqrt{1 + m^2}\right) = 0$</m>
        represents a family of circles touching each other at a common point for all <m>$k$</m>, where
        <m>$m$</m> is a given constant.
      </d:listitem>
      <d:listitem>
        <!-- 83 -->
        Show that the general equation of a circle which passes through the points <m>$(x_1, y_1)$</m> and
        <m>$(x_2, y_2)$</m> may be written as <m>$(x - x_1)(x - x_2) + (y - y_1)(y - y_2) +
        \lambda\begin{vmatrix}x &amp; y &amp; 1\\x_1 &amp; y_1 &amp; 1\\x_2 &amp; y_2 &amp; 1\end{vmatrix} =
        0$</m>, and hence, deduce the diameter form of the equation of a circle.
      </d:listitem>
      <d:listitem>
        <!-- 84 -->
        The line <m>$Ax + By + C = 0$</m> cuts the circle <m>$x^2 + y^2 + ax + bby + c = 0$</m> in
        <m>$P$</m> and <m>$Q$</m>. The line <m>$A'x + B'y + C' = 0$</m> cuts the circle <m>$x^22 + y^2 + a'x
        + b'y + c' = 0$</m> in <m>$R$</m> and <m>$S$</m>. If <m>$P, Q, R, S$</m> are concyclic points show
        that <m>$\begin{vmatrix}a - a' &amp; b - b' &amp; c - c'\\A &amp; B &amp; C\\A' &amp; B' &amp;
        C'\end{vmatrix} = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 85 -->
        A fixed circle is cut by circles passing through two given points <m>$A(x_1, y_1)$</m> and
        <m>$B(x_2, y_2)$</m>. Show that the chord of intersection of the fixed circle with any one of
        the circled, passes through a fixed point.
      </d:listitem>
      <d:listitem>
        <!-- 86 -->
        Tangents <m>$PQ$</m> and <m>$PR$</m> are drawn to the circle <m>$x^2 + y^2 = a^2$</m> from the point
        <m>$P(x_1, y_1)$</m>. Find the equation of the circumcircle of <m>$\triangle PQR$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 87 -->
        Find the equation of the circle passing through the point of intersection of the circles <m>$x^2 +
        y^2 - 6x + 2y + 4 = 0$</m> and <m>$x2 + y^2 + 2x - 4y - 6 = 0$</m> and with its center on the line
        <m>$y = x$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 88 -->
        If the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> bisects the circumference of the circle
        <m>$x^2 + y^2 + 2g'x + 2f'y + c' = 0$</m>, prove that <m>$2g'(g - g') + 2f'(f - f') = c - c'$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 89 -->
        Find the equation of the circle of radius 4 and passing through the point of intersection of circles
        <m>$x^2 + y^2 - 2x - 4y - 4 = 0$</m> and <m>$x^2 + y^2 - 10x - 12y + 40 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 90 -->
        Show that the common chord of the circles <m>$x^2 + y^2 - 6x - 4y + 9 = 0$</m> and <m>$x^2 + y^2 -
        8x - 6y + 23 = 0$</m> pass through the center of the second circle and find its length.
      </d:listitem>
      <d:listitem>
        <!-- 91 -->
        Find the equation of the circle whose diameter is the common chord of the circles <m>$x^2 + y^2 + 2x
        + 3y + 1 = 0$</m> and <m>$x^2 + y^2 + 4x + 3y + 2 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 92 -->
        If <m>$y = mx$</m> be the equation of a chord of the circle <m>$x^2 + y^2 - 2ax = 0$</m>, prove that
        the circle of which this chord is a diameter has the equation <m>$\left(1 + m^2\right)\left(x^2 +
        y^2\right) - 2a(x + my) = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 93 -->
        Find the equation of the circle which passes through the point of intersection of the circles
        <m>$x^2 + y^2 - 6x + 2y + 4 = 0$</m> and <m>$x^2 + y^2 + 2x - 4y - 6 = 0$</m> and whose center lies
        on the line <m>$y = x$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 94 -->
        Prove that the equation <m>$x^2 + y^2 + 2(3 + p)x + 2(3 - p)y + 4 = 0$</m> represents a circle for
        all values of <m>$p$</m>, passing through two fixed points. Find the fixed points.
      </d:listitem>
      <d:listitem>
        <!-- 95 -->
        Find the equation of the circle through the points of intersection of the circles <m>$x^2 + y^2 =
        4a^2$</m> and <m>$x^2 + y^2 - 2x - 4y + 4 = 0$</m> and touching the line <m>$x + 2y = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 96 -->
        Find the equation of the circle which passes through the points of intersection of the circle
        <m>$x^2 + y^2 - x - y = 0$</m> and the line <m>$x + y = 1$</m> and also through the point <m>$(1,
        1)$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 97 -->
        Find the equation of the circle which has for its diameter the chord cut off on the line <m>$px + qy
        - 1 = 0$</m> by the circle <m>$x^2 + y^2 = 𝑎^2$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 98 -->
        The point <m>$A(\alpha, \beta)$</m> is outside the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> and
        <m>$AP, AQ$</m> are tangents to the circle. Find the equation of the circumcircle of <m>$\triangle
        PQR$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 99 -->
        Show that the line <m>$3x - 4y = c$</m> will meet the circle having center at <m>$(2, 4)$</m> and
        the radius <m>$(2, 4)$</m> in real and distinct points if <m>$-35 &lt; c &lt; 15$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 100 -->
        Find the length of the chord <m>$4x - 3y = 5$</m> of the circle <m>$x^2 + y^2 + 3x - y - 10 = 0$</m>.
      </d:listitem>
      <d:listitem>
        <!-- 101 -->
        Prove that the line <m>$y = x + a\sqrt{2}$</m> touches the circle <m>$x^2 + y^2 = a^2$</m>. Also
        find the point of contact.
        </d:listitem>
        <d:listitem>
          <!-- 102 -->
          Find the equation of tangents of the circle <m>$x^2 + y^2 - 6x + 4y - 12 = 0$</m> which are
          parallel to the line <m>$4x + 3y + 5 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 103 -->
          Show that the common tangents to the circles <m>$x^2 + y^2 - 6x = 0$</m> and <m>$x^2 + y^2 + 2x =
          0$</m> form an equilateral triangle.
        </d:listitem>
        <d:listitem>
          <!-- 104 -->
          Three concentric circles of which the biggest is <m>$x^2 + y^2 = 1$</m> have their radii in
          A.P. If the line <m>$y = x + 1$</m> cuts all the circles in real and distinct points then find
          the interval in which the common difference of the A.P. will lie.
        </d:listitem>
        <d:listitem>
          <!-- 105 -->
          If <m>$4l^2 - 5m^2 + 6l + 1 = 0$</m>, then shown that the line <m>$lx + my + 1 = 0$</m> touches a
          fixed circle. Find the center and raidus of the circle.
        </d:listitem>
        <d:listitem>
          <!-- 106 -->
          Find the point <m>$P$</m> on the circle <m>$x^2 + y^2 - 4x - 6y + 9 = 0$</m> such that (i)
          <m>$\angle POX$</m> is minimum (ii) <m>$OP$</m> is maximum, where <m>$O$</m> is the origin and
          <m>$OX$</m> is the <m>$x$</m>-axis.
        </d:listitem>
        <d:listitem>
          <!-- 107 -->
          Show that the circle <m>$x^2 + y^2 - 2ax - 2ay + a^2 = 0$</m> touches both the axes. Also, find the
          point of contact.
        </d:listitem>
        <d:listitem>
          <!-- 108 -->
          Find the length of the chord of the circle <m>$x^2 + y^2 = 16$</m> which bisects the join of the
          points <m>$(2, 3)$</m> and <m>$(1, 2)$</m> perpendicularly.
        </d:listitem>
        <d:listitem>
          <!-- 109 -->
          Find the length of the chord intercepted by the straight line <m>$x - 7y + 4 = 0$</m> and the
          circle <m>$x^2 + y^2 - 14x + 4y + 28 = 0$</m>. Also, find the middle point of the chord.
        </d:listitem>
        <d:listitem>
          <!-- 110 -->
          Find the length of the common chords of the circles <m>$x^2 + y^2 + 3x + 5y + 4 = 0$</m> and
          <m>$x^2 + y^2 + 5x + 3y + 4 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 111 -->
          Find the equation and length of the common chord of the circles <m>$x^2 + y^2 + 2x + 3y + 1 =
          0$</m> and <m>$x^2 + y^2 + 4x + 3y + 2 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 112 -->
          Prove that the length of the common chord of the circlea <m>$(x - a)^2 + (y - b)^2 = c^2$</m> and
          <m>$(c - b)2 + (y - a)^2 = c^2$</m> is <m>$\sqrt{4c^2 - 2(a - b)^2}$</m>. Hence find the condition
          that the two circles may touch each other.
        </d:listitem>
        <d:listitem>
          <!-- 113 -->
           Prove that the length of the common chord of the two circles <m>$x^2 + y^2 + 2hx + a^2 = 0$</m>
           and <m>$x^2 + y^2 - 2ky - a^2 = 0$</m> is <m>$2\sqrt{\frac{\left(h^2 - a^2\right)\left(k^2 +
           a^2\right)}{h^2 + k^2}}$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 114 -->
          Prove that the length of the common chord of the circles <m>$x^2 + y^2 + ax + by + c = 0$</m> and
          <m>$x^2 + y^2 + bx + ay + c = 0$</m> is <m>$\sqrt{\frac{1}{2}(a  + b)^2 - 4c}$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 115 -->
          If the line <m>$px + qy + r = 0$</m> touches the circle <m>$x^2 + y^2 = a^2$</m> then prove that
          <m>$r^2 = a^2\left(p^2 + q^2\right)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 116 -->
          Prove that the line <m>$4x - 3y + 23 = 0$</m> touches the circle <m>$x^2 + y^2 - 6x + 10y =
          66$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 117 -->
          Show that for all values of <m>$\theta$</m>, <m>$x\sin\theta - y\cos\theta = a$</m> touches the
          circle <m>$x^2 + y^2 = a^2$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 118 -->
          If <m>$lx + my = 1$</m> touches the circle <m>$x^2 + y^2 = a^2$</m>, prove that the point <m>$(l,
          m)$</m> lies on the circle <m>$x^2 + y^2 = a^{-2}$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 119 -->
          Find the value of <m>$\lambda$</m> so that the line <m>$3x - 4y = \lambda$</m> may touch the
          circle <m>$x^2 + y^2 - 4x - 8y - 5 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 120 -->
          Show that the line <m>$(x - 1)\cos\theta + (y - 1)\sin\theta = 1$</m> touches a circle for all
          values of <m>$\theta$</m>. Find the circle.
        </d:listitem>
        <d:listitem>
          <!-- 121 -->
          Find those tangents to the circle <m>$x^2 + y^2 = 16$</m> which are parallel to <m>$3x - 16y =
          10$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 122 -->
          Find the equation of the tangents to the circle <m>$x^2 + y^2 - 2x - 4y - 4 = 0$</m> which
          are (i) parallel, (ii) perpendicular to the line <m>$3x - 4y - 1 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 123 -->
          Show that the line <m>$7y - x = 5$</m> touches the circle <m>$x^2 + y^2 - 5x + 5y = 0$</m> and
          find the equation of the other parallel tangent.
        </d:listitem>
        <d:listitem>
          <!-- 124 -->
           Find the equation of the tangent lines to the circle <m>$x^2 + y^2 = 15$</m> which are
           perpendicular to the line <m>$4x - y + 6 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 125 -->
          Find the equation of tangents to the circle <m>$x^2 + y^2 - 6x + 4y - 3 = 0$</m>, which are
          perpendicular to the line <m>$y = 2x - 1$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 126 -->
          Find the equation of the tangents to the circle <m>$x^2 + y^2 = 25$</m>, which make an angle of
          <m>$60^\circ$</m> with the positive direction of <m>$x$</m>-axis.
        </d:listitem>
        <d:listitem>
          <!-- 127 -->
          Find the equation of the family of circles which touch the pair of straight lines <m>$x^2 - y^2 +
          2y - 1 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 128 -->
          Examine if the two circles <m>$x^2 + y^2 - 2x - 4y = 0$</m> and <m>$x^2 + y^2 - 8y - 4 = 0$</m>
          touch each other externally or internally.
        </d:listitem>
        <d:listitem>
          <!-- 129 -->
          Prove that the circle <m>$x^2 + y^2 + 2ax + c^2 = 0$</m> and <m>$x^2 + y^2 + 2by + c^2 = 0$</m>
          touch each other if <m>$\frac{1}{a^2} + \frac{1}{b^2} = \frac{1}{c^2}$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 130 -->
          Prove that <m>$x^2 + y^2 = a^2$</m> and <m>$(x - 2a)^2 + y^2 = a^2$</m> are two equal circles
          touching each other. Find the equation of circles of equal radius touching both the circles.
        </d:listitem>
        <d:listitem>
          <!-- 131 -->
          Prove that the circles <m>$x^2 + y^2 + 2x + 2y + 1 = 0$</m> and <m>$x^2 + y^2 - 4x - 6y - 3 =
          0$</m> touch each other.
        </d:listitem>
        <d:listitem>
          <!-- 132 -->
          Prove that the condition for the circles <m>$(x - a)^2 + (y - b)^2 = c^2$</m> and <m>$(x - b)^2 +
          (y - a)^2 = c^2$</m> to touch each other is <m>$a = b \pm\sqrt{2}𝑐$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 133 -->
          Prove that the circles <m>$x^2 + y^2 + 2ux + 2vy = 0$</m> and <m>$x^2 + y^2 + 2u_1x + 2v_1y =
          0$</m> touch each other if <m>$uv_1 = u_1v$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 134 -->
          Find the length of the chord of the circle <m>$x^2 + y^2 = 4$</m> through <m>$\left(1,
          \frac{1}{2}\right)$</m>, which is of minimum length.
        </d:listitem>
        <d:listitem>
          <!-- 135 -->
          Find the angle that the chord of circle <m>$x^2 + y^2 - 4y = 0$</m> along the line <m>$x + y =
          1$</m> subtends at the circumference the larger segment.
        </d:listitem>
        <d:listitem>
          <!-- 136 -->
          Prove, analytically, that the angle in a semi-circle is a right angle.
        </d:listitem>
        <d:listitem>
          <!-- 137 -->
          A circle of diameter <m>$13$</m>m with the center <m>$O$</m> coinciding with the origin of
          coordinate axes, has the diameter <m>$AB$</m> on the 𝑥-axis. If the length of the chord
          <m>$AC$</m> be <m>$5$</m>m, find the equation of pair of lines <m>$BC$</m>, <m>$C$</m> having two
          possible positions.
        </d:listitem>
        <d:listitem>
          <!-- 138 -->
          Show that the least chord of the circle <m>$x^22 + y^2 + 2gx + 2fy + c = 0$</m> which passes
          through the internal point <m>$(\alpha, \beta)$</m> is equal to <d:sbr />
          <m>$2\sqrt{-\left(\alpha^2 + \beta^2 + 2g\alpha + 2f\beta + c\right)}$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 139 -->
          If the line <m>$a_1x + b_1y + c_1 = 0$</m> and <m>$a_2x + b_2y + c_2 = 0$</m> cut the coordinate
          axes in concyclic points prove that <m>$a_1a_2 = b_1b_2$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 140 -->
          The chord along the line <m>$y - x = 3$</m> of the circle <m>$x^2 + y^2 = k^2$</m>, subtends an
          angle of <m>$30^\circ$</m> in the major segment of the circle cut off by the chord. Find
          <m>$k$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 141 -->
          Prove that the tangent to the circle <m>$x^2 + y^2 = 5$</m> at the point <m>$(1, -2)$</m> also
          touches the circle <m>$x^2 + y^2 - 8x + 6y + 20 = 0$</m> and find its point of contact.
        </d:listitem>
        <d:listitem>
          <!-- 142 -->
          rove that the equation <m>$x^2 + y^2 - 2x - 2\lambda y - 8 = 0$</m>, where <m>$\lambda$</m> is a
          parameter, represents a family of circles passing through two fixed points <m>$A$</m> and
          <m>$B$</m> on the <m>$x$</m>-axis. Also, find the equation of that circle of the family the
          tangents to which at <m>$A$</m> and <m>$B$</m> meet on the line <m>$x + 2y + 5 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 143 -->
          <m>$AB$</m> is a diameter of a circle. <m>$CD$</m> is a chord parallel to <m>$AB$</m> and <m>$2CD
          = AB$</m>. The tangent at <m>$B$</m> meets the line <m>$AC$</m> produced at <m>$E$</m>. Prove that
          <m>$AE = 2AB$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 144 -->
          Two parallel tangents to a given circle are cut by a third tangent at point <m>$A$</m> and
          <m>$B$</m>. If <m>$C$</m> be the center of the given circle, prove that <m>$\angle ACB$</m> is a
          right angle.
        </d:listitem>
        <d:listitem>
          <!-- 145 -->
           A circle of radius <m>$5$</m> meters is having its center at <m>$A$</m> at the origin. Two
           circles II and III with centers <m>$B$</m> and <m>$C$</m> of radii <m>$3$</m> and <m>$4$</m>
           meters respectively touch the circle I and also touch the <m>$x$</m>-axis to the right of
           <m>$A$</m>. Find the equation to any two common tangents to the circles II and III.
        </d:listitem>
        <d:listitem>
          <!-- 146 -->
          Find the equation of the normal to the circle <m>$x^2 + y^2 - 2x - 4y + 3 = 0$</m> at the point
          <m>$(2, 3)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 147 -->
          The extremeties of a diagonal of a rectangle are <m>$(-4, 4)$</m> and <m>$(6, -1)$</m>. A circle
          circumscribes the rectangle and cuts an intercept <m>$AB$</m> on the <m>$y$</m>-axis. Find the
          area of the triangle formed by <m>$AB$</m> and the tangents to the circle at <m>$A$</m> and
          <m>$B$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 148 -->
           Find the equation of the tangent to the circle <m>$x^2 + y^2 - 4x - 6y = 12$</m> at the point
           <m>$(-1, -1)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 149 -->
          Prove that the tangents to the circle <m>$x^2 + y^2 - 7x - 5y + 18 = 0$</m> at the point <m>$(4,
          3)$</m> and <m>$(3, 2)$</m> are parallel to each other.
        </d:listitem>
        <d:listitem>
          <!-- 150 -->
           Find the equations of the tangents to the circle <m>$x^2 + y^2 = 169$</m> at <m>$(5, 12)$</m> and
           <m>$(12, -5)$</m> and prove that they cut at right angles. Find their point of intersection also.
        </d:listitem>
        <d:listitem>
          <!-- 151 -->
          The tangent at the point <m>$(\alpha, \beta)$</m> to the circle <m>$x^2 + y^2 = r^2$</m> cut the
          axes of coordinates in <m>$A$</m> and <m>$B$</m>. Prove that the area of the <m>$\triangle
          OAM$</m> is <m>$\frac{1}{2}.\frac{r^4}{|\alpha\beta|}$</m>, <m>$O$</m> being the origin.
        </d:listitem>
        <d:listitem>
          <!-- 152 -->
          Let 𝐴 be the center of the circle <m>$x^2 + y^2 - 2x - 4y - 20 = 0$</m>. Suppose that the tangents
          at the point <m>$B(1, 7)$</m> and <m>$D(4, -2)$</m> on the circle meet at the point
          <m>$C$</m>. Find the area of the quadrilateral <m>$ABCD$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 153 -->
          Prove that the line <m>$x + y = 5$</m> touches the circle <m>$x^2 + y^2 - 2x - 4y + 3 =
          0$</m>. Find the point of contact.
        </d:listitem>
        <d:listitem>
          <!-- 154 -->
          Prove that the tangent to the circle <m>$x^2 + y^2 = 5$</m> at the point <m>$(1, -2)$</m> also
          touches the circle <m>$x^22 + y^2 - 8x + 6y + 20 = 0$</m>, and find its point of contact.
        </d:listitem>
        <d:listitem>
          <!-- 155 -->
          Show that the circles <m>$x^22 + y^2 - 10x + 4y - 20 = 0$</m> and <m>$x^2 + y^2 + 14x - 6y + 22 =
          0$</m> touch each other. Find the coordinates of the point of contact and the equation of the
          common tangent at the point of contact.
        </d:listitem>
        <d:listitem>
          <!-- 156 -->
          Prove that the line <m>$y = x + 2$</m> touches the circle <m>$x^2 + y^2 = 2$</m>. Find the point
          of contact.
        </d:listitem>
        <d:listitem>
          <!-- 157 -->
          Find the condition that the straight line <m>$lx + my + n = 0$</m> should touch the circle
          <m>$x^22 + y^2 + 2gx + 2fy + c = 0$</m> and also find the coordinates of the point of contact.
        </d:listitem>
        <d:listitem>
          <!-- 158 -->
          If the line <m>$3x + 4y = k$</m> touches the circle <m>$x^2 + y^2 = 10x$</m>, find the value of
          <m>$k$</m> and also the point of contact.
        </d:listitem>
        <d:listitem>
          <!-- 159 -->
          Find the equation of the normal to the circle <m>$x^2 + y^2 = 5$</m> at the point <m>$(1, 2)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 160 -->
          Find the equation of the normal to the circle <m>$x^2 + y^2 = 2x$</m>, which is parallel to the
          line <m>$x + 2y = 3$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 161 -->
          The point <m>$(1, 4)$</m> is inside the circle <m>$S$</m> whose equation is of the form <m>$x^2 +
          y^2 - 6x - 10y + k = 0$</m>. What are the possible values of <m>$k$</m> if the circle <m>$S$</m>
          neither touches the axes not cuts the them?
        </d:listitem>
        <d:listitem>
          <!-- 162 -->
          Find the length of the tangent drawn from the point <m>$(5, 1)$</m> to the circle <m>$x^2 + y^2 +
          6x - 4y - 3 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 163 -->
          Distances from the origin to the centers of the three circles <m>$x^22 + y^22 - 2\lambda x =
          c^2$</m>, where <m>$c$</m> is a constant and <m>$\lambda$</m> a vairable are in G.P. Prove that
          the lengths of tangents drawn from any point on the circle <m>$x^2 + y^2 = c^2$</m> to the three
          circles are also in G.P.
        </d:listitem>
        <d:listitem>
          <!-- 164 -->
          From a point <m>$P$</m> tangents drawn to the circles <m>$x^2 + y^2 + x - 3 = 0, 3x^2 + 3y^2 - 5x
          + 3y = 0$</m> and <m>$4x^2 + 4y^2 + 8x + 7y + 9 = 0$</m> are of equal lengths. Find the  equation
          of the circle thorugh <m>$P$</m> which touches the line <m>$x + y = 5$</m> at the point <m>$(6,
          -1)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 165 -->
          If the length of the tangent from <m>$(f, g)$</m> to the circle <m>$x^2 + y^2 = 6$</m> be twice
          the length of the tangent from <m>$(f, g)$</m> to the circle <m>$x^2 + y^2 + 3x + 3y = 0$</m> then
          will <m>$f^2 + g^2 + 4f + 4g + 2 = 0$</m>?
        </d:listitem>
        <d:listitem>
          <!-- 166 -->
          If the length of the tangent from a point <m>$(f, g)$</m> to the circle <m>$x^2 + y^2 = 4$</m> be
          four times the length of the tangent from it to the circle <m>$x^2 + y^2 = 4x$</m>, show that
          <m>$15f^2 + 15g^2 - 64f + 4 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 167 -->
          Show that the length of the tangent from any point on the circle <m>$x^2 + y^2 + 2gx + 2fy + c =
          0$</m> to the circle <m>$x^2 + y^2 + 2gx + 2fy + c_1 = 0$</m> is <m>$\sqrt{c_1 - 1}$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 168 -->
          Find the point from which the tangents to the three circles <m>$x^2 + y^2 = 1, x^2 + y^2 - 8x + 15
          = 0$</m> and <m>$x^22 + y^2 + 10y + 24 = 0$</m> are equal in length.
        </d:listitem>
        <d:listitem>
          <!-- 169 -->
          Find the point from which the tangents to the three circles <m>$x^2 + y^2 - 4x + 7 = 0, 2x^2 +
          2y^2 - 3x + 5y + 9 = 0$</m> and <m>$x^2 + y^2 + y = 0$</m> are equal in length. Find the length as
          well.
        </d:listitem>
        <d:listitem>
          <!-- 170 -->
          If <m>$A_i$</m> is the center of the circle <m>$x^2 + y^2 + 2g_ix + 5 = 0$</m> and 𝑡𝑖 is the
          length of the tangents from any point to this circle <m>$i = 1, 2, 3$</m>; then show that <m>$(g_2
          - g_3)t_1^2 + (g_3 - g_1)t_2^2 + (g_1 - g_2)𝑡_3^2 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 171 -->
          Show that if the length of the tangent from a point <m>$P$</m> to the circle <m>$x^22 + y^2 =
          a^2$</m> be four times the length of the tangent from it to the circle <m>$(x - a)2 + y^2 =
          a^2$</m>, then <m>$P$</m> lies on the circle <m>$15x^2 + 15y^2 - 32ax + a^2 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 172 -->
          Find the equation of the pair of tangents drawn to the circle <m>$x^2 + y^2 - 2x + 4y = 0$</m>
          from point <m>$(0, 1)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 173 -->
          If from any point on the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m>, tangents are drawn
          to the circle <m>$x^2 + y^2 + 2gx + 2fy + c\sin^2\alpha + \left(f^2 + g^2\right)cos^2\alpha =
          0$</m>, show that the angle between the tangents is equal to <m>$2\alpha$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 174 -->
          Find the equations of tangents to the circle <m>$x^2 + y^2 = 25$</m> which pass through <m>$(1,
          -7)$</m> and show that they are at right angles.
        </d:listitem>
        <d:listitem>
          <!-- 175 -->
          Show that two tangents can be drawn from the point <m>$(9, 0)$</m> to the circle <m>$x^2 + y^2 =
          16$</m>; also find the equation of the pair of tangents and the angle between them.
        </d:listitem>
        <d:listitem>
          <!-- 176 -->
          Find the equations of the tangents through <m>$(7, 1)$</m> to the circle <m>$x^2 + y^2 = 25$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 177 -->
          Find the equation of the pair of tangents from the origin to the circle <m>$x^2 + y^2 + 2gx + 2fy
          + k^2 = 0$</m>, and show that their intercept on the line <m>$y = h$</m> is <m>$\frac{2hk}{k^2 -
          g^2}$</m> times the radius of the circle.
        </d:listitem>
        <d:listitem>
          <!-- 178 -->
          Find the equation of the chord of the circle <m>$x^2 + y^2 + 6x + 8y - 11 = 0$</m>, whose
          middle point is <m>$(1, -1)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 179 -->
          Find the equation of the chord of the circle <m>$x^2 + y^2 + 6x + 8y + 9 = 0$</m> whose mid-point
          is <m>$(-2, -3)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 180 -->
          Find the coordinates of the mid-point of the chord which the circle <m>$x^2 + y^2 + 4x - 2y - 3 =
          0$</m> cuts off on the line <m>$y = x + 2$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 181 -->
          Find the equation of the chord fo contact of the tangents drawn from <m>$(1, 2)$</m> to the circle
          <m>$x^2 + y^2 - 2x + 4y + 7 = 0$</m> .
        </d:listitem>
        <d:listitem>
          <!-- 182 -->
          Tangents are drawn from the point <m>$(h, k)$</m> to the circle <m>$x^2 + y^2 = a^2$</m>. Prove
          that the area of the triangle formed by them and their chord of contact is <m>$\frac{a\left(h^2 +
          k^2 - a^2\right)^{\frac{3}{2}}}{h^2 + k^2}$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 183 -->
          The chord of contact of tangents from a point on the circle <m>$x^2 + y^2 = a^2$</m> to the circle
          <m>$x^2 + y^2 = b^2$</m> touches the circle <m>$x^2 + y^2 = c^2$</m>. Show that <m>$a, b, c$</m>
          are in G.P.
        </d:listitem>
        <d:listitem>
          <!-- 184 -->
          Tangents are drawn to the circle <m>$x^2 + y^2 = 12$</m> at the points where it is met by the
          circle <m>$x^2 + y^2 - 5x + 3y - 2 = 0$</m>; find the points of intersection of these tangents.
        </d:listitem>
        <d:listitem>
          <!-- 185 -->
          Find the equation of the chord of contact of the tangents drawn from an external point <m>$(-3,
          2)$</m> to the circle <m>$x^2 + y^2 + 2x - 3 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 186 -->
          Find the equation of the chord of contact of the tangents drawn from <m>$(5, 3)$</m> to the circle
          <m>$x^2 + y^2 = 25$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 187 -->
          Find the coordinates of the point of intersection of tangents at the points where the line <m>$2x
          + y + 12 = 0$</m> meets the circle <m>$x^2 + y^2 - 4x + 3y - 1 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 188 -->
          The lnegth of tangents from the two given points to a given points to a given circle are
          <m>$t_1$</m> and <m>$t_2$</m>. If the tow given points are conjugate to each other w.r.t. the
          given circle, prove that the distance between the points will be <m>$\sqrt{t_1^2 + t_2^2}$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 189 -->
          Find the area of the triangle formed by the tangents drawn from the point <m>$(4, 6)$</m> to the
          circle <m>$x^2 + y^2 = 25$</m> and their chord of contact.
        </d:listitem>
        <d:listitem>
          <!-- 190 -->
          If <m>$OP$</m> and <m>$OQ$</m> are the tangents from the origin to the circle <m>$x^2 + y^2 + 2gx
          + 2fy + c = 0$</m>, where <m>$P$</m> and <m>$Q$</m> are the points of contact, show that the
          equation of the circumcircle of the <m>$\triangle OPQ$</m> is <m>$x^2 + y^2 + gx + fy = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 191 -->
          Find the point of contact of the tangents to the circle <m>$x^2 + y^2 = 25$</m> that pass through
          the point <m>$(7, 1)$</m> and give the equation of tangents.
        </d:listitem>
        <d:listitem>
          <!-- 192 -->
          Find the equation of the polar of the point <m>$(2, -1)$</m> w.r.t. the circle <m>$x^2 + y^2 - 3x
          + 4y - 8 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 193 -->
          Find the pole of the line <m>$3x + 5y + 17 = 0$</m> w.r.t. the circle <m>$x^2 + y^2 + 4x + 6y + 9
          = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 194 -->
          Show that the polars of the point <m>$(1, -2)$</m> w.r.t. the circle <m>$x^2 + y^2 + 6y + 5 =
          0$</m> and <m>$x^2 + y^2 + 2x + 8y + 5 = 0$</m> coincide. Prove that there is another point, the
          polars of which w.r.t. these circles are the same and find its coordinates.
        </d:listitem>
        <d:listitem>
          <!-- 195 -->
          Let <m>$C$</m> be the center of a circle. The lines <m>$L_1$</m> and <m>$L_2$</m> are the polars
          of the points <m>$A$</m> and <m>$B$</m> respectively. w.r.t. the circle. Perpendiculars
          <m>$AM$</m> and <m>$BN$</m> are dropped from <m>$A$</m> to the line <m>$L_2$</m> and from
          <m>$B$</m> to <m>$L1$</m>. Prove that <m>$CA:CB = AM:BN$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 196 -->
          Find the polar of the point of intersection of the line <m>$4x - y = 11$</m> and <m>$x - 2y =
          1$</m> w.r.t. the circle <m>$x^2 + y^2 = 7$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 197 -->
          Find the polar of the point <m>$(4, -1)$</m> w.r.t. the circle <m>$2x^2 + 2y^2 = 11$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 198 -->
          Find the polar of the point <m>$(1, -5)$</m> w.r.t. the circle <m>$x^2 + y^2 - 8x + 6y + 4 =
          0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 199 -->
           Prove that the polar of the point <m>$(p, q)$</m> w.r.t. the circle <m>$x^2 + y^2 = a^2$</m>
           touches <m>$(x - c)^2 + (y - d)^2 = b^2$</m> if <m>$b^2\left(p^2 + q^2\right) = \left(a^2 - cp -
           dq\right)^2$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 200 -->
          Show that the polar of the origin w.r.t. the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m>
          touches the circle <m>$x^2 + y^2 = a^2$</m> if <m>$𝑐2 = a^2\left(f^2 + g^2\right)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 201 -->
          Prove that if the pole of a straight line w.r.t. the circle <m>$x^2 + y^2 = c^2$</m> lies on the
          circle <m>$x^2 + y^2 = 9c^2$</m>, the line is a tangent to the circle <m>$9x^2 + 9y^2 = c^2$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 202 -->
           Find the pole of the straight line <m>$9x + y - 28 = 0$</m> w.r.t. the circle <m>$2x^2 + 2y^2 -
           3x + 5y - 7 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 203 -->
          Find the pole of the straight line <m>$2x - y + 10 = 0$</m> w.r.t. the circle <m>$x^2 + y^2 - 7x +
          5y - 1 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 204 -->
          Find the pole of the line <m>$ax + by + c = 0$</m> w.r.t. the circle <m>$x^2 + y^2 + 2gx + 2fy + c
          = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 205 -->
          Prove that the polar of any point w.r.t. a circle is perpendicular to the line joining the point
          and center of the circle.
        </d:listitem>
        <d:listitem>
          <!-- 206 -->
          Prove that the polar of a given point w.r.t. any of the circles <m>$x^2 + y^2 + 2px + c = 0$</m>,
          where <m>$p$</m> is a variable, always passes through a fixed point.
        </d:listitem>
        <d:listitem>
          <!-- 207 -->
          If the polar of the point <m>$(\alpha, \beta)$</m> w.r.t. the circle <m>$x^2 + y^2 = a^2$</m>
          touches the circle <m>$(x - a)^2 + y^2 = a^2$</m>, show that <m>$(\alpha, \beta)$</m> is on the
          curve given by <m>$y^2 + 2ax = a^2$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 208 -->
          Verify that the three points <m>$(x_1, y_1), (x_2, y_2)$</m> and <m>$(x_3, y_3)$</m> will be
          collinear if and only if their polars w.r.t. the circle <m>$x^2 + y^2 = a^2$</m> are concurrent.
        </d:listitem>
        <d:listitem>
          <!-- 209 -->
          Show that the circles <m>$x^2 + y^2 - 2x - 6y - 12 = 0$</m> and <m>$x^2 + y^2 + 6x + 4y - 6 =
          0$</m> cut each other orthogonally.
        </d:listitem>
        <d:listitem>
          <!-- 210 -->
          If <m>$S = 0$</m> and <m>$S_1 = 0$</m> are the two circles with radii <m>$a$</m> and <m>$a_1$</m>
          respectively. Show that the circles <m>$\frac{S}{a}\pm \frac{S_1}{a_1} = 0$</m> intersect at right
          angles.
        </d:listitem>
        <d:listitem>
          <!-- 211 -->
          Prove that the two circles pass through the points <m>$(0, 0)$</m> and <m>$(0, -a)$</m> and touch
          the line <m>$y = mx + c$</m> will cut orthogonally if <m>$c^2 = a^2\left(2 + m^2\right)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 212 -->
          Obtain the equation of the circle orthogonal to both the circles <m>$x^2 + y^2 + 3c - 5y + 6 =
          0$</m> and <m>$4x^2 + 4y^2 - 28x + 29 = 0$</m>, and whose center lies on the line <m>$3x + 4y + 1
          = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 213 -->
          Prove that a circle cutting the circle <m>$x^2 + y^2 = 4$</m> orthogonally and having its center
          on the line <m>$2x - 2y + 9 = 0$</m>, passes through two fixed points, and find the points.
        </d:listitem>
        <d:listitem>
          <!-- 214 -->
          Prove that the general equation of circles cutting the circles <m>$x^2 + y^2 + 2g_rx + 2f_ry + c_r
          = 0; r = 1, 2$</m> orthogonally is <m>$\begin{vmatrix}x^2 + y^2 &amp; -x &amp; -y\\c_1 &amp; g_1
          &amp; f_1\\c_1 &amp; g_1 &amp; f_1\end{vmatrix} + k\begin{vmatrix} -x &amp; -y &amp; 1\\g_1 &amp;
          f_1 &amp; 1\\g_2 &amp; f_2&amp; 1\end{vmatrix} = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 215 -->
          For what value of <m>$k$</m> the circles <m>$x^2 + y^2 + 5x + 3y + 7 = 0$</m> and <m>$x^2 + y^2 -
          8x + 6y + k = 0$</m> cut orthogonally.
        </d:listitem>
        <d:listitem>
          <!-- 216 -->
          Find the equation of the circle passing through the origin and cutting the circles
          <m>$x^2 + y^2 - 4x + 6y + 10 = 0$</m> and <m>$x^2 + y^2 + 12y + 6 = 0$</m> at right angles.
        </d:listitem>
        <d:listitem>
          <!-- 217 -->
          Find the equation of the circle passing through the origin and has its center on the line <m>$x +
          y + 4 = 0$</m> and cuts the circle <m>$x^2 + y^2 - 4x + 2y + 4 = 0$</m> orthogonally.
        </d:listitem>
        <d:listitem>
          <!-- 218 -->
          If two circles cut a third circle orthogonally then prove that their common chord
          will pass through the center of the third circle.
        </d:listitem>
        <d:listitem>
          <!-- 219 -->
          If a circle cuts orthogonally three circles <m>$S_1 = 0, S_2 = 0, S_3 = 0$</m>; prove that it cuts
          orthogonally any circle of the form <m>$kS_1 + lS_2 + mS_3 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 220 -->
          Prove that the two circles each of which passes through the points <m>$(0, k)$</m> and
          <m>$(0, -k)$</m> and touches the line <m>$y = mx + b$</m> will cut orthogonally if <m>$b^2 =
          k^2\left(2 + m^2\right)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 221 -->
          Find the general equation of a circle cutting <m>$x^2 + y^2 = c^2$</m> orthogonally and show that
          if it passes through the point <m>$(a, b)$</m> will also pass through the point
          <m>$\left(\frac{c^2a}{a^2 + b^2}, \frac{c^2b}{a^2 + b^2}\right)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 222 -->
          If <m>$P$</m> and <m>$Q$</m> be a pair of conjugate points w.r.t. a circle <m>$S$</m>, prove that
          the circle on <m>$PQ$</m> as a diameter cuts the circle <m>$S$</m> orthogonally.
        </d:listitem>
        <d:listitem>
          <!-- 223 -->
          Prove that the circle <m>$x^2 + y^2 - 6x - 4y + 9 = 0$</m> bisects the circumference of the circle
          <m>$x^2 + y^2 - 8x - 6y + 23 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 224 -->
           Find the equation of a circle which is coaxal with the circles <m>$2x^2 + 2y^2 - 2x + 6y - 3 =
           0$</m> and <m>$x^2 + y^2 + 4x + 2y + 1 = 0$</m>. It is given that the center of the circle to be
           determined lies on the radical axis of these circles.
        </d:listitem>
        <d:listitem>
          <!-- 225 -->
          If the radical axis of the circles <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> and <m>$2x^2 + 2y^2 + 3x
          + 8y + 2c = 0$</m> touches the circle <m>$x^2 + y^2 + 2x - 2y + 1 = 0$</m>, show that either
          <m>$g = \frac{3}{4}$</m> or <m>$f = 2$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 226 -->
          Find the general equation of circles, any two of which have the same radical axis as that of the
          circles <m>$x^2 + y^2 + 2x + 4y - 6 = 0$</m> and <m>$x^2 + y^2 = 4$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 227 -->
          The equations of three circles are <m>$x^2 + y^2 = 1, x^2 + y^2 - 8x + 15 = 0, x^2 + y^2 + 10y +
          24 = 0$</m>. Determine the coordinates of the point such that the tangents drawn from it to the
          three circles are equal in length.
        </d:listitem>
        <d:listitem>
          <!-- 228 -->
          The polars of a point <m>$P$</m> w.r.t. two given circles meet in a point <m>$Q$</m>; show that
          the radical axis of the circles bisedct the line <m>$PQ$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 229 -->
          Show that the locus of a point such that the ratio of its distances from two given points is a
          constant, is a circle. Hence, show that this circle cannot pass through the given points.
        </d:listitem>
        <d:listitem>
          <!-- 230 -->
          Two rods of lengths <m>$a$</m> and <m>$b$</m> slide along the axes in a manner that their ends are
          always concyclic. Find the locus of the center of the circle passing through these ends.
        </d:listitem>
        <d:listitem>
          <!-- 231 -->
          Two straight lines rotate about two fixed points. If they start from their positions of
          coincidence such that one rotates at the rate double that of the other. Prove that locus of their
          point of intersection is a circle.
        </d:listitem>
        <d:listitem>
          <!-- 232 -->
          A circle of radius <m>$r$</m> passes through the origin <m>$O$</m>, and cuts the axes at
          <m>$A$</m> and <m>$B$</m>. Let <m>$P$</m> be the foot of the perpendicular from the origin to the
          line <m>$AB$</m>. Find the equation of the locus of <m>$P$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 233 -->
          Show that the locus of points from which the tangents drawn to a circle are orthogonal, is a
          concentric circle or find the equation of the cirector circle of the circle <m>$x^2 + y^2 =
          a^2$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 234 -->
          Find the locus of the point of the point of intersection of tangents to the circle <m>$x =
          a\cos\theta, y = a\sin\theta$</m> at points whose parametric angles differ by
          <m>$\frac{\pi}{3}$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 235 -->
          The circles <m>$x^2 + y^2 + 2ax - c^2 = 0$</m> and <m>$x^2 + y^2 + 2bx - c^2 = 0$</m> intersect at
          <m>$A$</m> and <m>$B$</m>. A line through <m>$A$</m> meets one circle at <m>$P$</m> and a parallel
          line through <m>$B$</m> meets at the other circle at <m>$Q$</m>. Show that the locus of the
          mid-point of <m>$PQ$</m> is a circle.
        </d:listitem>
        <d:listitem>
          <!-- 236 -->
          Find the condition that the chord of contact of tangents from the point <m>$(\alpha, \beta)$</m>
          to the circle <m>$x^2 + y^2 = a^2$</m> should subtend a right angle at the center. Hence, find the
          locus of <m>$(\alpha, \beta)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 237 -->
          A tangent is drawn to each of the circle <m>$x^2 + y^2 = a^2$</m> and <m>$x^2 + y^2 =
          b^2$</m>. Show that if the two tangents are mutually perpendicular, the locus of their point of
          intersection is a cricle concentric with the given circle.
        </d:listitem>
        <d:listitem>
          <!-- 238 -->
          S how that the locus of the point, the tangents from which to the circle <m>$x^2 + y^2 = a^2$</m>
          include a constant angle <m>$\alpha$</m> is <m>$\left(x^2 + y^2 - 2a^2\right)^2\tan^2\alpha =
          4a^2\left(x^2 + y^2 - a^2\right)$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 239 -->
          A straight line passes through the fixed point <m>$(h, k)$</m>. Find the locus of the foot of the
          perpendicular drawn to it from the origin.
        </d:listitem>
        <d:listitem>
          <!-- 240 -->
          <m>$O$</m> is a fixed point and <m>$AP$</m> and <m>$BQ$</m> are two fixed parallel straight lines;
          <m>$BOA$</m> is perpendicular to both and <m>$\angle POQ$</m> is a right angle. Prove that the
          locus of the foot of the perpendicular drawn from <m>$O$</m> upon <m>$PQ$</m> is the circle on
          <m>$AB$</m> as diameter.
        </d:listitem>
        <d:listitem>
          <!-- 241 -->
          A variable circle passes through the point <m>$P(1, 2)$</m> and touches the <m>$x$</m>-axis; show
          that the locus of the other end of the diameter through <m>$P$</m> is <m>$(x - 1)^2 = 8y$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 242 -->
          Find the locus of a point, which is such that the lengths of the tangents from it to two
          concentric circles <m>$x^2 + y^2 = a^2$</m> and <m>$x^2 + y^2 = b^2$</m> vary inversely as their
          radii.
        </d:listitem>
        <d:listitem>
          <!-- 243 -->
          A point moves such that the sum of squares of its distances from the sides of a square of side
          unity is equal to <m>$9$</m>. Show that the locus of a circle whose center is coincides with the
          center of the square. Also, find its radius.
        </d:listitem>
        <d:listitem>
          <!-- 244 -->
          Find the locus of the center of the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> when the pair of
          tangents drawn from the origin to the circle are perpendicular to each other.
        </d:listitem>
        <d:listitem>
          <!-- 245 -->
          Determine the locus of centers of the circles which touches the two circles <m>$x^2 + y^2 =
          a^2$</m> and <m>$x^2 + y^2 = 4ax$</m> externally.
        </d:listitem>
        <d:listitem>
          <!-- 246 -->
          Find the locus of the centers of the circles which cut the circles <m>$x^2 + y^2 + 4x - 6y + 9 =
          0$</m> and <m>$x^2 + y^2 - 4x + 6y + 4 = 0$</m> orthogonally is <m>$8x - 12y + 5 = 0$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 247 -->
          Find the locus of the foot of the perpendicular drawn from a fixed point on the <m>$x$</m>-axis to
          any tangent to the circle <m>$x^2 + y^2 = a^2$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 248 -->
          The tangent at any point <m>$P$</m> on the circle <m>$x^2 + y^2 = 2$</m> cuts the axes in
          <m>$L$</m> and <m>$M$</m>. Find the locus of the middle point of <m>$LM$</m>.
        </d:listitem>
        <d:listitem>
          <!-- 249 -->
          A triangle has two of its sides along the axes, its third side touches the circle <m>$x^2 + y^2 -
          2ax - 2ay + a^2 = 0$</m>. Find the equation of the locus of the circumcenter of the triangle.
        </d:listitem>
        <d:listitem>
          <!-- 250 -->
          The point <m>$A(1, 5)$</m> is joined to any point <m>$P$</m> of the circle <m>$x^2 + y^2 =
          4$</m>. Find the locus of the middle point of <m>$AP$</m> as <m>$P$</m> moves on the circle.
        </d:listitem>
        <d:listitem>
          <!-- 251 -->
          Chords of the circle <m>$x^2 + y^2 + 2gx + 2fy + c = 0$</m> drawn through a fixed point <m>$A(a,
          b)$</m>. Find the locus of the mid-points of these chords and interpret the locus.
        </d:listitem>
        <d:listitem>
          <!-- 252 -->
          A straight line moves so that the algebraic sum of the perpendiculars drawn to it from two fixed
          points is constant, show that the line always touches a fixed circle.
        </d:listitem>
    </d:orderedlist>
  </d:sect1>
</d:chapter>
